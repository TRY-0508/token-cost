# LLM Agent 系统中 Token 与时间的记录：粒度体系、优化策略与发展脉络

> **覆盖 44 篇论文（2023–2026）。** 本文围绕一个工程问题——"在 LLM Agent 系统中应当记录哪些 token 和时间信息、用于什么决策"——梳理出 **五级记录粒度体系** 与 **四阶段研究演进** 的交叉视图，并提炼出贯穿全文的四条共性主题。

> **阅读导航**：本文有两条主线——**纵向**（第二章）讲"在什么粒度上记录"，**横向**（第三至六章）讲"研究如何一步步深入"。如果你只关心"怎么落地做记录"，重点读第二章；如果你想理解"这个领域在往哪个方向发展"，重点读各阶段的开头段落和阶段小结。第七章是两条主线的交叉汇总，第八章是跨阶段的通用经验。

---

## 一、问题与全景

### 1.1 Token 消耗的增长趋势

LLM Agent 系统的 token 消耗在过去两年经历了快速增长。Token Economics [2605.09104] 引用的 OpenRouter 平台数据显示，其周 token 处理量从 2024 年 12 月的 0.4 万亿上升到 2026 年 3 月的 27.0 万亿，15 个月内增长 68 倍。这一增长既来自用户规模扩张，也来自单任务消耗量的上升。

Harness Effect [2607.06906] 将单任务消耗上升的现象命名为 **token maxing**：开发者在 prompt 中加入更多上下文、让模型执行更多推理步、让工具返回完整输出而非摘要、让 Agent 进行更多轮交互。每一次单独的选择都是合理的工程权衡，但累积起来，单任务的 token 增速超过了任务本身的价值增速。同期主流 LLM API 的 token 单价持续下降，但单价下降反而刺激了更激进的消耗，总支出依然在上升。换言之，token maxing 的隐蔽性在于：单价下降制造了"成本在降低"的假象，掩盖了单任务消耗量的增长。两者方向相反但总账单方向取决于哪一个变化更大——而实证表明消耗量的增速超过了单价的降速。

### 1.2 核心工程问题

面对这一局面，一个基础的工程问题自然浮现：

> **在 LLM Agent 系统中，应当在什么粒度上记录哪些 token 和时间信息？这些记录如何支撑后续的优化决策？**

这个问题比表面上更复杂。一次典型的 Agent 任务涉及几十到几百次 LLM 调用，每次调用产生三类 token（输入、输出、推理），延迟从几百毫秒到几十秒不等，工具调用的时间可能更长。两个极端都不行：

- **只记总量**——"跑了 50 万 token、花了 $3"——无法回答消耗发生在哪一阶段、哪个 Agent 是消耗大户，以及必要消耗与可优化消耗各占多少。
- **记到极细**——记录本身也带来工程开销，且原始数据量大到无法直接回答业务问题。举例来说，如果每次 LLM 调用的每个 token 都打上时间戳和来源标签，一次 50 万 token 的任务会产生数十万条原始记录，存储和查询这些数据的开销可能超过 token 消耗本身，且零散的 token 级数据无法直接回答"哪个 Agent 阶段是消耗瓶颈"——这需要对这些数据进行聚合和归因后才能得到。

合适的层级介于两者之间，且要随系统能力的演进而扩展。本文梳理的工作可以读作对这一问题的逐级回答。

### 1.3 双轴视角：粒度与演进

回答上述问题需要两个轴同时看：

- **粒度轴（纵）**：记录可以在五个抽象层级上发生——调用、步、任务、会话、系统。下层是上层的数据来源，上层为下层提供聚合语境。
- **时间轴（横）**：研究工作按认知深化的顺序经历了四个阶段——度量（消耗在哪）、控制（运行时怎么限制）、优化（哪些消耗本不该存在）、闭环（系统能不能自己观测和调整）。

四个阶段的速览如下：

| 阶段 | 核心问题 | 新激活的记录粒度 | 代表工作 |
|------|---------|---------------|---------|
| **一：度量** | Token 消耗在哪里？ | 调用级（原子）+ 任务级（聚合） | Tokenomics, Token Economics |
| **二：控制** | 能否在运行时限制消耗？ | + 步级（控制输入）+ 任务级（控制反馈） | BAVT, ARC, Stackelberg, Token Budgets |
| **三：优化** | 哪些消耗是设计上不必要的？ | + 会话级（跨任务趋势）+ 系统级（跨 Agent 视角） | SimpleMem, CWL, SVR-MAD, HiveMind 等 |
| **四：闭环** | 系统能自己观测和调整吗？ | 五级全覆盖，记录直接进入自治决策回路 | Harness Effect, AHE, Early Diagnosis, GATS 等 |

粒度的逐阶段填充：

```
              阶段一   阶段二   阶段三   阶段四
  系统级       —       —      ✓新增     ✓
  会话级       —       —      ✓新增     ✓
  任务级     ✓聚合   ✓反馈     ✓        ✓
  步级         —     ✓新增     ✓        ✓
  调用级     ✓新增     ✓       ✓        ✓
```

`✓` 表示该粒度上有活跃研究工作；`—` 表示尚未涉及；`✓新增` 标记该阶段首次激活的粒度。

---

## 二、五级记录粒度体系

在 LLM Agent 系统中，对 token 和时间信息的记录天然存在五个抽象层级。它们之间是严格的依赖关系：下层是上层的数据来源，上层为下层提供聚合语境。

### 2.1 总览

| 粒度 | 记录字段 | 数据来源 | 首次建立 |
|------|---------|---------|---------|
| **调用级** | input/output/reasoning/cached tokens, ttft_ms, total_latency_ms, cost_usd, model, provider, success | API 返回值 + 客户端计时 | 阶段一 |
| **步级** | step_index, step_duration_ms, tool_calls[], tool_results_size, context_utilization, belief_progress | 客户端 wrapper 计算 | 阶段二 |
| **任务级** | total_tokens(按 input/output/reasoning 分项), total_steps, total_tool_calls, task_status, wall_time_ms, cost_total_usd | 调用级 + 步级聚合 | 阶段一（聚合）/ 阶段二（控制回路） |
| **会话级** | running_token_total, compaction_count, cache_hit_rate, memory_retrieval_count, memory_store_size, session_cost_total | 任务级聚合 | 阶段三 |
| **系统级** | agent_count, agent_roles[], per_agent_token_share{}, communication_tokens, communication_rounds, orchestration_overhead, dead_agent_count, queue_wait_time_ms | 各 Agent 调用记录聚合 | 阶段三 |

### 2.2 各级详解

**调用级（Call Level）**：不可再分的原子数据，来自每次 LLM API 调用的返回值与客户端计时。调用级是一切上层分析的基石——如果这一层不准确，所有上层分析都建立在错误数据之上。将每次调用的 token 按阶段归因可以定位消耗热点，将每次调用的消耗与预算上限比较可以实现硬预算控制，分析历史调用数据中被缓存的 token 比例可以评估 prompt cache 策略的实际效果。

**步级（Step Level）**：一次 Agent 步骤通常对应一次完整的 ReAct 循环（接收观测→推理→生成动作→工具返回）。在 ReAct 模式下，Agent 并不一次性生成最终答案，而是在"思考（Reasoning）"和"行动（Action）"之间交替：先分析当前观测，决定需要什么信息或操作，调用对应工具，获得工具返回结果，再基于结果继续分析。步级记录在调用级基础上增加了语义信号：使用了什么工具、工具返回了多大内容、上下文窗口占用了多少比例、Agent 自身评估的进度变化。步级记录驱动了运行时动态调整（BAVT 的探索/利用切换）、运行时空转检测（SupervisorAgent 和 Early Diagnosis 的检测依据）、训练信号区分（ActFocus 的动作/推理 token 分类）。

**任务级（Task Level）**：一个任务从开始指令到最终输出，包含若干步骤。任务级记录不需要额外的计量代码——总 token、总步数、总工具调用数、任务状态、端到端耗时、总花费都是聚合计算的结果。任务级是策略对比的基础单元：Harness Effect 对比不同编排层的效果，ARC 训练配置选择策略，SAS vs MAS 实现公平对比，均依赖任务级记录。

**会话级（Session Level）**：由多个相关任务的记录聚合得到。关注跨任务的趋势——缓存命中率在上升还是下降、记忆系统在积累有用信息还是堆砌垃圾、驱逐策略是否在控制上下文增长的同时保持了精度。CWL 依靠会话级的依赖图和驱逐历史验证了 80M token 无退化的效果；Shared Selective Memory 通过会话级完成率追踪发现全量历史反而劣化性能（79% → 71%）。

**系统级（System / MAS Level）**：将各 Agent 的调用记录按系统全局视角重新聚合。单个 Agent 内部的记录无法回答跨 Agent 的问题——哪个 Agent 消耗了最多 token、Agent 间的通信占用了多少比例、编排层自身的开销有多大、多少 Agent 因资源竞争而失效。SVR-MAD 利用系统级的通信 token 量判断哪些 Agent 对之间的通信可以剪枝；HiveMind 利用系统级的并发 Agent 死亡数和排队延迟发现了调度失效问题。

五级粒度之间存在严格的聚合依赖：调用级是唯一从外部系统（LLM API）直接获得数据的层级，步级通过包装调用级数据并附加语义标签得到，任务级对步级做求和/计数聚合，会话级对任务级做趋势统计，系统级对多个 Agent 的任务/会话级数据做跨 Agent 聚合。这意味着任何上层分析的准确性都受限于最底层调用级数据的准确性——如果调用级的 token 计数因为 budget alias 或 double-spend 等并发问题而出错（参见 4.4 节 Token Budgets 的实证），所有上层分析的结论都不可靠。

### 2.3 各级记录驱动优化的方式

| 粒度 | 驱动优化的方式 | 代表案例 |
|------|-------------|---------|
| 调用级 | 消耗归因、预算硬控制、缓存评估 | Tokenomics 按阶段归因（CR 占 59.4%）；Token Budgets 编译时检查 |
| 步级 | 探索/利用切换、空转检测、训练信号重加权 | BAVT 预算感知搜索；ActFocus 动作/推理 token 梯度区分 |
| 任务级 | 策略 A/B 对比、架构选型、编排效率归因 | Harness Effect 编排层因果实验；SAS vs MAS 等预算对比 |
| 会话级 | 长期效率趋势、记忆健康度、驱逐效果评估 | CWL 80M token 无退化；Shared Selective 完成率追踪 |
| 系统级 | 通信效率诊断、负载均衡、并发健康度 | SVR-MAD 通信剪枝（-61%）；HiveMind 并发调度（失败率 72%→18%） |

---

## 三、第一阶段：度量

> 核心问题：**Token 消耗在哪里？** 在 2025 年之前，业界对 LLM Agent 的 token 成本讨论几乎全部停留在总量层面。总量级别的信息对诊断和优化几乎没有帮助。

### 3.1 度量缺失的困境

Token Economics [2605.09104] 通过综述性质的工作为这一困境提供了理论层面的分析。其核心贡献是提出了一个统一的分析框架，将 token 在 LLM Agent 系统中的角色分为三个维度：

- **token 作为直接成本**——每次 Agent 调用 LLM 的 token 数直接对应 API 计费。从这个角度看，记录 token 等同于记录开支。
- **token 作为通信开销**——在多 Agent 系统中，Agent 间的消息本身就是 token。通信开销可能超过实际解决问题的开销。
- **token 作为效率度量**——"花 10 万 token 完成 5 个任务"和"花 10 万 token 完成 10 个任务"，后者的效率是前者的两倍。

基于这三个维度，Token Economics 构建了一个四层分析框架：(1) 单 Agent 层（如何在 token 类型间最优分配预算）；(2) 多 Agent 层（如何最小化 Agent 间通信）；(3) 生态层（多租户共享资源时的定价与调度）；(4) 安全层（攻击者注入的 token 浪费如何防御）。这一框架的价值在于为后续所有工作提供了共同的分析语言和分类体系。但框架本身不提供数据。

### 3.2 Tokenomics：调用级记录与阶段级归因

第一个填补"数据真空"的工作是 Tokenomics [2601.14470]（MSR 2026）。研究设定：在 ChatDev 上用 GPT-5 reasoning 模型运行了 30 个完整的软件开发任务，对每次 LLM 调用记录输入 token、输出 token、推理 token。仅这个设定就蕴含了重要的方法论选择——**不在模拟环境中推算，而是在真实 Agent 框架上跑真实任务、用真实的 API 返回数据**。

但只记录每次调用的三类 token 是不够的——30 个任务 × 每个任务几十到上百次调用，产生数千条原始记录。Tokenomics 的核心方法创新是**将调用级记录按业务阶段聚合**：

1. ChatDev 框架内部有预设的工作阶段（需求分析、代码生成、代码审查、测试等）。
2. Tokenomics 将这些内部阶段映射为标准 SDLC 阶段——Design、Coding、Code Completion、Code Review、Testing、Documentation。
3. 将所有调用级记录按所属阶段汇总。

这一步映射将"总数 50 万 token"这样无法指导优化的总量数据，转化为"Code Review 阶段消耗了 29.7 万 token（59.4%），Coding 阶段消耗了 4.3 万 token（8.6%），Design 阶段消耗了 1.2 万 token（2.4%）"这样可以直接指导优化的分布图景。它也建立了一个方法论范式——**两级记录方案：调用级计量 + 业务阶段归因**——被后续几乎所有工作继承和扩展。

### 3.3 三个关键发现

**发现一：Code Review 阶段平均消耗 59.4% 的总 token。** 这意味着在一个软件工程 Agent 系统中，主要的 token 开销不是生成代码本身，而是"审查→发现不足→修改→再审查"的迭代循环——每次循环都需要将当前版本的代码完整地输入到 LLM 的上下文窗口。这个发现直接影响了后续 Harness Effect 的"结构化增量压缩"机制。

**发现二：在所有阶段中，输入 token 平均占 53.9%，输出占 24.4%，推理占 21.6%。** 但 Coding 阶段是显著的异常值——输出 token 占 58.0%，输入仅占 6.9%。这说明不同阶段的优化策略不应相同：Code Review 应压缩输入，Coding 应压缩输出。输入 token 始终是最大单项（53.9%），为后续大量工作集中在上下文压缩方向提供了实证依据。

**发现三：各 SDLC 阶段的 token 类型分布差异显著，不存在"平均分布"。**

| SDLC 阶段 | 输入占比 | 输出占比 | 推理占比 | 优化策略方向 |
|-----------|---------|---------|---------|------------|
| Design | ~55% | ~20% | ~25% | 压缩输入 |
| Coding | **6.9%** | **58.0%** | ~35% | 压缩输出 |
| Code Completion | ~50% | ~30% | ~20% | 均衡优化 |
| Code Review | **~60%** | ~20% | ~20% | 压缩输入（最优先） |
| Testing | ~60% | ~20% | ~20% | 压缩输入 |
| Documentation | ~80% | ~10% | ~10% | 极度压缩输入 |

这个发现告诉后续工作：不存在一个统一的"token 优化配方"可以不做区分地应用到所有阶段。这一洞察被推广到更广泛的场景中：在 MAS 系统中，不同 Agent 角色也很可能有截然不同的 token 消耗模式，应该按角色制定不同的优化策略。

### 3.4 阶段小结

Tokenomics 的 30 个任务规模有限，仅覆盖了单一框架（ChatDev）和单一模型（GPT-5），Code Completion 和 Testing 的样本量也较小。更大的局限在于：**度量告诉你消耗在哪里，但度量本身不会减少消耗。** 知道 Code Review 消耗 59.4% 之后，下一步是用这些数据做控制还是做优化，就进入了阶段二和阶段三的范畴。

---

## 四、第二阶段：预算控制

> 核心问题：**能否在运行时主动限制消耗？** 既然阶段一已经建立了调用级的精确计量，自然的问题是能否利用这些数据在运行时主动控制 token 消耗，或者在编译时就保证不超预算。这一阶段将**步级**和**任务级**引入了控制回路。

### 4.1 步级价值评分驱动搜索：BAVT

BAVT [2603.12634] 解决的问题是推理树搜索中的效率困境。复杂推理任务需要一个 Agent 在推理树上做搜索——尝试不同的推理路径，评估哪条路径最有希望。传统方案要么无限制地探索（浪费 token），要么用硬编码的步数上限截断（不灵活）。共同问题是：**预算是一个外在限制，而非搜索策略的内在驱动力**。

BAVT 的核心想法是将预算信息直接编码到搜索策略中：

- **残差价值评分**：推理树每个节点衡量的不是"当前状态有多好"（绝对评分），而是"从上一步到现在，取得多少进展"（残差评分）。选择残差评分是因为 LLM 在绝对自评估上已知存在过置信问题。
- **预算感知选择**：节点被选中的概率 = `value_score^(1/剩余预算比例)`。预算充足时（已消耗 10%），指数接近 1，各节点近似等概率被探索。预算紧张时（已消耗 90%），指数升至约 10，仅高价值节点有显著被选概率。

该公式的过渡是完全连续且平滑的——无需预设探索/利用切换的阈值，预算水平本身即作为实时变化的阈值。在四个多跳 QA 基准和两个模型家族上的实验中，严格低预算约束下 BAVT 的准确率超过了基线在 **4 倍资源分配**下的水平。论文还提供了理论收敛保证：在显式有限预算约束下，达到终止答案的概率 ≥ 1-ε。

> 该公式选择指数形式而非线性形式的理由在于：线性加权（如 `概率 ∝ 价值 × 预算比例`）在预算紧张时仍会给低价值节点分配不可忽略的概率，导致资源分散。指数形式在预算紧张时（指数大）将选择分布急剧收窄到最高价值节点，而在预算充足时（指数接近 1）退化为近乎均匀的探索——实现了单一参数连续控制探索-利用权衡的效果。

**对记录体系的贡献**：引入了**步级记录**作为控制输入。调用级数据只能告诉 Agent"你花了多少"，但无法告诉 Agent"这一步花得值不值"。步级记录填补了这个空白。

### 4.2 任务级按难度分配资源：ARC

BAVT 关注的是单个任务内部如何在不同步骤之间分配预算。ARC [2602.11574] 关注的是一个互补的问题：**在不同任务之间，如何根据任务难度分配不同的总预算？**

ARC 的出发点是大多数 Agent 系统采用"一刀切"的配置方案——所有查询使用相同的工具集、prompt 模板、token 预算、模型选择。但查询的难度差异巨大。"翻译这句话"和"分析这个代码仓库的安全漏洞"不应该用同一套资源配置。

ARC 将配置选择建模为半马尔可夫决策过程（SMDP）。配置空间包含四个维度：工具集、prompt 模板、token 预算、模型选择。一个轻量级的分层策略通过强化学习训练——学习信号来自任务完成后的反馈：质量得分和 token 消耗。

ARC 的实验设计中有一个关键的公平性考量：**所有对比都是在"预算匹配"的基线上进行的**。如果 ARC 为某个查询分配了 5000 token 预算，基线也会获得 5000 token。区别仅在于：ARC 对不同查询分配不同配置，而基线对所有查询都用同一个固定配置。这排除了"ARC 只是多给 token 而已"的替代解释。

> 推理准确率平均 **+31.3%**，工具使用准确率 **+13.95%**。τ-Bench 的 Airline 场景下 Pass¹ 成功率从 9.0% 翻倍到 18.0%。

**对记录体系的贡献**：引入了**任务级反馈**作为控制输入。策略的训练需要有任务级的数据——每个配置选择最终对应什么样的任务质量和 token 消耗。

### 4.3 双层博弈与策略修复：Stackelberg

Stackelberg [2606.23026]（Tencent 技术报告）将预算控制进一步抽象为一个两层决策问题：

| 层 | 角色 | 决策内容 |
|----|------|---------|
| 上层（Controller） | 设定目标 | 质量目标 + 成本上限（如"80% 质量，总成本 ≤ $0.05"） |
| 下层（Executor） | 选择执行参数 | 用多少上下文、prompt 详细程度、开放哪些工具访问 |

核心技术贡献是"**策略修复**"（policy repair）步骤。在模拟环境中学到的策略如果直接部署到真实 API 上，会因为模拟与真实之间的偏差而出问题。策略修复通过真实 API 的校准运行，将学到的策略投影修正到真实环境中可以工作的操作点。

> 300 轮真实 API 评估：token 成本减少 **17.4%**（统计显著，Welch p=0.022），质量变化不显著（p=0.44）。

### 4.4 编译时硬保证路线：Token Budgets

Khan 的 Token Budgets [2606.04056] 与前三个方案采取了完全不同的技术路线：不在运行时动态调整，而是通过编译时类型系统保证超支在代码层面不可能发生。

**第一部分：事故实证。** 从 21 个编排框架（2023–2026 年间活跃的开源或商业框架）的 GitHub issues 中，收集了 **63 起已确认的预算超支生产事故**。事故被两名评分者独立归类，最终形成 8 个失败模式簇，评分者间信度 Cohen's κ = 0.837。最常见和最危险的四种：

| 失败模式 | 描述 |
|---------|------|
| 重试循环无上限 | API 失败后无最大重试限制，一次循环一夜消耗数千美元 |
| 预算别名 | 多 Agent 共享同一计数器变量，并发修改导致计数错乱 |
| 委托后继续消耗 | 主 Agent 委托子任务给子 Agent 后，主 Agent 自身代码路径仍在消耗同一份预算 |
| 双花 | 两个并发操作同时读取"剩余 1000 token"，各自扣减后写入，最终只扣了 1000 |

这些事故说明了一个重要事实：**运行时 wrapper 是可以被绕过的**——不是因为设计者不谨慎，而是因为在并发、异步、回调密集的 Agent 代码中，保证计数器逻辑在所有异常路径上都正确极其困难。

**第二部分：技术方案。** 用 Rust 的 affine type 系统将 token 预算实现为一个类型层面的对象。三个关键属性由编译器在编译时强制检查：不可复制（无法 clone）、不可双花（move semantics 保证每次操作后原值失效）、委托后自动失效（split 操作将预算拆分为两个子预算，原预算不可再用）。

**第三部分：实验结果。** Python asyncio 版本的计数器超支 **30/30 次**，Rust affine type 版本 **0/30 次**。跨 5 个运行时、3 个提供商、4 个温度层级的 160 次测试中：**零次上限违规，零次误拒绝**。论文同时明确了重要限定：**对于单 Agent 场景，4 行 Python 计数器可以达到完全相同的结果**。Affine type 的独特价值在于多 Agent 委托场景下的操作防错。

### 4.5 阶段小结：两条技术路线

阶段二的工作在技术路线上出现了清晰的分化：

| 对比维度 | 运行时动态调整 | 编译时硬保证 |
|---------|-------------|------------|
| **代表工作** | BAVT, ARC, Stackelberg | Token Budgets |
| **技术手段** | 步级价值评分、SMDP、双层博弈 | Rust affine type |
| **记录需求** | 步级进展数据 + 任务级质量反馈 | 调用级 token 计数 + 所有权追踪 |
| **适用场景** | 预算充足但希望更节约 | 超支即为事故的生产环境 |
| **核心优势** | 灵活应对任务难度变化 | 逻辑上不可绕过 |
| **核心代价** | 需要较复杂的运行时数据基础设施 | 需要支持 affine type 的技术栈 |

两条路线的共同前提是阶段一建立的两级记录方案：没有精确的调用级 token 计数，预算控制就无从谈起。

---

## 五、第三阶段：结构性优化

> 核心问题：**有哪些消耗是系统设计层面就不该存在的？** 度量告诉你消耗在哪里（阶段一），控制能让你限制消耗量（阶段二），但最根本的问题是消除结构性浪费。这一阶段最重要的技术特征是**记录粒度的向上扩展**——**会话级**和**系统级**记录的建立和使用。

### 5.1 上下文管理与记忆系统

输入 token 在所有阶段中平均占 53.9%（Tokenomics 的发现二），所以减少不必要的上下文加载是这一阶段的主要优化方向。上下文管理的一系列工作直接推动了会话级记录的建立——区分"有用的上下文"和"冗余的上下文"需要跨步骤甚至跨任务的信息。

十四项工作的全景如下，后续按方向展开：

| 工作 | 优化角度 | 核心技术 | 粒度 | 关键效果 |
|------|---------|---------|------|---------|
| LLMLingua | 通用 prompt 压缩（基础） | 困惑度驱动的粗到细 token 级压缩 + 预算控制器 | 调用 | 最高 20× 压缩，性能基本保持 |
| LongLLMLingua | 长上下文 prompt 压缩 | 关键信息密度与位置感知的压缩 | 调用 | NQ 性能 +21.4%（4× token 减少）；LooGLE 成本 -94% |
| TCRA-LLM | RAG 场景 prompt 压缩 | T5 摘要压缩 + 语义级词项压缩 | 调用 | 输入 token -65%，准确率不降 |
| SimpleMem | 让记忆更浓缩 | 三阶段流水线：结构化压缩、在线合成、意图检索 | 步+会话 | 推理 token 减少 30 倍，F1 提升 26.4% |
| RecMem | 不该记的时候不记 | 检测语义簇形成后，延迟触发 LLM 做一次性整合 | 会话 | 记忆构建 token 减少 87% |
| CWL | 删除已完成且不再需要的 | 依赖图驱逐，按步骤效果的持久性而非时间顺序决定删除 | 会话 | 80M token 连续会话无精度退化 |
| PEEK | 缓存结构方向，不是轨迹 | 蒸馏、制图、淘汰三个模块维护固定大小的上下文地图 | 会话 | 成本降低 1.7 至 5.8 倍 |
| Stateful ReAct | 不让历史重复堆积 | 类型化持久状态在轮次间传递，替代对话历史堆叠 | 步+会话 | O(n²)→O(n)，超参数调优减 90%，代码优化减 52% |
| ACE | 删了还能恢复 | 原始和压缩双副本存储，每步可逆选择展示粒度 | 会话 | 适配 4 个 Agent 框架 |
| VISTA | 让 Agent 看到自己的上下文 | 暴露每个上下文块的大小、年龄和访问次数的仪表盘 | 会话 | Gemini-3-Flash 在 LOCA-Bench 上提升 28 个百分点 |
| TokenPilot | 压缩不能破坏缓存 | 全局层稳定前缀保证缓存连续性，局部层按残差效用惰性驱逐 | 会话 | 成本降低 61% 至 87% |
| LRE | 极简识别关键信息 | 几 KB 的 CPU 评分器，annotation-free 训练，学习识别承重历史 | 会话 | 峰值上下文减少 52% |
| AgentOCR | 文本历史转视觉 token | 渲染历史为图像 + 段光学缓存 + 自压缩率 RL 训练 | 步+会话 | token -50% 保持 95% 性能；渲染加速 20× |
| Shared Selective | 只记四种可复用的 | 保留任务规格、数据 schema、工具配置、输出约束，丢弃推理轨迹 | 会话+跨会话 | token 节省 97 倍，完成率 96% |

#### 5.1.1 早期基础：通用提示压缩

在 agent 专项工作之前，提示压缩的研究已在通用 LLM 场景下展开。三个工作都**操作在调用级**——压缩器只看当前 prompt 内容，不需要步级或会话级的语义信号。

**LLMLingua** [2310.05736]（EMNLP 2023）提出粗到细的 token 级压缩：先用一个小语言模型评估每个 token 的困惑度，再依据预算控制器删除低困惑度 token，配合指令调优对齐压缩前后的分布。在 GSM8K、BBH 等上最高实现 **20× 压缩，性能基本保持**。

**LongLLMLingua** [2310.06839]（ACL 2024）将这一思路推广到长上下文场景，关键洞察是 LLM 性能取决于关键信息的密度与位置而非上下文总长。NaturalQuestions 上以约 4× 的 token 减少取得了 21.4% 的性能提升，LooGLE 上成本降低 94.0%，10k token 量级的 prompt 在 2–6× 压缩下端到端延迟加速 1.4–2.6×。

**TCRA-LLM**（EMNLP Findings 2023）针对 RAG 场景，先用 T5-based 模型做摘要压缩、再做语义级词项压缩，输入 token 减少 65% 时准确率反而提升 0.3%。

这些通用压缩方法奠定了"低困惑度 token 是冗余的"这一基本假设，被后续 SimpleMem、CWL、LRE 等 agent 专项工作继承并细化。

#### 5.1.2 语义压缩：SimpleMem 与 RecMem

**SimpleMem** [2601.02553] 的核心设计是一个三阶段流水线：

- **第一阶段（语义结构化压缩）**：将非结构化的交互拆解为多字段记忆单元 `{时间, 地点, 意图, 原始文本}`，支持按时间、地点、意图等维度独立索引。
- **第二阶段（在线语义合成）**：在同一会话内合并相关信息——如果下一条消息是"帮我订周二北京到上海的机票"，它会与第一条合并为"北京出差之旅，去程周二北京→上海"。
- **第三阶段（意图感知检索）**：查询到达时先推断用户的搜索意图，再动态确定检索范围。

在 LoCoMo 基准上：**推理 token 减少 30 倍，F1 提升 26.4%**。不是"用质量换成本"，而是"同时改善了质量和成本"。

**RecMem** [2605.16045] 解决的则是记忆系统自身消耗 token 的问题。传统记忆系统（eager consolidation）每来一条新消息就调用一次 LLM 去提取和存储记忆。在长期运行的系统中，用户可能在 50 条消息中零散地提到 7 次"性能问题"，每一条都调 LLM 处理意味着大量 LLM 调用被浪费。

RecMem 改为基于语义簇的延迟整合（recurrence-based consolidation）：

- **"潜意识记忆层"**：所有交互被轻量 embedding 模型编码后存储（不需要 LLM），并在向量空间中持续追踪语义相似度。
- **"整合触发"**：当语义簇的形成被检测到（如"性能"相关交互在向量空间中形成了密集簇），才触发一次 LLM 调用做深度提取和总结。

关键设计是 **LLM 只在语义簇稳定后才介入**——确保了每次 LLM 调用处理的是一组语义相关的、信息密集的交互。实验结果是**记忆构建 token 减少 87%，准确率不降**。

SimpleMem 和 RecMem 分别解决了"记忆使用"和"记忆构建"两个阶段的问题。两者共同使用了新的记录维度——**记忆单元的 token 密度**（每个记忆 token 承载多少有效信息）——这在传统调用级记录中不存在。

#### 5.1.3 上下文驱逐：CWL 与 LRE

上下文超出 token 预算时需要驱逐部分内容。"驱逐哪些内容"的决策是会话级记录最直接的应用场景。

**CWL** [2606.11213] 维护一个 episode dependency graph——Agent 在每一步执行时标注这一步的类型和它与之前步骤的依赖关系（这一步的效果是否已持久化到环境/文件系统中？）。驱逐策略按依赖图工作：**优先删除已完成且效果已持久化的步骤**（副作用已在环境中）；**保留用户的原始消息和当前正在推理的探索上下文**（Agent 下一步决策所必需）。

关键验证是在 **80M token 的单 Agent 连续会话中（连续执行 89 个任务），任务精度与逐任务隔离会话相比无退化**。CWL 作者对比了 summarization-based compaction（将旧上下文用 LLM 压缩为摘要），指出四个已知缺陷：

| 缺陷 | 描述 |
|------|------|
| 信息丢失不可预测 | 不知道 LLM 在摘要中丢掉了什么细节 |
| 因果结构破坏 | 摘要打乱了原始上下文的时序和逻辑结构 |
| 阻塞性 LLM 调用 | 做摘要本身就在消耗 token 和时间 |
| 压缩引致幻觉 | 摘要中可能包含 LLM 编造的内容 |

**LRE** [2606.20954] 将驱逐决策做到最节俭的形式：一个只有几 KB 大小、跑在纯 CPU 上、完全不需要 LLM 调用的评分器。通过训练学习识别哪些历史信息是"承重"的（load-bearing）——一旦删除会导致后续步骤失败的内容。训练过程可以用系统自身的行为轨迹作为监督信号（annotation-free）。结果是匹配保留全量历史的准确率，同时**峰值上下文减少 52%**。

#### 5.1.4 方向缓存：PEEK

PEEK [2605.19932] 的思路与压缩或驱逐都不相同——它缓存的是"方向感"。对于需要重复访问同一外部上下文（如一个代码仓库）的场景，PEEK 维护一个固定大小的上下文地图（context map）。地图里存储的不是"上一次执行了什么操作"（那是轨迹），而是"这个上下文空间有什么、怎么组织的、哪些导出的实体/常量历史上频繁被用到"。

三个模块维护这个地图：**Distiller** 从 Agent 推理时的内部信号中提取可迁移的结构知识；**Cartographer** 将提取的知识转为结构化编辑操作；**Evictor** 在 token 预算超限时按优先级（条目对任务成功率的历史贡献）淘汰最不重要的条目。在长上下文推理和信息聚合任务上，相比 SOTA 方法 ACE，准确率提升 6.3–34.0%，迭代次数少 93–145 轮，**成本低 1.7–5.8 倍**。

#### 5.1.5 有状态设计：Stateful ReAct

Stateful ReAct [2606.14945] 识别了一个算法层面的结构性 token 浪费——通过改变系统的状态管理方式从根源上消减了 O(n²) 的复杂度。

无状态（stateless）Agent 的设计下，每次迭代都需要将当前 task、observation、所有 prior history 完整组装为 prompt 输入。第 i 轮操作重新传输完整的历史上下文，该轮的成本随 i 线性增长 O(i)，执行 n 轮后总 token 成本为 **O(n²)**。

Stateful ReAct 改用 LangGraph——一个支持类型化持久状态的编排框架——在轮次之间传递关键信息。每个轮次将实验进展、关键观察和状态写入 typed state 对象而非全部追加到对话记录。在第 i 轮，LLM 只看到"当前环境状态 + 传入的上一轮 typed state 摘要"，上下文缓冲大小退化为 O(1)，总 token 成本降至 **O(n)**。

| 任务 | 无状态 | 有状态 | 减少 |
|------|-------|-------|------|
| 超参数调优（15 轮） | 24,465 token | 2,492 token | **-90%** |
| 代码性能优化（40 轮） | 1,275K token | 627K token | **-52%** |

优化质量持平。关键点：这个优化是**结构性的**——不是"压缩掉了一些 token"，而是"改变了 token 消耗的数学复杂度"。

#### 5.1.6 可逆与可视化：ACE、VISTA、TokenPilot

**ACE** [2606.31564] 为每个历史步骤同时保存原始完整消息和压缩后的摘要。在每一步根据当前任务状态动态决定每个历史步骤以哪种形式进入 LLM 上下文——原始、摘要或丢弃。决定是每步重新评估的——上一步被标记为"摘要"的步骤，如果后来变得相关，可以在下一步切换回"原始"——这正是"可逆弹性"的含义。配合了 4 个主流 Agent 框架，无需训练和架构修改。

**VISTA** [2606.30005] 的出发点是一个基础但被普遍忽视的问题：LLM 对自己的上下文窗口内的实际状态缺乏感知——模型不知道 context window 里有哪些内容、各自占据了多少 token、哪些是最新的。VISTA 给 Agent 暴露了一个运行时仪表盘，以 typed, addressable blocks 的形式展示元数据——每个块的大小、年龄、访问次数。Agent 在 prompt 中看到这些信息后，自己决定把哪些块保留在上下文中。关键实验结果是 LOCA-Bench 上 Gemini-3-Flash 从 22.7% 提升至 **50.7%（+28pp）**，提升幅度随上下文压力增大而增大。

**TokenPilot** [2606.17016] 发现了一个此前被忽略的隐藏代价：token 压缩/驱逐操作会改变上下文的序列布局，导致 LLM 推理引擎中 KV cache 的前缀不匹配，使本来能命中的 prompt cache 失效。双粒度方案——**全局**在输入入口处稳定前缀（确保 cache 连续性）、**局部**按每个上下文段的"剩余价值"做惰性驱逐——在 PinchBench 和 Claw-Eval 上将**成本降低了 61% 到 87%**。

#### 5.1.7 视觉化历史：AgentOCR

AgentOCR [2606.40082] 提出一个跨模态的结构性压缩方案：将 Agent 的文本历史渲染为图像，用视觉 token 替代文本 token 表示累积的 observation-action 序列。其核心观察是视觉 token 的信息密度显著高于文本 token。

AgentOCR 包含三个核心机制：(1) **段光学缓存**——将历史分解为可哈希的段并维护视觉缓存，避免冗余重渲染，带来 20× 渲染加速；(2) **Agent 自压缩**——Agent 主动发出压缩率，训练时使用 compression-aware reward 平衡任务成功与 token 效率；(3) **多轮 rollout 可扩展性**——通过缓存复用让 RL 训练可行。

在 ALFWorld 和 search-based QA 上，AgentOCR **保持文本 Agent 95% 以上的性能，同时减少 50% 以上的 token 消耗**。这项工作在记录体系上的贡献是引入了**模态密度比**（同一信息在文本 token 与视觉 token 下的字节比），并将压缩决策从启发式规则推进到 Agent 自主发出的连续控制量。

#### 5.1.8 跨会话共享：Shared Selective Memory

Shared Selective Persistent Memory [2607.09493] 聚焦于 coding agent 的一个具体痛点：每次新会话开始时，Agent 从零开始，上一轮会话中摸索出来的配置选择、数据 schema 理解、工具使用模式全部丢失。解决方案是将会话内容分类为"应该被保留的东西"——四类可复用上下文（任务规格、数据 schema、工具配置、输出约束）——和"应该被丢弃的东西"（会话特定的推理轨迹），通过角色访问控制在用户间共享复用部分。

> **96% 任务完成率**（对比无记忆 79%，而**全量历史反退化为 71%**——全量历史不如选择性记忆的原因在于旧的推理轨迹会 bias Agent 的当前行为）。
>
> 零 token 数据刷新使常规更新无需重复调用 LLM（14x 任务时间减少、**97x per-invocation token 节省**）。

### 5.2 路由策略

路由策略解决的是"是否需要为大模型付费"的问题。Routing Survey [2502.00409] 将问题形式化为：在接收到查询后，系统决定由哪个模型或组件来处理——目标是在给定的成本约束下最大化任务质量。

这一思路的奠基工作可以追溯到 FrugalGPT [2305.05176]（2023）。该工作首先观察到主流 LLM API 的定价相差可达两个数量级，并系统提出三类成本降低策略：**prompt adaptation**、**LLM approximation**、**LLM cascade**。FrugalGPT 实例化的 LLM cascade 学习将不同查询分配给不同 LLM 组合，**以 98% 的成本减少匹配 GPT-4 性能，或同等成本下将准确率提升 4%**。

四种实现方式：

| 路由类型 | 决策方式 | 数据需求 | 代表工作 | 特点 |
|---------|---------|---------|---------|------|
| 相似度路由 | 查询 embedding 匹配历史路由记录 | 历史调用级数据 | 早期方案 | 简单但依赖充足历史 |
| 监督路由 | 训练分类器预测最优模型 | 标注数据 | Fast or Better [2502.12145] | 用户可控 α 参数平衡成本和质量 |
| 强化学习路由 | bandit/RL 边执行边学习 | 在线反馈 | ARC [2602.11574] | 适应性强但需要持续反馈 |
| 生成式路由 | LLM 自己判断是否需要更强模型 | 查询文本 + LLM 自评估 | MemFlow [2605.03312] | 零额外训练，但依赖 LLM 判断力 |

**Fast or Better** 提供了用户可控的方案：两个分类器分别针对准确率和效率目标训练，用户通过参数 α 在两者之间灵活调整。**Local-Splitter** [2604.12301] 做了最系统的测量工作——7 种策略在 4 种工作负载上的效果对比：

| 工作负载类型 | 最优策略组合 | 云端 token 节省 | 特点 |
|-------------|------------|---------------|------|
| edit-heavy（频繁修改代码） | 本地路由 + prompt 压缩 | 45–79% | 简单策略即高效 |
| explanation-heavy（频繁解释代码） | 本地路由 + prompt 压缩 | 45–79% | 同上 |
| general chat（通用对话） | 语义缓存 + prompt 压缩 | 中等 | 缓存命中率高 |
| RAG-heavy（大量检索增强） | 全策略组合（含草稿-审查） | 51% | 需要复杂策略 |

核心结论是**策略效果高度取决于负载类型**。

路由策略的可靠性依赖两个经常被忽视的基本条件——**Routing Diversity** [2607.09197] 从不同角度回应了这两个条件：(1) 候选者间必须有足够的行为差异——如果候选模型中存在高度相似的模型，路由就没有意义；(2) 路由策略必须对输入扰动稳定——同语义的查询变体应当被路由到相同模型。实验发现 KNN 路由虽然准确率最高但扰动稳定性最差，而 prompt-based 路由在所有扰动类型下均保持稳定——**准确率和有意义的可靠性是两个可以相互独立的维度**。

### 5.3 计算量节省的隐藏代价

两种"节省"策略在进一步分析中暴露了评估陷阱：

**量化模型未必更快。** Quantization Inflates Reasoning [2606.25519] 发现 INT4/INT3 量化虽然降低了 per-token 的生成延迟，但量化使得模型生成了更多的 token——更长的思维链。如果 token 数量增加的比例超过了 per-token 加速的比例，端到端延迟可能不变甚至变差。论文提出了"CoT Token Inflation Ratio"作为补充评估指标。记录层面的启示是：评估任何降低 per-token 成本的技术时，必须同时记录总推理 token 数的变化。

**Skill 和 Memory 模块不一定值得它们的 token 成本。** Budget-Constrained Web Agent [2606.15017] 做了一个关键实验：将 vanilla agent（无任何 skill/memory 增强）和带有 skill/memory 模块的 agent 分配到相同的 token 预算下，vanilla 多出来的 token 就用来多做几轮推理。在 3 个 WebArena 域和 3 个模型上的结果是 **vanilla agent 匹配甚至超越了三个最先进的增强方案**。

> **方法论贡献**：任何声称能节省 token 的增强方案，都必须与获得相同 token 预算的未增强 baseline 做公平对比。如果 baseline 获得了更少的 token 预算，那么"节省"可能就是伪的。

### 5.4 系统级记录驱动的多 Agent 优化

多 Agent 系统中，Agent 之间的通信本身不直接产生任务价值——它是一种必要代价，优化目标是用尽可能少的通信 token 完成有效协作。这些优化首次要求将"每 Agent 调用计数"按系统全局视角重新聚合。

| 工作 | 解决的问题 | 解决方案 | 系统级记录需求 | 关键效果 |
|------|----------|---------|-------------|---------|
| SVR-MAD | 辩论式 MAS 通信膨胀 | 利用贝叶斯后验逐轮剪枝通信图，仅保留答案可靠的 Agent 继续参与 | 通信 token 总量、各 Agent 在辩论中的存活状态 | 通信 token 减少 61% |
| SupervisorAgent | 运行时浪费检测需要额外 LLM 调用 | LLM-free 过滤器检测步级异常，仅在触发时启动 Supervisor 介入 | 步级信号：输出变化量、上下文占用比例 | token 消耗减少 29.68% |
| HiveMind | 并发 Agent 因缺乏调度而无效死亡 | HTTP 代理层部署准入控制、速率追踪、退避、预算管理和优先级队列 | 并发 Agent 数量、失败率、排队延迟 | 失败率从 72–100% 降至 0–18% |
| Agora | 任务分配依赖模型自报能力，能力评估不可靠 | 拍卖机制让 Agent 出价而非自报能力，单参数控制成本-质量权衡 | 系统级通信 token、任务分配准确率 | 通过真实能力识别减少无效分配 |
| RCR-Router | 静态/全量路由导致 MAS token 冗余与记忆暴露 | 角色感知的动态记忆子集路由 + 严格 token 预算 + 共享记忆存储 | 角色-阶段-记忆三维映射、各 Agent token 份额 | token 减少 30%，质量持平或提升 |

#### 5.4.1 通信剪枝：SVR-MAD

SVR-MAD [2605.23099] 解决辩论式 MAS 中的通信膨胀问题。传统 MAD 设计让多轮辩论中"所有 Agent 之间互相通信"，每轮结束后进入下一轮前将上一轮的所有回复注入所有 Agent 的上下文（总 token 量接近 O(rounds × n²)）。SVR-MAD 将辩论建模为贝叶斯过程：

- **首轮**：所有 Agent 独立回答同一问题。
- **次轮**：每对 Agent 交换答案并互相生成挑战（critique）。
- **第三轮及以后**：仅保留那些"答案经受住了同行挑战"的 Agent，逐步将通信图缩减为稀疏子图。

关键是筛选依据——代理 A 的答案在代理 B 的 challenging 面前存活——是"**后验证据**"（基于事实），而非不可靠的"先验信号"（Log-probability、自报置信度等——在幻觉场景下可能给出高置信度但内容错误）。**通信 token 减少最高 61%，准确率不变或提高**。

#### 5.4.2 零开销运行时监督：SupervisorAgent

SupervisorAgent [2510.26585]（ICLR 2026）提出了一个运行时降低 token 消耗的框架：一个 **LLM-free 的自适应过滤器**在每个 Agent 步骤检查多个信号——输出是否有实质性变化、上下文窗口是否接近上限——只有当过滤器检测到异常时才触发 Supervisor 模块干预。由于过滤器本身是 LLM-free 的，**监控不额外消耗 LLM 调用的 token**。在 GAIA 基准上，**token 消耗减少 29.68%**（成功率不降），且跨 5 个不同类型的基准测试和多模型验证了泛化性。

#### 5.4.3 并发调度：HiveMind

HiveMind [2604.17111] 的动机来自一个发现：**11 个 coding agent 并发运行并向同一 API endpoint 发送请求时，27% 的 agent 因连接重置和 HTTP 502 错误而失败——但该 API 的总计容量完全足以串行处理所有 11 个请求**。问题不在 API 容量，而在并发请求之间的协调缺失。HiveMind 在 HTTP 代理层部署了五项调度机制：

| 机制 | 作用 |
|------|------|
| 准入控制 | 限制同时活跃的 agent 数 |
| rate-limit 追踪 | 根据 API provider 的限额动态调整请求速率 |
| AIMD 退避及 circuit breaker | 逐级降低请求频率直到服务恢复 |
| token 预算管理 | 每个 agent 独立的硬上限 |
| 优先级队列 | 确保关键任务不被边缘化 |

5–50 个并发 Agent 下的失败率**从 72–100% 降至 0–18%**，消除了 48–100% 的浪费计算，代理层开销低于 3ms 每请求。

#### 5.4.4 真实能力识别与角色感知路由

**Agora** [2607.09600] 用拍卖机制替代自报能力评估：让 Agent 对任务出价而非自报能力，通过竞价过程更可靠地揭示真实能力。

**RCR-Router** 在系统级维护一个**结构化共享记忆层**，基于轻量级评分策略为每个 Agent 动态选择与其**角色**和**任务阶段**相关的记忆子集。在 HotPotQA、MuSiQue、2WikiMultihop 三个多跳 QA 基准上，**减少 30% token 同时维持或提升答案质量**。该工作引入了"角色 × 阶段 × 记忆子集"的三维映射，把系统级记录从单纯的"按 Agent 聚合"推进到"按 Agent 角色与任务阶段细分"。

### 5.5 等计算量对比方法的确立：SAS vs MAS

SAS vs MAS [2604.02460] 对 MAS 与 SAS 的评估建立了一条核心实验原则：

> **任何 SAS vs MAS 的性能对比必须控制计算量（Token 预算）。**

论文将"thinking token"独立计量——仅包含中间推理、不含 prompt 和最终答案的那部分 token——并将此作为计算量归一化的核心度量。在 3 个模型家族、5 种 MAS 架构、多跳推理任务上，一旦 thinking token 预算被对齐且精确化，**SAS 一致性地达到或超越了 MAS 的性能**。

这条结论说明，已知的文献中 MAS 展现的优越性能很可能被**未控制的计算量差异**所过度解释——MAS 用更多的 token 换来了更好的"报告分数"，而架构本身并没有提供内在优势。论文进一步指出当单 Agent 的上下文利用率因会话过长而退化时 MAS 更具竞争力。这里的"上下文利用率退化"指：随着单 Agent 会话长度增加，其上下文窗口被历史信息填满，留给当前推理的有效空间减少，导致信息检索效率下降——此时将任务拆分为多个 Agent 分别维护较小的上下文窗口反而更有效。因此架构选择（SAS vs MAS）应基于具体的任务长度和上下文压力，而非一概而论地认为某一种架构更优。

### 5.6 阶段小结

阶段三最重要的特征是记录粒度的向上扩展。所有优化策略按目标分为四类：

| 策略类别 | 包含的工作 | 优化目标 | 涉及的记录粒度 |
|---------|----------|---------|-------------|
| 上下文压缩与驱逐 | LLMLingua, LongLLMLingua, TCRA-LLM, SimpleMem, RecMem, CWL, ACE, TokenPilot, LRE, AgentOCR | 减少每次 LLM 调用中的冗余输入 token | 调用级、步级、会话级 |
| 记忆与缓存 | PEEK, Stateful ReAct, VISTA, Shared Selective | 避免重复加载相同上下文、跨任务积累有用知识 | 会话级、跨会话级 |
| 路由选择 | FrugalGPT, Routing Survey, Fast or Better, Local-Splitter, Routing Diversity | 用合适的模型处理合适的查询 | 调用级到系统级 |
| 多 Agent 协调 | SVR-MAD, SupervisorAgent, HiveMind, Agora, RCR-Router, SAS vs MAS | 最小化通信开销和并发失效 | 步级到系统级 |

三类隐藏代价（量化膨胀、Skill 模块的 token 成本、等预算对比的必要性）为这些优化策略建立了公平评估的基准——任何声称的"节省"都必须在 token-matched baseline 下验证。

---

## 六、第四阶段：自治闭环

> 核心问题：**能否让系统自己完成"观测→诊断→改进→验证"的循环？** 当所有粒度的记录体系完整建立之后，第四阶段的自然演进方向是让系统自主完成优化循环，而不依赖人类工程师手动分析和制定改进方案。

### 6.1 编排层是决定性杠杆：Harness Effect

Harness Effect [2607.06906] 是这一阶段最具标志性的工作。其实验设计采用严格控制的变量法：

| 维度 | 设定 |
|------|------|
| 任务 | 22 个锁定评估任务 |
| 模型 | 6 个：Claude Sonnet 4.6、Gemini 3.1、Gemini Flash 3.5、Qwen 3.6、GLM 5.1、Palmyra X6 |
| 编排层 | A 组用传统生产循环（每轮重新提交完整对话历史）；B 组用 Writer Agent Harness |
| 锁定变量 | 任务、模型、评估者、价格表完全相同，仅编排代码不同 |

实验结果：

| 指标 | A 组（传统） | B 组（Harness） | 改善 |
|------|-----------|---------------|------|
| 每任务成本 | $0.21 | $0.12 | **-41%** |
| 每任务 token | 14.2K | 8.8K | **-38%** |
| wall-clock | 48s | 27s | **-44%** |
| 任务质量 | 0.78 | 0.81 | 持平（方向性提升） |

两组对比最能说明问题：(1) 从最便宜模型切换到最贵模型，成本降低最多 36%；(2) 保持任意模型不变，仅切换编排层，成本降低 33–61%。**编排层的优化效果在绝对值上全面超越"换一个更强的模型"**。更强模型从编排中获益更多（harness leverage r=0.99）。

**六个效率机制：**

| # | 机制 | 描述 |
|---|------|------|
| ① | Cache-shape discipline | 将 prompt 划分为稳定前缀区（命中率 99.9%）和可变尾部区 |
| ② | 结构化增量压缩 | 上下文占用率达到 80% 时触发，将历史折叠为有类型检查点 |
| ③ | 上下文卸载 | 子 Agent 摘要上限 8KB，工具输出超 20K 字符写入文件系统 |
| ④ | 零 token 等待 | Agent 等待外部事件时序列化状态并释放资源，等待期间 token 为 0 |
| ⑤ | 失败-支出治理 | 分类处理错误、重试硬上限 50 次、工具并行度上限 4 |
| ⑥ | 模型无关执行基底 | 编排层代码与具体模型解耦 |

其中机制①（Cache-shape discipline）和机制②（结构化增量压缩）贡献了最大比例的节省。机制①的原理是：大多数 LLM API 提供商对 prompt 中与之前请求完全相同的 token 前缀免收输入费用（prompt caching），但缓存仅在 byte-identical 的前缀上命中。因此将 prompt 设计为"不变的 system prompt + 用户需求"在前、每轮变化的新对话在后的两段式结构，可以最大化缓存命中——实验中达到 99.9%，使有效输入 token 价格降至目录价的约 10%。机制②解决的是当长对话超出上下文预算时的压缩策略：传统方案调用 LLM 做摘要（本身即消耗 token 且存在 5.1.3 中 CWL 指出的四个缺陷），而结构化增量压缩将历史折叠为固定格式的检查点——持久记忆 + 最近 N 段执行摘要 + 用户原始需求——不调用 LLM，无信息丢失风险。

**编排层适配小模型。** Better Harnesses, Smaller Models [2607.08938] 用一个 meta agent 自动分析 SLM 的失败轨迹，自动生成针对性的 harness 调整。在 7 个业务型 Agent 任务和 3 个 SLM 家族上，21 个 task-model 组合中有 16 个获得提升，7 个完全弥合 SLM 和 LLM 的差距。最好的 SLM 在**仅 4% 的成本下恢复了 LLM 的 89.7% 性能**。

**编排层不只影响结果，还影响决策。** Harness-Induced Belief Divergence [2607.04528] 进一步证明：即使 final answer 相同，编排层的设计本身（哪些 action 被允许/禁止、错误被如何修复）会改变 Agent 在多步决策中的"信念轨迹"。Harness 不是一个 implementation detail，而是一个决定实验可复现性和 Agent 行为的重要变量。

### 6.2 可观测性驱动的自动演化：AHE

AHE [2604.25850] 将编排层的改进从一个手动 process 变成自动化闭环。其核心创新是"**三柱可观测性**"设计：

| 柱 | 内容 | 作用 |
|----|------|------|
| **组件可观测性** | Harness 的每个可编辑组件（工具、中间件、记忆、系统提示）都有独立的文件级表示和版本控制 | 编辑是可回滚的 |
| **经验可观测性** | 从百万 trajectory token 中蒸馏出分层 drill-down 证据语料 | 最顶层显示全局工具使用模式，下钻层显示具体失败轨迹 |
| **决策可观测性** | 每次编辑带有一条自声明预测——"修改 middleware X 的参数应让 tool Y 成功率提升 5%" | 下一轮评估中验证预测，构成可证伪的改进契约 |

> 10 轮自动化演化后，Terminal-Bench 2 的 pass@1 从 **69.7% 提升至 77.0%**——超越了人类手写的最优配置 Codex-CLI（71.9%），也超越了两个 baseline 自演化方法。SWE-bench-verified 上以 12% 更少的 token 达到了最高综合成功率。

消融实验将增益定位到**工具、中间件和长期记忆的改进**，而非系统提示的措辞调整——harness 的结构性改进比 prompt 层面的文字优化更有效。

### 6.3 浪费检测与实时干预：Early Diagnosis

Early Diagnosis [2606.01365] 回答了一个操作性问题：**实时检测到浪费 token 的瞬间并及时干预，能挽回多少浪费**。

其三层检测架构对应着递增的计算成本和递增的评估可靠性：

| 层 | 触发时机 | 算力 | 检测内容 |
|----|---------|------|---------|
| (1) 在线信号层 | 运行时实时 | 完全不调 LLM | 循环检测、预算压力、低信息增益、工具不稳定 |
| (2) 离线语义层 | 任务结束后 | 较多计算资源 | 语义接地性、工具输出一致性 |
| (3) LLM-as-judge 层 | 最终手段 | LLM 评估 | 最终质量评估 |

这些 signal 都需要步级的记录来产生，并且必须是 LLM-free 的——**如果用 LLM 来检测 LLM 的浪费，那么监控本身的 token 开销就会抵消监控带来的节省**。

> 在 165 个 GAIA 验证 trace 上：**在失败的 run 中，平均 58.1% 的 token 是在第一个警告被触发后继续消耗的**。
>
> 这意味超过一半的浪费 token 在已检测到异常后仍在继续累积——系统虽然已经发出警告信号，但 Agent 的默认行为仍然是在当前策略下继续执行，直到预算耗尽或达到最大步数上限。这说明仅靠"检测"不够，必须在检测到异常后进行"主动干预"——例如强制切换搜索策略、要求 Agent 提供额外证据、或增加行为多样性。在 10 个任务的 Level-2 试点中验证了这一点：**主动干预将 post-warning token 占比从基线 63.8% 降至 30.4%**。干预的具体形式包括：当检测到连续三步输出无实质性变化时，强制 Agent 切换工具或改变检索关键词；当检测到预算消耗速率超过阈值时，截断返回长度并要求 Agent 用更简洁的方式给出中间结论。

### 6.4 步级训练信号区分：ActFocus 与 PABU

**ActFocus [2605.14558]** 在 Agent RL 训练场景中发现了"**Action Bottleneck**"现象。在 agentic RL 中，一个 trajectory 由交替出现的"长推理 chain"和"短 tool-call action token"构成。PPO 和 GRPO 对所有 token 同等对待，但训练信号**高度集中在占比仅一小部分的 action token**（如 `func=read_file, args={...}`），而非占比更大的 reasoning token。ActFocus 仅调整 loss 函数中两类 token 的权重：对 reasoning token 降权、对 action token 加权。

> 四组环境和不同模型大小下：**最终步性能比 PPO 提升最高 65.2pp，比 GRPO 提升最高 63.7pp**，零额外 runtime 或 memory 开销。

这项工作对记录体系的要求是：**步级记录必须能区分动作 token 和推理 token**——如果模型在 tokenization 层面将二者混在一起，梯度重加权就无法实现。

**PABU [2602.09138]** 从另一个角度解决了步级效率问题：不让 Agent 看全部历史，而是让它在每一步预测两个值——自上一轮以来的相对进度和是否值得记住当前交互。仅被判定为"值得记住"的交互输入到信念状态中。AgentGym 8 个环境中 **task completion rate 达到 81.0%，超过 full-history SOTA 达 23.9%，平均步骤数降至 9.5（-26.9%）**。

### 6.5 从减少到消除 Token：GATS、CoACT、CompactionRL

阶段四最前沿的工作探索了一个新边界——当执行日志和训练数据积累到足够量级时，**能否完全取代 LLM，或将 token 压缩能力内化为模型自身的 capability**？

| 工作 | 技术路线 | 记录基础 | 关键结果 |
|------|---------|---------|---------|
| GATS | 三层世界模型逐级替代 LLM：符号匹配（L1）→ 统计推断（L2）→ LLM 回退（L3）+ UCB1 树搜索 | 历史动作-结果对，来自步级执行日志 | 规划阶段零次 LLM 调用，100% 成功率 |
| CoACT | 以"下一步动作是否一致"为压缩保真判据，teacher-student 方式训练轻量压缩器 | 原始观测 + 对应下一步动作，步级记录 | 平均 token 降低 33%，任务能力持平 |
| CompactionRL | RL 训练中联合优化任务执行和摘要生成，使用 token 级损失归一化和跨 trajectory GAE | RL 轨迹数据，覆盖步级和会话级 | SWE-bench +7.0pp，已部署至 GLM-5.2 训练流水线 |

**GATS [2607.08894]——零 LLM 调用的规划。** 三层世界模型逐步替代 LLM：

| 层 | 触发条件 | 实现 |
|----|---------|------|
| L1（精确符号匹配） | 当前动作在历史中精确出现过 | 直接返回缓存结果 |
| L2（统计推断） | 遇到类似但非精确匹配的动作 | 利用执行日志中的统计模型预测最可能结果 |
| L3（LLM 回退） | 完全未见过的未知动作 | 才调用 LLM |

合成规划任务 **100% 成功率**（vs LATS 92%、ReAct 64%）；12 个跨领域压力测试全 100% 成功。**每个任务的规划阶段零次 LLM 调用**（LATS 平均 37 次）。产出的计划是确定性的——不同运行之间零方差。

**CoACT [2607.02911]——动作保持的观测压缩。** 用 NAP（Next-Action Preservation）准则：**压缩后的观测送入 Agent 后，其下一个动作是否与不压缩时完全相同？如果是，则压缩是"信息无损"的**。在 SWE-bench Verified 上，三个 Agent 模型平均降低**总 token 消耗 33.0%**，同时任务解决能力接近不压缩 baseline。

**CompactionRL [2607.05378]——训练中内化压缩。** 与 CoACT 的"外挂压缩器"不同，CompactionRL 在 RL 训练阶段就把"任务执行"和"压缩摘要"两个目标联合优化——使模型在强化学习中就学会：当上下文紧张时，如何有效给自己做摘要。

> GLM-4.5-Air 在 SWE-bench Verified 提升到 **66.8% (+7.0pp)**，Terminal-Bench 2.0 上达到 **24.5% (+3.1pp)**。这是**首个在实际大模型训练流水线（GLM-5.2, 750B-A40B）中集成此技术的案例**。

### 6.6 阶段小结

阶段四的演进揭示了三条独立的路径，都指向同一个方向——**将 token 优化从"外部专家手动调试"变为"系统内在能力"**：

| 路径 | 代表工作 | 核心思想 |
|------|---------|---------|
| 编排层优化与自动化 | Harness Effect, AHE, Better Harnesses | 编排层而不是模型本身是最大的效率杠杆 |
| 实时检测与干预 | Early Diagnosis, PABU, ActFocus | 在步级粒度上实现从"事后归因"到"事中控制"的跃迁 |
| Token 消除 | GATS, CoACT, CompactionRL | 从"如何更省地使用 LLM token"转向"哪些步骤根本不需要 LLM token" |

---

## 七、交叉视图：粒度 × 阶段矩阵

### 7.1 粒度-阶段矩阵

将记录粒度的扩展（纵轴）与研究阶段的推进（横轴）放在同一个矩阵中，可以看到完整的演进图景：

|  | 阶段一：度量 | 阶段二：控制 | 阶段三：结构性优化 | 阶段四：自治闭环 |
|------|-----------|-----------|----------------|--------------|
| **调用级** | Tokenomics 建立两级记录方案 | Token Budgets 编译时保证；Stackelberg 成本约束 | Local-Splitter 七策略测量；Routing Diversity 可靠性条件；Quantization Inflates 代价检测 | Harness Effect 缓存评估；Early-Token Confidence 早期质量预测 |
| **步级** | — | BAVT 价值评分驱动搜索 | SupervisorAgent 空转检测；ActFocus 训练信号区分 | Early Diagnosis 在线信号；PABU 进度预测；GATS 零 LLM 规划；CoACT 动作保持压缩 |
| **任务级** | Tokenomics 阶段聚合 | ARC/Stackelberg 任务级反馈 | SAS vs MAS 计算量对齐；Budget-Constrained Web 公平评估 | Harness Effect 因果实验；AHE 自动演化 |
| **会话级** | — | — | CWL 依赖图驱逐；PEEK 方向缓存；RecMem 时机优化；Stateful ReAct O(n) 设计 | Shared Selective Memory 跨会话共享；CompactionRL 训练内化 |
| **系统级** | — | — | SVR-MAD 通信剪枝；HiveMind 并发调度；RCR-Router 角色感知路由 | Agora 拍卖分配；Early Diagnosis 跨 Agent 检测 |

> **阅读方法**：每一行对应一个记录粒度，每一列对应一个研究阶段。单元格中的工作列表表示在该阶段、该粒度上有活跃研究；"—"表示该粒度在该阶段尚未被涉及。纵向看一列可以了解某个阶段覆盖了哪些粒度；横向看一行可以了解某个粒度在哪些阶段被激活和运用。

### 7.2 三条演进规律

从粒度的扩展模式中可以验证三条深层规律：

1. **粒度随时间向下和向上同时扩展**——阶段一仅覆盖调用级，但到阶段四时所有五个粒度上都活跃着研究工作。没有任何一个层级被"降级"或废弃。具体而言：阶段一的工作（Tokenomics）仅在调用级和任务级（通过聚合得到）有记录；阶段二激活了步级记录（BAVT）；阶段三激活了会话级和系统级记录（CWL、PEEK、SVR-MAD 等）；阶段四在所有五个粒度上同时有活跃工作，且各粒度之间的数据流形成了闭环——例如 AHE 同时使用任务级的成功率数据和文件级的组件版本数据进行自动演化。

2. **下层记录是所有上层优化的必要前提**——GATS 的零 LLM 规划依赖步级执行日志的积累；Harness Effect 的编排层因果实验依赖完整精确的调用级计量；SVR-MAD 的通信剪枝依赖系统级通信 token 的归因。没有任何一个优化策略能仅在自己所处的粒度上完成。这意味着记录体系的建设必须从最底层开始——如果调用级的 token 计数因并发竞态条件而不准确，所有上层分析（包括会话级的缓存效率评估、系统级的通信占比归因）都不可信。

3. **研究问题从"记录"演化到"决策"**——早期工作关注"怎么记录、记录什么"（Tokenomics 的调用级计量），后期工作关注"记录的数据如何驱动自动决策"（AHE 的自演化闭环、CompactionRL 的训练内化）。前三个阶段本质上是"为人类工程师提供更好的数据来手动优化"，阶段四则转向"让系统自己根据数据做出调整决策"——从观测工具变为自治系统。

---

## 八、跨阶段共识

前述四个阶段的分析中，部分洞察跨阶段反复出现、彼此呼应，构成了领域的共识。

### 8.1 编排层是最大效率杠杆

阶段四的 Harness Effect 用因果实验证明了"换编排"比"换模型"更有效（33–61% vs 36%），但这一洞察在阶段一已埋下伏笔：Tokenomics 发现 Code Review 占 59.4% token——这本身是 ChatDev 框架的编排选择（"审查→修改→再审查"循环）造成的，而不是模型本身的属性。

| 工作 | 角度 | 关键证据 |
|------|------|---------|
| Harness Effect [2607.06906] | 因果实验 | 同任务同模型下，仅切编排层，成本 -41% |
| Better Harnesses [2607.08938] | 编排适配小模型 | SLM 在 4% 成本下恢复 LLM 89.7% 性能 |
| AHE [2604.25850] | 自动化演化 | 演化的 harness 超越人类手写的最优 |
| Tokenomics [2601.14470] | 间接证据 | 59.4% 集中在 Code Review——框架选择的结果 |
| Harness Belief [2607.04528] | 决策轨迹 | Harness 设计改变 Agent 中间信念轨迹 |

**共性结论**：在评估"换模型 vs 换框架"时，编排层至少是和模型同等重要的变量；在工程实践中，编排层通常是更便宜的优化方向。

### 8.2 公平评估需要 token-matched baseline

随着优化技术的多样化，越来越多的工作发现：如果不严格控制计算量，"节省"和"提升"都可能是评估假象：

| 工作 | 揭示的评估陷阱 |
|------|-------------|
| ARC [2602.11574] | 不公平对比：基线获得了更少 token 预算。修正：所有对比都 budget-matched。 |
| SAS vs MAS [2604.02460] | 不公平对比：MAS 用更多 thinking token 换来了更高"报告分数"。修正：独立计量 thinking token 并对齐。 |
| Budget-Constrained Web [2606.15017] | 不公平对比：增强方案 vs vanilla，但 vanilla 没获得同等 token。修正：把省下的 token 给 vanilla 多做几轮推理。 |
| Quantization Inflates [2606.25519] | 不公平对比：只看 per-token 加速，忽略总 token 增加。修正：同时记录总推理 token。 |
| Local-Splitter [2604.12301] | 评估片面：单一工作负载上的结论不能推广。修正：在 4 种负载上系统测量。 |

**共性结论**：任何声称的"节省"或"提升"，必须在 (1) 等计算量、(2) 多负载、(3) 多模型的条件同时满足时才可信赖。

### 8.3 监控的零开销原则

如果监控浪费本身需要消耗 LLM 调用，那监控的开销可能抵消它带来的节省。多项工作自觉地遵守了"监控必须 LLM-free 或近零开销"的原则：

| 工作 | 监控的实现 | 开销 |
|------|-----------|------|
| Early Diagnosis [2606.01365] | 在线信号层完全不调 LLM | 实时，零 token |
| SupervisorAgent [2510.26585] | LLM-free 自适应过滤器 | 接近零 |
| LRE [2606.20954] | KB 级 CPU 评分器 | 几 KB 内存、CPU 推理 |
| HiveMind [2604.17111] | HTTP 代理层调度机制 | < 3ms / 请求 |
| CWL [2606.11213] | 依赖图驱逐 | 不调 LLM |

**共性结论**：监控/调度基础设施应作为系统的"零开销层"来设计——它产生信号，但不消耗被监控的资源（LLM token）。这是 wasted-computation 检测能正向 ROI 的必要条件。

### 8.4 从外部优化到模型内生

阶段四最前沿的方向展示了一个清晰的转向：token 优化正在从"外部专家设计压缩/驱逐策略"变为"模型自己在训练中学会压缩"：

| 阶段 | 路径 | 代表 |
|------|------|------|
| 早期 | 外部压缩器/驱逐规则 | LLMLingua, LongLLMLingua, SimpleMem, CWL, LRE, TokenPilot |
| 中期 | 外部 + 检测/调度基础设施 | Early Diagnosis, HiveMind, RCR-Router |
| 晚期 | 模型内化压缩能力 | CompactionRL, AgentOCR |
| 最前沿 | 完全替代 LLM | GATS（零 LLM 规划） |

**CompactionRL** 已在 GLM-5.2（750B-A40B）训练流水线中部署——这是首个工业级落地的案例。**AgentOCR** 通过 compression-aware reward 让 Agent 自主发出压缩率，把压缩从启发式规则升级为 Agent 的连续控制量。**GATS** 则展示了更激进的边界：当执行日志足够丰富时，某些步骤根本不需要 LLM token。

**共性结论**：长期看，"省 token"和"做训练"会逐渐统一——模型在训练时就把"什么时候不需要调自己"内化为能力。记录体系（特别是步级执行日志和 trajectory 数据）是这个转向的基础设施。

---

## 九、论文总览表

> 按四阶段排列，覆盖 44 篇论文。列含义：**记录的指标**=论文实际采集或计量的 token / 时间数据；**记录粒度**=数据在五级体系中的层级及采集方式；**优化策略**=论文采用的核心技术手段；**关键效果**=论文报告的主要量化结果。

### 阶段一：度量

| ID | 论文 | 记录的指标 | 记录粒度与方式 | 优化策略 | 关键效果 |
|----|------|----------|-------------|---------|---------|
| 2605.09104 | Token Economics | OpenRouter 平台周 token 量趋势（0.4T→27.0T）；token 作为成本/通信/效率度量的三重角色 | 框架级（综述，不产生原始数据） | 四层分析框架：微观（单 Agent 预算分配）、中观（MAS 通信成本）、宏观（生态定价）、安全（对抗防御） | 统一分类体系，提出"可微分 token 预算"方向 |
| 2601.14470 | Tokenomics | 每次 API 调用的 input / output / reasoning token；阶段级 token 总量及各类占比 | 调用级→阶段级：在 ChatDev 框架内嵌入计量代码，内部 phase 映射为 SDLC 六阶段（Design, Coding, Code Completion, Code Review, Testing, Documentation） | 两级记录方案（调用级计量 + 业务阶段归因）；不在模拟环境推算，用真实 API 返回数据 | Code Review 占 59.4% 总 token；全局输入 53.9% / 输出 24.4% / 推理 21.6%；Coding 阶段输出异常高（58.0%） |

### 阶段二：预算控制

| ID | 论文 | 记录的指标 | 记录粒度与方式 | 优化策略 | 关键效果 |
|----|------|----------|-------------|---------|---------|
| 2603.12634 | BAVT | 剩余预算比例（已消耗 token / 总预算）；残差价值评分（步骤间的相对进展而非绝对状态质量） | 步级：推理树每个节点计算价值评分和预算状态，节点选择概率 = value_score^(1/剩余预算比例) | 训练无关的推理时框架；残差价值预测器缓解 LLM 自评估过置信；指数形式实现探索→利用的平滑连续过渡 | 低预算 BAVT 准确率超过基线 4× 资源分配；理论收敛保证 ≥ 1-ε |
| 2602.11574 | ARC | Token 预算（分档）、工具集、workflow 模式、prompt 模板；每查询的配置成本和正确性信号 | 任务级：SMDP 建模，分层 RL（高层选 workflow/工具/预算，低层组合 prompt），masked RL + SFT 混合训练 | 按查询难度动态选择配置四元组（工具集、prompt 模板、token 预算、模型）；预算匹配基线保证公平性 | 推理准确率 +31.3%，工具使用 +13.95%；τ-Bench Pass¹ 9.0%→18.0% |
| 2606.23026 | Stackelberg | 任务级质量评分和成本数据；调用级 token 消耗 | 调用级 + 任务级：Controller（上层）设定质量目标+成本激励，Executor（下层）选择执行参数 | Stackelberg 双层博弈；策略修复（policy repair）——真实 API 校准运行，将模拟策略投影修正到真实操作点 | Token 成本 -17.4%（p=0.022）；质量无显著变化（p=0.44）；条件性理论保证 |
| 2606.04056 | Token Budgets | 每次 API 调用的 token 消耗（provider-reported spend）；在线估计器（静态 4-6×、自适应 2.11×、tokenizer-direct ~1.0×） | 调用级：Rust affine type 实现——Budget 值不可 Clone/Copy，spend/split/merge 均 move self，编译器 enforce 不可复制、不可双花、委托后自动失效 | 编译时硬保证：1180 行 Rust（无 unsafe），用 borrow checker 替代 Arc<Mutex>；63 起事故实证（21 框架，8 失败模式，κ=0.837） | Python asyncio 30/30 超支 vs Rust 0/30；160 次测试零违规零误拒绝；单 Agent 场景 4 行 Python 等效 |

### 阶段三：结构性优化

| ID | 论文 | 记录的指标 | 记录粒度与方式 | 优化策略 | 关键效果 |
|----|------|----------|-------------|---------|---------|
| 2305.05176 | FrugalGPT | 各 LLM API 的定价数据；每查询的 token 消耗和准确率 | 调用级：按查询将调用路由到不同 LLM 组合 | LLM cascade：prompt adaptation + LLM approximation + LLM cascade 三级联 | 以 98% 成本匹配 GPT-4 性能；或同等成本准确率 +4% |
| 2310.05736 | LLMLingua | 每个 token 的困惑度（用小语言模型评估）；压缩前后 token 数和性能对比 | 调用级：粗到细 token 级压缩，预算控制器决定保留比例，指令调优对齐分布 | 困惑度驱动的 token 级剪枝：删除低困惑度 token，保留高困惑度（高信息量）token | GSM8K、BBH 等最高 20× 压缩，性能基本保持 |
| 2310.06839 | LongLLMLingua | 关键信息的密度与位置（非上下文总长）；压缩前后对比 | 调用级：位置感知压缩，针对长上下文场景 | 基于"LLM 性能取决于关键信息密度与位置而非总长"洞察的压缩 | NQ +21.4%（4× token 减少）；LooGLE 成本 -94.0%；10k→2-6× 压缩下延迟加速 1.4-2.6× |
| findings-emnlp.655 | TCRA-LLM | 输入 token 数（压缩前后）；准确率 | 调用级：T5 摘要压缩 + 语义级词项压缩，针对 RAG 场景 | 两级压缩（摘要层+词项层），先粗后细 | 输入 token -65%，准确率 +0.3% |
| 2601.02553 | SimpleMem | 推理 token 消耗（压缩前后）；记忆存储 token 数；F1 分数 | 步级 + 会话级：三阶段流水线——(1)非结构化交互→多字段记忆单元（时间/地点/意图），(2)在线语义合成（合并相关信息），(3)意图感知检索（按需加载） | 语义结构化压缩 + 在线语义合成 + 意图感知检索 | 推理 token -30×，F1 +26.4%（质量成本同时改善） |
| 2605.16045 | RecMem | 记忆构建 token 数（LLM 调用次数 × 每次 token）；准确率 | 会话级：embedding 编码（不需 LLM）跟踪语义相似度，检测语义簇形成后才触发 LLM 做一次性整合 | 基于语义簇的延迟整合（recurrence-based consolidation）：eager → lazy，LLM 仅在语义簇稳定后介入 | 记忆构建 token -87%，准确率不降 |
| 2606.11213 | CWL | 上下文窗口占用率；驱逐决策的 token 影响；episode dependency graph 中的步类型和依赖标注 | 会话级：维护 episode dependency graph，每步标注类型（用户指令/工具调用/环境观察/推理过程）和依赖关系（效果是否已持久化） | 依赖感知驱逐：优先删除"已完成且效果已持久化"的步骤，保留"用户消息+当前活跃推理上下文" | 80M token 连续会话（89 任务）精度无退化；避免 compaction 四缺陷 |
| 2605.19932 | PEEK | 固定大小 context map 的 token 预算；迭代次数；成本（美元） | 会话级：三个模块——Distiller（提取可迁移结构知识）、Cartographer（结构化编辑）、Evictor（按优先级淘汰） | 固定大小方向缓存：缓存"模块组成/目录结构/常用常量"等可跨任务复用的方向知识，非执行轨迹 | 准确率 +6.3-34.0%；迭代 -93-145 轮；成本 -1.7-5.8×；多模型泛化 |
| 2606.14945 | Stateful ReAct | 每轮 token 消耗；总 token 消耗；任务质量指标 | 步级 + 会话级：LangGraph typed persistent state 在轮次间传递，每轮仅看"当前状态 + 上轮摘要"，上下文 O(1) | 有状态 ReAct 替代无状态设计：typed state 替代完整历史堆叠，算法复杂度从 O(n²) 降至 O(n) | 超参数调优 -90%（24,465→2,492）；代码优化 -52%（1,275K→627K）；结构性优化 |
| 2606.31564 | ACE | 上下文 token 数（原始 / 摘要 / 丢弃三类）；每步自适应选择 | 步级 + 会话级：每步对每个历史步骤动态选择 Raw / Abstract / Drop，选择可逆（可回退），双副本存储 | 可逆弹性上下文：原始和压缩双副本存储，每步重新评估，不掉入不可逆压缩陷阱 | 适配 4 个主流 Agent 框架；无需训练和架构修改 |
| 2606.30005 | VISTA | 上下文块元数据（每个块的大小、年龄、访问次数）；Agent 自主管理决策 | 会话级：typed, addressable blocks + 运行时仪表盘暴露元数据，Agent 自主决定 archive/read/delete | 上下文状态可见化：让 Agent 感知自身上下文状态（此前 LLM 对 context window 内部状态"盲"） | LOCA-Bench +28pp（22.7%→50.7%）；提升幅度随上下文压力增大 |
| 2606.17016 | TokenPilot | 压缩后 token 消耗；prompt cache 前缀连续性（KV-cache 命中率） | 会话级：全局层稳定前缀（确保 byte-identical 缓存连续性），局部层按残差效用惰性驱逐 | 缓存友好双粒度方案：保证压缩/驱逐不破坏 prompt cache 对齐，避免 cache miss 的 pre-fill 代价 | 成本 -61% 至 -87%；cache 命中率 38.7%→79.2% |
| 2606.20954 | LRE | 上下文 token 数量（峰值和平均值）；KB 级评分器开销 | 会话级：几 KB 的 CPU logistic regression 评分器，causal features（仅依赖 prefix），annotation-free 训练 | 学习识别"承重"历史（load-bearing）：哪些上下文块删除会导致后续失败；knapsack 选择保留 | 峰值上下文 -52%，匹配全量历史准确率；零 compressor 调用 |
| 2606.40082 | AgentOCR | 文本 token vs 视觉 token 消耗对比；渲染缓存命中率；compression rate | 步级 + 会话级：段光学缓存（hash→visual cache，20× 加速）；Agent 主动发出压缩率作为 action | 跨模态压缩：文本历史渲染为图像，视觉 token 信息密度 ~10× 于文本 token；RL 训练 compression-aware reward | Token -50% 保持 95% 性能；渲染加速 20× |
| 2607.09493 | Shared Selective Memory | 每次调用 token 节省倍数；任务完成率；per-invocation token | 会话级 + 跨会话：四类可复用上下文分类（任务规格、数据 schema、工具配置、输出约束）+ 角色访问控制 | 选择性持久记忆：保留四类可复用上下文，主动丢弃推理轨迹；零 token 数据刷新 | 完成率 96%（vs 无记忆 79%，全量历史反退化至 71%）；97× per-invocation 节省 |
| 2502.00409 | Routing Survey | 调用级 token 消耗和延迟；路由决策历史数据 | 调用级：每查询的路由决策和效果记录 | 四类路由策略分类：相似度路由、监督路由、RL 路由、生成式路由 | 综述性质，确立路由为调用级 token 数据驱动决策的典型范式 |
| 2502.12145 | Fast or Better | 检索 token 成本；准确率；用户可控参数 α | 调用级：两个分类器——Cost-Optimized（便宜但可答对）和 Reliability-Optimized（准确优先），用 α 插值 | Flare-Aug：通过 α 参数实现准确率-成本连续权衡，用户可控 | 适用于多跳 QA；不同模型有不同的 cost-reliability 曲线 |
| 2604.12301 | Local-Splitter | 云端 token vs 本地处理 token；每种策略节省百分比 | 调用级：7 种策略（本地路由、prompt 压缩、语义缓存、本地草稿+云端审查、最小 diff、意图提取、批处理+缓存对齐）在 4 种负载上的系统测量 | 工作负载感知的策略组合选择 | edit-heavy 45-79% 节省；RAG-heavy 需要复杂组合（51%）；开源 shim 实现 |
| 2607.09197 | Routing Diversity | 调用级 token 数据；路由准确率；扰动稳定性 ρ；Hierarchic Social Entropy | 调用级：行为向量 cosine similarity + 多级扰动（字符/词/句法/释义/冗长）下的路由一致性测量 | 确立路由有意义的两个根本条件：(1)候选模型间行为差异足够，(2)路由策略对输入扰动稳定 | KNN 路由准确率高但扰动不稳定；prompt-based 路由全扰动类型稳定 |
| 2606.25519 | Quantization Inflates | 推理 token 数量（量化前后）；CoT Token Inflation Ratio；per-token 延迟 | 调用级：量化前后推理 token 计数对比，step-level semantic similarity 检测重复推理 | 警示性研究：量化增加推理 token 数抵消 per-token 加速，需同时记录二者 | INT4/INT3 均导致 token 膨胀；INT3 最高 +292%；prompt 缓解存在 accuracy-length 权衡 |
| 2606.15017 | Budget-Constrained Web | Token 预算约束下的任务完成率；多 run 方差（Any-of-3 / All-of-3） | 任务级：total token accounting（split by actor/module, prompt/completion），预算匹配对比 | 确立规则：任何增强方案必须与获相同 token 预算的未增强 baseline 对比 | Vanilla 匹配或超越 3 个 SOTA 增强方案（AWM, ASI, ReasoningBank） |
| 2605.23099 | SVR-MAD | 系统级通信 token（Agent 间消息）；Survival Rate（答案经同行挑战后的存活率） | 系统级：每轮辩论后记录各 Agent 的 survival status，基于后验证据（非自报置信度/log-probability）剪枝通信图 | 贝叶斯后验通信剪枝：首轮全回答→次轮成对挑战→逐轮仅保留"答案经受住挑战"的 Agent | 通信 token -61%，准确率不变或提高；prior signals 在困难问题上的正确率仅 5-34% |
| 2510.26585 | SupervisorAgent | 步级检测开销（接近零）；总 token 节省 | 步级 + 系统级：LLM-free 自适应过滤器在三个关键交互点（Agent-Agent, Agent-Tool, Agent-Memory）检查信号，仅在触发时启动 Supervisor | 零开销运行时监督：LLM-free 过滤器检测步级异常（输出无变化/上下文接近上限），监控本身不消耗 LLM token | Token -29.68%（GAIA, pass@1）；跨 5 基准多模型泛化 |
| 2604.17111 | HiveMind | 系统级：并发 Agent 数、失败率、重试次数、API 连接重置率、排队延迟 | 系统级：HTTP 代理层部署五个原语——准入控制、rate-limit 追踪、AIMD 退避+circuit breaker、token 预算管理、优先级队列 | 操作系统级调度：问题不在 API 容量而在并发协调（11 Agent 并发 27% 死于连接重置但总容量足够串行处理） | 失败率 72-100%→0-18%；消除 48-100% 浪费计算；代理层 <3ms/请求 |
| 2607.09600 | Agora | 系统级通信 token；任务分配准确率；校准后的能力评分 | 系统级：拍卖机制——Agent 对子任务出价（非自报能力），单参数 β 控制成本-质量权衡 | 拍卖式任务分配：用竞价过程揭示真实能力（LLM 自评估存在过置信） | 多模基准上通过功能性互补改善路由；校准前拍卖可能劣于基线 |
| RCR-Router | RCR-Router | 各 Agent token 份额；角色-阶段-记忆三维映射；答案质量 | 系统级：结构化共享记忆层 + 轻量级评分策略（角色 relevance × 任务阶段 × 新近度），动态选择记忆子集 | 角色感知动态上下文路由：替代全量/静态路由，按 Agent 角色和当前任务阶段分配预算化记忆子集 | Token -30%，质量持平或提升；三维映射推进系统级记录细分 |
| 2604.02460 | SAS vs MAS | Thinking token 预算（仅中间推理，不含 prompt 和最终答案）；有效上下文利用率 | 任务级：thinking token 独立计量作为计算量归一化核心度量；上下文退化实验（删除/掩码/替换/干扰） | 方法论原则：任何 SAS vs MAS 对比必须控制 thinking token 预算；架构选择应基于单 Agent 的上下文利用率 | 等预算下 SAS 一致匹配或超越 5 种 MAS 架构；MAS 优势多源于未控制的计算量差异 |

### 阶段四：自治闭环

| ID | 论文 | 记录的指标 | 记录粒度与方式 | 优化策略 | 关键效果 |
|----|------|----------|-------------|---------|---------|
| 2607.06906 | Harness Effect | 调用级：每次 API 调用的 token 和延迟；任务级：成本（$）、质量（0-1）、wall-clock（s）；复合：每百万 token 完成任务数、每美元质量 | 调用级 + 任务级：trace shim 记录 per-turn prompt tokens / completion tokens / tool events / wall-clock，任务级聚合；6 模型 × 2 编排层严格控制变量 | 六个效率机制：① cache-shape discipline（两段式 prompt，命中率 99.9%）② 结构化增量压缩（80% 满载触发）③ 上下文卸载（子 Agent 摘要 8KB）④ 零 token 等待 ⑤ 失败-支出治理（重试上限 50）⑥ 模型无关基底 | 成本 -41%（$0.21→$0.12）；token -38%（14.2K→8.8K）；wall-clock -44%（48s→27s）；编排层效果（33-61%）> 换模型（36%）；harness leverage r=0.99 |
| 2604.25850 | AHE | 任务级反馈（成本、质量）；百万 trajectory token 蒸馏为分层证据；文件级版本控制 | 任务级："三柱可观测性"——①组件可观测性（7 种组件类型，文件级版本控制）②经验可观测性（分层 drill-down 证据语料）③决策可观测性（每次编辑 + 自声明预测 + 结果验证） | 自动化 harness 演化闭环：Agent Debugger 蒸馏失败轨迹→meta-agent 生成组件级编辑→下一轮验证预测→错误回滚 | Terminal-Bench 2 pass@1：69.7%→77.0%（超人类最优 71.9%）；SWE-bench 12% 更少 token 达最高分 |
| 2606.01365 | Early Diagnosis | 步级：post-warning token 占比（警告后继续消耗的 token 比例）；在线信号开销（LLM-free） | 步级 + 系统级：三层检测——(1)在线信号层（运行时，LLM-free，循环/预算压力/低信息增益/工具不稳定）(2)离线语义层（任务后，接地性评分）(3)LLM-as-judge（最后手段） | 实时浪费检测+主动干预：基于步级信号在运行时检测异常，干预包括强制切换策略/要求证据/增加多样性 | 失败 run 中 58.1% token 在首次警告后继续消耗；主动干预将 post-warning 从 63.8% 降至 30.4% |
| 2606.10307 | Early-Token Confidence | 前几个 token 的置信度；推理质量预测 | 调用级：生成初期（前几个 token）的置信度信号作为推理质量预测器 | 早期终止：如果初期 token 置信度表明推理质量差，提前终止节省后续 token | 前几个 token 置信度可有效预测最终推理质量 |
| 2602.09138 | PABU | 步级进度估计（0-100%，自上一轮相对进展）；选择性保留决策（binary gate） | 步级：每步预测两个值——相对进度 + 是否值得记住当前交互，仅被判定"值得记住"的交互进入信念状态 | 进度感知信念更新：基于进度预测做选择性上下文保留 | 步数 -26.9%（平均 9.5 步）；task completion 81.0%，超过 full-history SOTA 23.9% |
| 2605.14558 | ActFocus | 动作 token vs 推理 token 的分类计数和梯度贡献（token-level free energy） | 步级：区分 trajectory 中 `<think>` 和 `<answer>` 两类 token，计算每个 token 对 reward variance 的"能量"贡献 | Token 级训练信号重加权：reasoning token 降权、action token 加权（不确定性越高权重越大），零额外开销 | 比 PPO +65.2pp，比 GRPO +63.7pp；action-token energy 与 reward variance ρ=+0.537 |
| 2607.08938 | Better Harness | 任务级：SLM vs LLM 成本比例；性能恢复百分比；task diversity 度量（tool-call Levenshtein distance） | 任务级：meta-agent 自动分析 SLM 失败轨迹→按 failure mode 分类（tool-use / instruction-following / knowledge / long-context / planning）→生成针对性 harness 编辑 | 编排层适配小模型：GEPA-style genetic search 在 harness pool 中自动优化 | SLM 4% 成本恢复 LLM 89.7% 性能；16/21 task-SLM 对提升，7 对完全弥合差距 |
| 2607.04528 | Harness Belief | 步级决策轨迹（9 维信念：progress, risk, recoverability, constraints, failure mode, uncertainty, future success, repair cost, next action） | 步级：K-step belief trajectory JSONL，cross-harness belief divergence D_belief = D_arrival（即时偏移）+ D_growth（horizon 累积偏移） | 系统性研究：harness 设计本身改变 Agent 多步决策信念轨迹，通过 BIWM 协议使 harness-occluded 信息可审计 | Harness 是实验可复现性的关键变量；不同 harness 导致相同模型决策轨迹显著不同 |
| 2607.08894 | GATS | 规划阶段 LLM 调用次数（0 vs 37）；计划确定性 | 步级：三层世界模型——L1 精确符号匹配（历史缓存）→L2 统计推断（执行日志统计模型）→L3 LLM 回退（仅未知动作） | 零 LLM 调用规划：UCB1 树搜索替代 LLM 引导探索；persistent state-transition graph 合并相同状态节省预测成本 | 规划阶段 0 次 LLM 调用（LATS 37 次）；100% 成功率；12/12 压力测试通过；计划零方差 |
| 2607.02911 | CoACT | 观测压缩后 token 数；下一步动作一致性（NAP score） | 步级：teacher 生成压缩候选→NAP 奖励过滤（保留不改变 agent 下一步 action 的候选）→length-reduction 奖励选最短→训练轻量 compressor | 动作保持的观测压缩（NAP 准则）：压缩后观测送入 Agent 后下一步动作不变即为信息无损 | 总 token -33.0%，任务能力接近不压缩 baseline；SWE-bench Verified 上三模型验证 |
| 2607.05378 | CompactionRL | 压缩率（Agent 主动发出）；benchmark 分数；summary 长度变化；reasoning tokens per turn | 步级 + 会话级：PPO 联合训练执行和摘要生成，token 级损失归一化，cross-trajectory GAE（跨压缩边界保留时序信用） | 训练中内化压缩：上下文超预算时 policy 生成摘要而非推理时外挂压缩器 | GLM-4.5-Air SWE-bench +7.0pp（66.8%）；Terminal-Bench +3.1pp；已部署 GLM-5.2（750B-A40B）训练流水线 |

---

## 十、论文索引

| ID | 论文 | 涉及粒度 | 核心贡献 |
|----|------|---------|---------|
| 2605.09104 | Token Economics | — | 四层分析框架，68× 增长背景数据 |
| 2305.05176 | FrugalGPT | 调用 | 成本感知 LLM cascade 三策略（-98% 成本） |
| 2310.05736 | LLMLingua | 调用 | 粗到细 token 级 prompt 压缩（20×） |
| 2310.06839 | LongLLMLingua | 调用 | 长上下文 prompt 压缩（NQ +21.4%） |
| findings-emnlp.655 | TCRA-LLM | 调用 | RAG 场景 prompt 压缩（-65% token） |
| 2601.14470 | Tokenomics | 调用→阶段 | SDLC 六阶段 token 分布实证（CR 59.4%） |
| 2603.12634 | BAVT | 步 | 剩余预算驱动探索/利用平滑切换 |
| 2602.11574 | ARC | 任务 | SMDP 按难度动态分配配置 |
| 2606.23026 | Stackelberg | 调用+任务 | 双层博弈+策略修复（-17.4%） |
| 2606.04056 | Token Budgets | 调用 | 63 起事故+affine type 编译时保证 |
| 2601.02553 | SimpleMem | 步+会话 | 三阶段语义压缩（-30× token） |
| 2605.16045 | RecMem | 会话 | 语义簇检测→延迟整合（-87%） |
| 2605.19932 | PEEK | 会话 | 固定大小方向缓存（-1.7~5.8× 成本） |
| 2606.14945 | Stateful ReAct | 步+会话 | O(n²)→O(n) 结构性优化（-90%/-52%） |
| 2606.11213 | CWL | 会话 | 依赖图驱逐（80M token 无退化） |
| 2606.31564 | ACE | 会话 | 可逆弹性上下文（4 框架） |
| 2606.30005 | VISTA | 会话 | 上下文状态可见化仪表盘（+28pp） |
| 2606.17016 | TokenPilot | 会话 | 缓存友好双层上下文（-61~87%） |
| 2606.20954 | LRE | 会话 | KB 级 CPU 驱逐评分器（-52%） |
| 2606.40082 | AgentOCR | 步+会话 | 视觉 token 历史表示（-50% token，95% 性能） |
| 2607.09493 | Shared Selective Memory | 会话 | 四类可复用上下文（97× 节省） |
| 2502.00409 | Routing Survey | 调用 | 四类路由策略综述 |
| 2502.12145 | Fast or Better | 调用 | 用户可控 α 成本-质量平衡 |
| 2604.12301 | Local-Splitter | 调用 | 7 策略工作负载维度测量（-45~79%） |
| 2607.09197 | Routing Diversity | 调用 | 路由有意义性两个根本条件 |
| 2606.25519 | Quantization Inflates | 调用 | INT4 量化增加推理 token |
| 2606.15017 | Budget-Constrained Web | 任务 | Token-matched baseline 必需性 |
| 2605.23099 | SVR-MAD | 系统 | 贝叶斯后验通信剪枝（-61%） |
| 2510.26585 | SupervisorAgent | 步+系统 | LLM-free 运行时监督（-29.68%） |
| 2604.17111 | HiveMind | 系统 | HTTP 代理层调度防并发崩溃（72%→18%） |
| 2607.09600 | Agora | 系统 | 拍卖式任务分配 |
| RCR-Router | RCR-Router | 系统 | 角色感知动态上下文路由（-30%） |
| 2604.02460 | SAS vs MAS | 任务 | 等预算 SAS 不弱于 MAS |
| 2607.06906 | Harness Effect | 调用+任务 | 编排层因果实验（-41% 成本） |
| 2604.25850 | AHE | 任务 | 三柱可观测性闭环（+7.3pp） |
| 2606.01365 | Early Diagnosis | 步+系统 | 三层检测（58% 浪费可挽回） |
| 2606.10307 | Early-Token Confidence | 调用 | 前几个 token 置信度预测推理质量 |
| 2602.09138 | PABU | 步 | 进度预测+选择性保留（-27% 步） |
| 2605.14558 | ActFocus | 步 | Token 类型训练梯度区分（+65pp） |
| 2607.08938 | Better Harness | 任务 | 编排层适配让 SLM 接近 LLM |
| 2607.04528 | Harness Belief | 步 | 编排层改变 Agent 决策轨迹 |
| 2607.08894 | GATS | 步 | 零 LLM 调用规划（100% 成功） |
| 2607.02911 | CoACT | 步 | NAP 动作保持压缩（-33%） |
| 2607.05378 | CompactionRL | 步+会话 | RL 训练中内化压缩（+7.0pp） |
