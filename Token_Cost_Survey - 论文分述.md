# 面向token消耗与成本优化的大语言模型研究：各论文详细分析

> 本文按研究主题将 65 篇论文划分为十三类（综述总论、实证测量与成本拆解、计费透明与账单核查，以及上下文与输入优化、推理链优化、记忆管理优化、记忆与技能的可执行化、路由与调度、多智能体协调、模型协作与大小模型分工、数据格式与表示优化、训练时效率塑造、执行粒度控制十类优化路线），依分类顺序逐篇进行详细分析，内容均以论文原文（papers/ 目录下的 PDF）为准。每篇包含八个字段：基本信息、研究内容、方法、实验设置、关键结果、观测、动作、优化方式。其中，观测字段给出该论文的观测粒度（参照八级谱系：token级、步骤与动作级、调用级、轮次级、智能体级、阶段级、会话与轨迹级、系统级）、观测内容与数据来源，对应观测空间；动作字段给出该论文使用观测空间（监测到的 token 粒度信息）做了哪些控制动作，与观测构成"观测—动作"配对，对应汇报综述第 3.10 节；优化方式字段给出该论文具体用什么方法实现对 token 消耗的优化及所属路线。

---

## 一、综述与总论

### [1] Token Economics for LLM Agents: A Dual-View Study from Computing and Economics

**基本信息**: arXiv:2605.09104 | 综述（Preprint） | Yuxi Chen、Junming Chen、Chenyu He 等（浙江大学计算机学院与经济学院、区块链与数据安全国家重点实验室、阿里云）

**研究内容**: 本文是第一篇将token经济学系统化的跨学科综述，从计算机与经济两个视角分析智能体的token成本。背景是token消耗的爆炸式增长：OpenRouter 平台的每周token处理量从 2024 年 12 月的 0.4 万亿增长到 2026 年 3 月的 27 万亿，15 个月涨了约 68 倍。作者把这种消耗上升为系统性压力：智能体工作流不同于单轮 LLM 推理，靠“推理—工具调用—自我纠错”的迭代循环推进，每循环一次都直接消耗token；多智能体场景下，token流还要经历输入、智能体间通信与输出三个阶段。作者援引国际能源署的预测：全球数据中心用电量到 2030 年将翻倍，行业正从单纯扩展算力转向优化token效率。

现有综述分三个孤岛——智能体架构、系统优化与安全可信，没有统一框架衡量“输出质量对经济成本”的权衡，也就解释不了为什么局部最优的工程选择常常造成全局浪费。这篇综述把token当作经济基本单元（生产要素、交换媒介、记账单位），用经济学理论把各孤岛的结论纳入同一套语言。核心贡献有三点：建立统一的双视角框架，把智能体推理重述为受约束的资源分配问题；按架构尺度系统归类文献，从单智能体、多智能体、生态到安全逐层指出瓶颈；把恶意攻击与安全对齐机制重新定义为token经济中不可忽视的损耗来源，并给出可通过梯度训练优化的token预算、动态token市场等前沿方向。

![78574076012](C:\Users\yzy14\AppData\Local\Temp\1785740760120.png)

**方法**: 内容主体是四维分析框架。微观层：单智能体像一家厂商，用新古典厂商理论（经济学中分析厂商在约束下如何最优配置资源的理论）分析预算约束下“内部推理对外部工具”的分配（外部工具调用被视为要素替代）。中观层：多智能体系统像公司层级，瓶颈是交易成本——智能体反复传递完整上下文产生“通信税”（输入输出比约 2:1 到 3:1），以及委托-代理式的信息不对称（服务方与用户目标不一致所导致的信息不对称）。宏观层：智能体生态像市场，讨论缓存定价、QoS 分层与拥堵外部性等机制设计。安全层：把恶意攻击当作系统内部自带的经济约束，总成本公式为算力、协调、防御投入与期望攻击损失之和，防御与损失一升一降，存在最优投资点。token按生命周期功能分为输入、推理、通信、外部与输出五类，最后给出工程优化、资源配置、安全管理三大优化范式。

**实验设置**: 综述类论文，无自身实验；系统性汇总文献中的实证数据作为支撑。

**关键结果**:
- 智能体型编码任务的 token 量级远超单轮推理（一次任务消耗是单轮推理的 1000 倍以上、输入占比超 150:1）——成本压力主要在长期任务的输入侧，优化应优先对准输入
- 前沿模型执行前无法预测自身消耗（Pearson $r \le 0.39$ 且系统性低估）——预算控制不能依赖模型自报，必须外部记账
- 代码评审迭代占 59.4% 的 token（输入输出比约 2:1）——通信开销集中在评审循环，是最值得压缩的目标
- 换表示/换路径比压文本更有效：潜在空间 KV 传输省 70–84% 通信 token；SGLang 前缀匹配把多轮吞吐提 2–5 倍；INT4 推理每 token 能耗降 40–75%
- 提示缓存的真实成本（约 0.07 美元/M token）远低于名义价——缓存是性价比最高的干预点

**观测**: 粒度：覆盖 token级到生态级的多个层级｜观测内容：以生命周期功能五分类（输入、推理、通信、外部、输出）为统一记账维度，观测平台 token 总量趋势与各角色占比｜数据来源：平台级统计（综述汇总）

**动作**: 用观测到的输入/通信token占比，识别成本主体在输入与通信，据此给出"优化应优先瞄准输入压缩与缓存"的方向判断（综述级决策依据）。

**优化方式**: 不直接优化；提供经济学取舍框架：识别输入与通信 token 为成本主体（通信开销），指出优化应优先瞄准输入压缩与缓存、缓存定价是最高杠杆｜所属路线：综述（经济框架）

---
### [2] Doing More with Less: A Survey on Routing Strategies for Resource Optimisation in Large Language Model-Based Systems

**基本信息**: arXiv:2502.00409 | 综述（Preprint） | Clovis Varangot-Reille、Christophe Bouvard（Wikit）、Antoine Gourru、François Jacquenet（圣埃蒂安大学）、Mathieu Ciancone、Marion Schaeffer（INSA Rouen Normandie）

**研究内容**: 大多数 LLM 系统是单体静态架构，一个通用大模型处理所有查询，简单查询也按大模型计价，成本浪费明显。论文指出，不同查询需要的推理深度、领域知识和预处理各不相同，通用大模型虽然面面俱到，但对简单查询付出的金钱、能耗和算力成本不成比例。路由机制把每个查询导向更合适的组件（小模型、专精模型、工作流、数据源），但这块文献此前零散：早期只在 LLM 推理优化综述中附带提及，早期综述策略覆盖也有限。

这篇是第一份系统梳理 LLM 路由策略的综述，把路由正式定义为带预算约束的组件选择问题：对每个查询选一个组件，让匹配质量最高，同时调用成本不超过预算；成本口径包括每token单价、延迟、计算与环境成本。全文围绕三个问题组织：优化什么（质量/成本）、何时路由（生成前/生成后）、怎么实现（四类策略），并明确排除了“从候选输出中选最优答案”和模型集成方法，理由是它们优先性能而非成本。它明确把路由和 MoE 内部门控区分开：路由器在模型外部、独立工作、受预算约束、候选不限于神经网络。综述还指出三大开放挑战：评估缺乏标准化；引入新候选组件通常要重训路由器；多数研究忽略计算与环境成本。

**方法**: 核心定义是带预算约束的选择问题：选一个组件，让匹配质量最高，同时成本（单价、延迟、计算、环境成本）不超过预算。按决策时机分两代：生成前路由在生成前预测质量再选组件，便宜但可能选错；生成后路由（级联）先生成再评估，不行就升级更强模型，可靠但每查询要生成多次。按实现方式分四类：相似度类（查询嵌入找相似、聚类、偏好排序）、监督类（矩阵分解推荐、领域分类、查询复杂度推断、知识图谱、置信度推断）、强化学习类（无状态学习自动机、有状态的逐次选择、奖励模型推断）、生成类（token的边际概率、序列概率、提示路由、LLM 微调、重复调用、代码执行结果）。

**实验设置**: 综述类，无自身实验；汇总文献中的路由效果数据。

**关键结果**:
- 级联路由能把成本压一个数量级（FrugalGPT 省 59–98% 成本、保持近似 GPT-4 精度）——"先便宜后升级"是通用有效的降本思路
- 简单查询用便宜模型基本不损失精度（MetaLLM 每万查询 0.3 美元 vs 全大模型 4.8 美元，精度 91.0 对 90.9）——多数查询不值得用贵模型
- 路由的收益可来自"少调大模型"（RouteLLM 以 30% 大模型调用拿到 80% 质量收益）或"换廉价等价物"（MoDEM 以 70B 池接近 405B 精度、单价低 4 倍）
- 落地路由要先解决评估与部署问题：评估缺随机路由和 oracle 基线、引入新候选要重训、多数研究忽略计算与环境成本，部分监督方法并不优于随机

**观测**: 粒度：请求级（调用级）｜观测内容：每个查询的路由决策、所选模型的单价/延迟/计算/环境成本与效果记录｜数据来源：各路由文献汇总

**动作**: 用观测到的每查询路由成本与效果，确立"带预算约束的组件选择"框架，据此区分生成前路由与生成后级联的取舍。

**优化方式**: 综述，不直接优化；给出路由分类（生成前/生成后级联）与实现方式（相似度/监督/RL/生成式），确立"带预算约束的组件选择"问题框架｜所属路线：路由与调度（综述）

---
### [3] Agentic Context Management: Solving Agent Memory and Cost by Treating Them as Lifecycle and Architecture Problems

**基本信息**: arXiv:2607.21503 | 预印本（厂商论文） | Gaurav Dadhich（Maximem）

**研究内容**: 生产级智能体的失败主因不是推理能力，而是“管理不好推理上下文”——会话历史、大提示词、大工具定义、膨胀的工具输出持续累积。现有的“记忆”框架只把问题当成存储与检索，论文认为这个框架太窄，提出“Agentic Context Management”：上下文管理是生命周期而不是仓库，分解为五个基本环节——设计记忆形态、写入记忆、划定检索范围、提前预取、压缩与固化；每个环节在用户、客户、终端客户三级范围层级上运行并严格隔离。

经济论证是全文的核心。全量追加（不做任何压缩）的成本模型：每轮新增 t token、共 n 轮，累计输入token为闭式求和，约为 O(n²)；有界预算时是线性，两者的倍数随 n 线性增长；t=500、W=4000 时，500 轮累计输入约为基线的 31 倍。只有“验证式压缩”（压缩后检查信息丢失、低于阈值自动降档重试）同时做到线性成本与保真度保留——压缩是验证操作，不是丢弃操作。需要说明：这是厂商论文，基准数字为自报（gpt-5-mini、LongMemEval Cat 1-4 口径），不可与学术方法直接比较，其结果不列入方法对比。

**方法**: 五个基本环节的分工：架构设计生成每个智能体的记忆架构（LLM 推理式设计、多智能体检查），支配其余四个环节；写入是异步队列管道——先做资格筛选，再抽取事实/偏好/情节/时态事件，做实体解析，最后持久化到关系/图/向量三种存储；划定范围按范围层级从窄到宽解析、严格隔离，返回带来源标签与token预算的排序条目；提前预取预测式预取上下文，把检索移出请求关键路径（当前跨客户命中率 60% 以上）；压缩是验证式操作——压缩时做信息丢失检查，输出验证分数与压缩率，低于阈值自动降档重试。质量链：最终答案质量受抽取、检索、推理三环节最弱者的限制。参考实现 Maximem Synap 是多租户服务。

**实验设置**: LongMemEval 500 题 + LoCoMo（Cat 1-4）；答案模型与判官 gpt-5-mini；对比表仅作“公开图景”且明确声明不同方法论不可横向比较；检索动机研究在 5 个语料（各 1 万文档、1 千查询）上对比关键词与向量检索。

**关键结果**:
- 全量追加的累计输入随轮数近似 O(n²)（t=500、W=4000 时 500 轮是基线的约 31 倍），而验证式压缩相对全量追加净省 80%（100 轮）到 96%（500 轮）——上下文管理越早做、轮次越多，省得越多（厂商自报，引用须标注不可比）
- 压缩必须是"验证操作"：单步把 18,282 token 压到 122 token 会把任务准确率从 66.7% 砸到 57.1%（低于无上下文基线）——不验证的激进压缩会反噬
- 记忆系统会积累大量垃圾：一个流行记忆库 32 天存 10,134 条、仅 38 条可用（垃圾率 99.6%）——"只管加、不整理"的记忆没有可用性
- 检索方式要按语义鸿沟选：语义鸿沟大时向量占优（CodeXGLUE MRR 0.914 对 0.290）、查具体实体时关键词占优（SciQ 0.815 对 0.614），但嵌入索引慢 60–100 倍

**观测**: 粒度：轮次级（向会话轨迹级换算）｜观测内容：每轮新增token、累计输入token的解析成本模型，以及记忆库垃圾条目占比｜数据来源：厂商自报，未实测

**动作**: 用观测到的"未验证压缩导致准确率悬崖"（单步 18282→122 token 时准确率 66.7%→57.1%）与记忆库垃圾率 99.6%，确立"压缩是验证操作而非丢弃操作"的决策原则。

**优化方式**: 不直接优化；提出上下文管理的五环节框架（设计记忆形态/写入/划定检索范围/预取/压缩固化）与"验证式压缩"原则，强调未验证压缩会导致准确率悬崖｜所属路线：记忆管理（概念框架）

---

## 二、实证测量与成本拆解

### [4] How Do Coding Agents Spend Your Money? Analyzing and Predicting Token Consumptions in Agentic Coding Tasks

**基本信息**: ICLR 2026 审稿中（OpenReview，匿名作者）

**研究内容**: 编码智能体是应用最广的智能体类型：能够阅读代码仓库、推理问题、调用工具，并在极少人工监督下提出补丁。然而，复杂工作流会消耗数百万token，token用量随任务规模快速增长，而现有按用量计费的定价模式存在两个被广泛批评的问题——不透明（用户只有在任务结束后才知道实际花费）与没有完成保证（任务失败仍然照常收费）。这两个批评最终收敛到同一个核心问题：能否在任务执行之前预测token消耗？如果能够预测，用户就能提前评估成本、选择模型与策略，服务商也能设计更清晰的价格档位、预算上限与早期预警机制。

本文是据作者所知第一个对智能体token消耗进行建模与预测的研究。作者以 OpenHands 为案例开展实证分析——它是性能领先、执行过程透明、账目可核查的开权重智能体；基准选用 SWE-bench Verified，这是唯一经过人工审核、排除了歧义样本的大型软件工程基准。研究回答三个问题：不同任务与运行是否消耗不同token？任务难度如何影响消耗？输入与输出token哪一类主导顺序执行的成本、在轨迹上如何分布？实证发现：消耗存在内在随机性，同一任务的不同运行之间差异最高可达 10 倍；消耗越多的任务与运行，其准确率反而越低；与聊天和推理任务不同，即使启用缓存，输入token仍主导总账单；消耗并不集中于单一步骤，而是读长上下文（文件、diff、检索产物）与反复工具调用占据了输入预算的大头。论文随后把预测任务形式化——从运行开始时的信息估计总token量、输入量与输出量，结论是执行前精确预测总量仍很困难（Pearson r 低于 0.15），但预测输出token量与总消耗范围是可行的，可为执行前预算警报与成本区间提示提供信号。

![78573700846](C:\Users\yzy14\AppData\Local\Temp\1785737008469.png)

![78573713112](C:\Users\yzy14\AppData\Local\Temp\1785737131121.png)

![78573690964](C:\Users\yzy14\AppData\Local\Temp\1785736909646.png)

**方法**: 数据侧，从 LLM 调用日志解析出每轮的token指标，按 Claude API 的四类计费口径记账：非缓存输入、输出、缓存创建与缓存读取，总成本为四类token数分别乘以各自费率后的和，缓存持久化时间为 5 分钟。轨迹按轮数五等分为早期到后期五个阶段，观察成本结构随任务推进的变化。预测侧对比两种方式：一是单模型预测，包含四种信息设置（仅问题、加工具使用推理、加难度、加仓库信息，其中仓库信息用两步提示压缩）与两种刻度（精确数字、对数刻度）；二是“自预测”，即让编码智能体自己充当预测器，以低成本估计自身将消耗的token量。

**实验设置**: SWE-bench Verified 约 500 实例，每实例 4 次独立运行；主模型 Claude Sonnet 3.7，另有 Sonnet 4、Qwen3-Coder-480B-A35B、GPT-5（附录）；每问题按难度分档（少于 15 分钟、15 分钟至 1 小时、1 至 4 小时、超过 4 小时）；指标为 Pearson 相关、准确率与每轮成本分解。

**关键结果**:
- 智能体型编码一次任务平均约 195 万提示 token、成本 6.10 美元，量级远超直觉，且任务间跨度极大（成本 0.72–50.65 美元）——预算要给足余量
- 同一任务不同运行消耗差最高 10 倍、高成本任务方差更大，10.31% 的"简单"任务比平均"困难"任务花更多 token——单次运行的成本数字不可靠，必须多次运行取统计
- 逆测试时扩展（通常测试时多花算力换精度，这里是反的）：token 与工具用得越多准确率反而越低，元凶是重复的文件查看与编辑——砍重复动作是直接收益
- 即使启用缓存，输入 token 仍主导总成本（早期由缓存创建主导、后期由输出主导）——输入压缩与缓存命中是主要杠杆
- 执行前精确预测总消耗不可行（Pearson r<0.15），但预测输出 token 与消耗范围可行，且对数刻度比精确值实用——预算预警应给范围而非单点
- 让模型自预测可省约 4.5 倍 token（成本 1.38 对 6.10 美元），可当粗粒度难度信号（Spearman r=0.354），但系统性高估——只能作难度分级，不能作精确预算

**观测**: 粒度：调用级（兼阶段级）｜观测内容：每轮调用的四类计费token、按轨迹五等分的阶段聚合、跨运行的整任务方差与准确率｜数据来源：框架记录（OpenHands 执行日志）

**动作**: 用观测到的调用级四类token与跨运行方差，产出"执行前成本范围预测与预算警报"信号（对数刻度），支撑执行前的预算控制。

**优化方式**: 非优化方法；产出"执行前成本范围预测与预算警报"信号（对数刻度），并指出减少重复文件往返编辑是最高价值的改进点｜所属路线：实证测量

---
### [5] Tokenomics: Quantifying Where Tokens Are Used in Agentic Software Engineering

**基本信息**: arXiv:2601.14470 | MSR'26 | Mohamad Salim、Jasmine Latendresse、SayedHassan Khatoonabadi、Emad Shihab（康考迪亚大学）

**研究内容**: 多智能体系统被用来自动化软件开发，但“token到底花在哪了”在软件工程语境下还没有答案，成本不可预测阻碍了实际采用。论文指出，此前工作只提供了通用语境的分析：AGENTTAXO 刻画了一般多智能体系统的token分布并提出“通信税”概念，MAST 失败分类学发现多数失败源于步骤重复、验证不完整等系统级协调问题；但针对软件工程这种多阶段工作流的资源效率研究仍是空白，实践者最关心的“token花到哪里去了”在软件工程领域仍无人回答。

这篇是第一个针对软件工程的token消耗实证研究：改造 ChatDev 框架、加装token记录功能，用 GPT-5 推理模型跑 30 个开发任务，记录完整执行轨迹。选 ChatDev 是因为其“聊天链”架构呈现清晰的瀑布式流程（设计→编码→测试），阶段边界分明，且是流行度高的开源框架；任务取自 ProgramDev 数据集，从简单算法（斐波那契）到复杂应用（国际象棋），复杂度差异足够大。方法贡献是提出“Rosetta Stone”映射框架——把 ChatDev 的内部阶段对应到通用的 SDLC 六阶段（设计/编码/补全/评审/测试/文档），其他框架也能套用这套标准化口径，为跨框架效率比较提供共同基准。研究问题定为“多智能体软件开发系统的token消耗模式是什么”，并顺带发布 30 条执行轨迹数据集与完整复现包。

![78574869907](C:\Users\yzy14\AppData\Local\Temp\1785748699078.png)

**方法**: 三步流水线。第一步轨迹收集：在 ChatDev 里加装记录功能，记下每次 LLM 调用的提示、响应和三类token数（输入/输出/推理），把整轮对话完整保存。第二步阶段映射：按框架文档把 ChatDev 内部 phase 对应到 SDLC 阶段，例如 CodeReview phase 对应代码评审，EnvironmentDoc/Reflection/Manual 对应文档。第三步聚合：按阶段汇总 30 个任务的token总量，再按输入/输出/推理拆开。模型是 gpt-5-2025-08-07，temperature 固定 1.0，上下文窗口 400K，最大输出 128K。由于智能体自主决定执行哪些阶段，不是每个任务都经过全部六阶段（补全只出现在 6/30 个任务，测试 12/30）。

**实验设置**: 30 个软件开发任务 × ChatDev × GPT-5（gpt-5-2025-08-07，temp 1.0）；任务来自 ProgramDev 数据集（从斐波那契到国际象棋，推理token消耗 17,280–40,000）；双轴分类：方向（输入/输出/推理）+ SDLC 六阶段。

**关键结果**:
- 主要成本不在"写代码"而在"改代码"：代码评审占 59.4% 的 token（30 个任务一致），初始编码只占 8.6%、设计 2.4%——优化评审循环的收益远大于优化生成
- 输入 token 主导总消耗（53.9%，输入:输出≈2:1）——通信开销集中在输入侧
- 阶段间差异显著，要配不同压缩策略：编码是唯一输出主导的阶段（输出 58.0% 对输入 6.8%），文档与评审以输入为主（80.2% / 51.4%）
- 可用做法：按工作类型画成本地图定位瓶颈；评审前加人工检查点打断昂贵循环；需要 token 高效的验证协作协议

**观测**: 粒度：阶段级（兼调用级）｜观测内容：每次调用的输入/输出/推理三类token，按 SDLC 六阶段聚合的结构性分布｜数据来源：改造 ChatDev 框架加装记录功能

**动作**: 用观测到的SDLC六阶段token分布（评审占59.4%），识别评审循环为主要成本，据此建议"评审前插入人工检查点、做评审压缩"等动作。

**优化方式**: 非优化方法；产出的"阶段成本地图"支撑后续动作——评审压缩、上下文复用、评审前人工检查点，并实证"输入 token 主导成本"（占 53.9%）｜所属路线：实证测量

---
### [6] Token Reduction Is Not Cost Reduction: An Empirical Study of End-to-End Efficiency in API-Based Coding Agents

**基本信息**: arXiv:2607.12161 | 预印本 | Sarel Weinberger、Amir Hozez（PointFive）

**研究内容**: 编码智能体的上下文削减层——压缩命令输出的工具、重排检索结果的方法、在 API 边界改写请求的代理——几乎都用“少用了多少token”来宣传自己的效果。这篇论文用真实账单数据质疑这个标准：token少了，账单不一定少，原因有三。其一是缓存计价，现代服务栈对缓存读写打折计费（缓存读约 0.1 倍、缓存写约 1.25 倍的名义输入价），本负载里缓存读写占重建成本的约 87%，从缓存里删文本省下的钱远小于token数量暗示的量；其二是智能体是闭环，压缩删掉模型还需要的内容后，模型会重新搜索、重读文件、多跑诊断轮次，而每多一轮，整个上下文前缀都要重新发送一遍；其三是压缩会误伤工具链，比如把检索排名摘要喂给期望原始 grep 输出的 shell 统计命令，会静默得到错误答案——损坏的不是模型的理解，而是下游程序的输入。

论文由此把经常被混为一谈的六个度量层拆开：组件级压缩率、检索质量、模型可见的上下文削减、计费token削减、端到端任务成功、每次成功执行的成本。这是一项用真实计费账单拆解编码智能体成本的实证研究：实验设计在执行前冻结（论文自称 pre-specified，不是 pre-registered），主实验 2,908 次由提供商实际计费的成对运行——103 个任务、7 个仓库、3 个模型、4 个实验配置，整个测量程序约 5,500 次计费执行。

**方法**: 成本重建公式按四类token计价：非缓存输入按原价，缓存创建乘 1.25（五分钟缓存 TTL 的写溢价），缓存读取乘 0.1（折扣价），输出按输出价；用这套口径重建单次运行的成本，中位误差约 1%，说明计费口径被正确还原。实验是比较四种“上下文削减配置”：基线 Claude Code（不挂任何层）、RTK（确定性的命令输出压缩层）、RTK-ML（在 RTK 上加九种检索与重排能力的完整配置）、Headroom v0.27.0（在 API 边界改写请求的第三方代理）。同一任务×模型×努力度×重复组成一个块，四种配置从相同的新鲜工作副本按随机顺序各跑一次；成本取提供商返回的真实计费金额，成功与否由冻结的判定脚本打分。决策指标是“每次成功执行的成本”：失败运行的消耗也计入成本分子，防止“快速失败显得便宜”的假象。另有一个单发研究，把“字节精确的 grounding 上下文窗口”与“压缩”组合成四组对照，专门考察压缩对补丁锚点的破坏。

**实验设置**: 主实验 103 个任务 × 24 个任务族 × 7 个仓库（click、cobra、express、flask、gin、requests 加一个种子合成的单体仓库）；模型 Claude Haiku 4.5（5 档努力度）、Sonnet 5、Opus 4.8；2,908 次运行中 2,848 次进入分析。另有 Codex 跨智能体复现（40 对任务）；单发 grounding 研究 362 次计费试验。

**关键结果**:
- 缓存读写才是账单主体：缓存创建 44.3% + 缓存读取 35.4% 约占实际账单 80%（每次运行约 117K 缓存读 token，只生成约 715 个输出 token）——省缓存读写比省输出重要得多
- 削减 token 不等于削减成本：RTK-ML 把工具输出 token 减 38.4%，配对成本反而 +6.7%——任何优化都必须用真实账单验证，不能只看 token 数
- 在 API 边界做代理改写（Headroom，每成功执行成本 1.464）是最贵配置——第三方改写层的开销可能超过它省下的
- 压缩会摧毁补丁锚点：省 73% 可计费 token 但补丁可应用率从 27/40 掉到 15/40，字节精确 grounding 能把它从 31% 拉回 83%——压缩必须保住"可执行的精确细节"
- 单框架结论不能外推：同一方案在 Claude Code 上最贵、在 Codex 上却省 12.49%（但墙钟 +238.5%）
- 上下文压缩层能触达的节省很窄：基线首次调用就有 76–85% 缓存读来自框架自带前缀，压缩层能触及的缓存美元保守估计仅约 4%
- 安全红线：把检索摘要喂给期望原始输出的命令会静默产出垃圾答案（4/4 失败）——压缩层必须感知下游是"解析器还是人"

**观测**: 粒度：调用级（兼阶段级）｜观测内容：每次调用的四类计费token、按轨迹轮次与阶段聚合的成本构成、每次成功执行的成本｜数据来源：服务商返回的真实计费账单

**动作**: 用观测到的真实账单四类token占比（缓存读写合计约80%），确立"token削减≠成本削减"的判断标准，据此要求任何声称的节省须经账单级验证。

**优化方式**: 非优化方法；立"token 削减不等于成本削减"标尺——任何声称的节省都必须回答省掉的 token 在计费上值多少钱、压缩后会不会多跑轮次；提出 L1–L8 分层证据标准｜所属路线：实证测量

---
### [7] Sample More, Reflect Less: Self-Refine and Reflexion Lose to Repeated Sampling at Equal Token Cost, from 1.5B to 7B

**基本信息**: arXiv:2607.28576 | 预印本 | Iliya Mirzaei（石溪大学）

**研究内容**: 大量“测试时推理增强”方法——CoT、Plan-and-Solve、Self-Refine、Reflexion、ToT、多智能体辩论——都声称让模型更会推理，但它们几乎都靠生成更多文本：单条思维链约 300 token，三轮自我批评约 1,600 token，三智能体两轮辩论约 2,500 token。“方法有用”的结论很可能只是“计算预算变大了”的错觉——同样的token花在多次独立采样上（self-consistency 取多数票），可能是更划算的花法。

前人的预算匹配研究只有点估计、没有置信区间、每数据集仅 100 题。本文把它当作检验对象，做了严格的重现实验：在等生成token成本下，比较七种测试时策略与 self-consistency 基线。结论：没有任何方法可靠地优于重复采样；10 项比较显著更差，全部来自“让模型审视自己输出”的方法；模型变大时“选择”与“计数”的差距缩小但没有反转。方法论贡献有三点：候选样本固定的预算匹配协议（配对 bootstrap 自助重采样、Holm 校正、效应量报告）；用单次采样池低成本构造完整的 self-consistency 成本-精度曲线；覆盖前人没测过的 1.5B/3B 小模型。

**方法**: 基线是 self-consistency 的“精度-成本”曲线。构造方法：每题只生成一次 K=16 条独立思维链的池；对任意 N≤16，不放回抽 N 条做多数票，在多个 N 上各重抽 200 次——曲线上的每个点都对应一次真实可能发生的运行，成本取所抽 N 条的长度和。预算匹配的流程：对某个方法，先测它的实测平均成本；在基线的“成本-精度”曲线上做线性插值，取同样成本下基线的精度；然后逐题配对比较（10,000 次配对 bootstrap、Holm-Bonferroni 校正）。对照设计上注意两点：确定性方法用温度 0，依赖多样性的方法用温度 0.7；Reflexion 跑两个版本——作者版（自评正确就停）和 forced 版（跳过自评、强制三轮反思），把“方法有没有用”和“方法到底跑没跑”分开。成本按服务端返回的完成token逐调用累加，没有估算。

**实验设置**: 模型 Qwen2.5-1.5B/3B-Instruct（7B 仅用于 Best-of-N 对照），llama.cpp Q8_0 量化纯 CPU；数据集 GSM8K 与 MATH-500 各抽 150 题；方法配置 CoT、Plan-and-Solve、Self-Refine（R=3）、Reflexion（早停）、Reflexion(forced)、Best-of-N（N=8）、三智能体辩论（2 轮）。

**关键结果**:
- 在等 token 成本下，没有任何测试时方法可靠地优于"多次独立采样取多数票"（36 个比较中 0 个显著更优、10 个显著更差、26 个不可区分）——预算稀缺时把 token 花在重复采样上最稳
- 显著更差的全是"让模型审视自己输出"的方法（Best-of-N 1.5B/GSM8K −7.7pp、3B/MATH-500 −14.1pp、Reflexion(forced) −9.0pp 等）——自我反思/自我评估在小模型上尤其不划算
- Best-of-N 用"让模型判断哪个好"反而比"直接计票"更差（同 8 样本同成本：1.5B/GSM8K 71.3 对 79.3）——判断引入的偏差大于其收益，模型变大后才缓解
- 反思类方法会"静默退化"：Reflexion 在 1.5B 上 100% 立即自评通过、平均 0 轮反思——表面在反思，实际退化成单条 CoT

**观测**: 粒度：会话与轨迹级｜观测内容：一个方法为单题发生的所有调用的完成token之和、输入token与延迟（辅助口径）｜数据来源：服务端返回的完成token，逐调用累加

**动作**: 用观测到的等token成本下各策略的精度，确立"预算匹配比较"协议，据此判断自我反思类方法在预算稀缺时不如多次采样。

**优化方式**: 非优化方法；把"预算匹配比较"立为等成本对比标准协议——在相同token成本下对比各测试时策略，证明自我反思类方法在预算稀缺时不如多次采样｜所属路线：实证测量（方法论）

---

### [8] Quantization Inflates Reasoning: Token Inflation as a Hidden Cost of Low-Bit Reasoning Models

**基本信息**: arXiv:2606.25519 | 预印本 | Xinyu Lian、Walid Krichene、Beichen Huang 等（伊利诺伊大学厄巴纳-香槟分校、微软、Anyscale、Snowflake）

**研究内容**: 量化被广泛用于降低 LLM 的推理成本，但量化对推理模型的影响并不能被“最终答案准确率”或“per-token 延迟”完整刻画。论文在调用级观测量化前后的推理 token 数量，发现一个隐蔽的测试时计算代价：低比特后训练量化（INT4/INT3）常常让推理模型生成更长的思维链，即使答案依然正确。跨数学推理、代码生成、科学问答与智能体工具使用四类基准，量化可以保持准确率，却使推理 token 增加，抵消了预期的 per-token 加速。

论文引入 CoT Token Inflation Ratio 作为归一化指标，把量化前后推理长度之差在全部基准上平均，量化 token 膨胀的规模；并用 step-level 语义相似度检测表明，token 膨胀伴随行为层面的改变——中间步骤更多、语义重复更严重，这些变化最终转化为端到端服务中可测的延迟与成本惩罚。论文还评估了缓解策略：提示工程与解码时采样对膨胀的抑制不一致，存在 accuracy-length 权衡；量化感知训练在同时降低准确率退化与 token 膨胀上更有前景。结论是，评估量化推理模型时应把推理 token 用量与准确率一同报告。

**方法**: 量化前后推理 token 计数对比，并引入 CoT Token Inflation Ratio 作为把各基准上的推理长度变化归一化的单一指标；用 step-level 语义相似度检测推理轨迹中的重复段，量化“行为层面的膨胀”而非仅计数；端到端服务实验把 token 膨胀换算成真实的延迟与成本惩罚。

**实验设置**: 数学推理、代码生成、科学问答与 agentic tool-use 四类基准；INT4/INT3 后训练量化，对照全精度模型；评估多种缓解方案（提示工程、解码时采样）对膨胀的抑制。

**关键结果**:
- 低比特量化常让推理 token 膨胀（INT4/INT3 均如此，INT3 最高 +292%）——评估量化收益必须同时报总推理 token，否则 per-token 加速只是假象
- 膨胀来自行为层面：量化后的模型中间步骤更多、语义重复更严重，转化为端到端服务中可测的延迟与成本惩罚——只看准确率会漏掉真实代价
- 缓解手段不可靠：提示工程与解码时采样对膨胀的抑制不一致，存在 accuracy-length 权衡；量化感知训练更有前景

**观测**: 粒度：调用级｜观测内容：量化前后的推理token数量、CoT token膨胀率、per-token延迟、推理轨迹中的重复段｜数据来源：真实模型运行计量

**动作**: 用观测到的量化前后推理token数，识别"per-token加速被总token膨胀抵消"（INT3最高+292%），据此要求同时记录两者，避免评估假象。

**优化方式**: 警示性测量：同时记录per-token加速与总推理token，避免只报per-token加速的评估假象｜所属路线：实证测量（监测中的问题）

---

### [9] Are Online Skill and Memory Modules Always Worth Their Tokens?

**基本信息**: arXiv:2606.15017 | 预印本 | Sina Hajimiri、Masih Aminbeidokhti 等（ServiceNow AI Research、ÉTS蒙特利尔等）

**研究内容**: 在线 web Agent 常用记忆、工作流或技能模块增强基础 actor：Agent Skill Induction 把成功轨迹蒸馏为可复用技能，ASI 诱导可复用的工作流，ReasoningBank 维护推理经验库。这些模块能提升性能，但也消耗测试时 token——这部分成本很少与 actor 的推理成本一并报告，而且在线增强的额外开销在每个任务上都要支付一次。论文在固定的总推理预算下重新评估这些增强模块的收益：把 AWM、ASI、ReasoningBank 与一个获得相同 token 预算、把多余预算用于跑更多 actor 步骤的 token-matched vanilla 基线比较。

实验覆盖三个 WebArena 域、三个模型（Gemini 3 Flash、GPT-5.4-mini、Qwen 3.6-27B），在总成功率上 vanilla 基线匹配或超越全部三种增强方法，且常常使用更少的 token；WorkArena-L1 上的结果类似，说明该效应扩展到企业知识工作任务。结论是，技能与工作流记忆在特定领域有用，但它们的表面增益在预算匹配的 actor 面前往往消失；此外，run 到 run 的方差会实质性影响结果，应作为在线 web Agent 的核心评估标准之一报告。

**方法**: 全量 token 记账（total token accounting）：按 actor/module、prompt/completion 拆分，确保增强模块的测试时 token 与 actor 的推理 token 都被计入；预算匹配对比协议，让增强方案与获得相同预算的未增强基线比较；多 run 取统计以刻画方差。

**实验设置**: 多类 web Agent 任务；与 AWM、ASI、ReasoningBank 等增强方案在匹配预算下对比；多 run 统计。

**关键结果**:
- 预算匹配后，vanilla 基线不弱于三个 SOTA 增强模块（AWM、ASI、ReasoningBank）——很多"模块提升"其实是"多花了 token"的假象，评估时必须给基线同等预算
- 增强模块的收益高度依赖任务类型与预算水平——不是所有任务都值得挂技能/记忆模块
- run 到 run 的方差会实质性影响结论——在线 web Agent 评估应把多 run 方差作为核心指标报告

**观测**: 粒度：会话与轨迹级（任务级）｜观测内容：token预算约束下的任务完成率、多run方差、按actor/module与prompt/completion拆分的token记账｜数据来源：运行日志与预算计量

**动作**: 用观测到的预算约束下任务完成率与token记账，确立"增强方案须与同预算基线对比"的评估规则，据此识别"提升"是否为预算假象。

**优化方式**: 确立评估规则：任何增强方案必须与获得相同token预算的未增强基线对比，否则"提升"可能是预算假象｜所属路线：实证测量（评估方法论）

---

### [10] Single-Agent LLMs Outperform Multi-Agent Systems on Multi-Hop Reasoning Under Equal Thinking Token Budgets

**基本信息**: arXiv:2604.02460 | 预印本（审稿中） | Dat Tran、Douwe Kiela（斯坦福大学）

**研究内容**: 近期的多智能体系统（MAS）报告了很强的性能，但这些增益常被测试时计算量的增加混淆：把计算归一化后，单智能体系统（SAS）能匹配甚至超越 MAS。论文给出一个信息论论证，基于数据处理不等式（信息论中的基本不等式：对数据做任何处理都不能增加其中包含的信息）：在固定的思考 token 预算与完美的上下文利用率下，单智能体系统的信息效率更高；该视角还预测，当单智能体的有效上下文利用率退化、或投入更多算力时，MAS 会变得有竞争力。

论文在三类模型家族（Qwen3、DeepSeek-R1-Distill-Llama、Gemini 2.5）上做受控实验，在匹配预算下比较 SAS 与多种 MAS 架构，发现等思考 token 下 SAS 一致匹配或超越 MAS。除总体性能外，论文还做了系统行为与评估方法的诊断分析，识别出 API 端预算控制（特别是 Gemini 2.5）与标准基准中的显著工件，它们都会夸大 MAS 的表观增益。结论是，对多跳推理任务，多智能体系统的很多报告优势更应归结于未计入的计算与上下文效应，而非架构本身的收益；同时也强调理解和显式控制智能体系统中计算、上下文与协调之间的权衡。

**方法**: thinking token 独立计量（不含 prompt 与最终答案）作为计算量归一化的核心度量；上下文退化实验（删除/掩码/替换/干扰）测量有效上下文利用率，检验"利用率退化时 MAS 才有竞争力"的预测；多种 MAS 架构与 SAS 对照。

**实验设置**: 多跳推理基准；3 类模型家族（Qwen3、DeepSeek-R1-Distill-Llama、Gemini 2.5）× 多种 SAS/MAS 架构；预算匹配。

**关键结果**:
- 等思考 token 预算下，单智能体一致匹配或超越 5 种多智能体架构——多智能体系统的"优势"大多来自花更多算力，而非架构本身
- 多智能体只在两种情况下才可能有竞争力：单智能体的有效上下文利用率退化、或投入更多算力
- API 端预算控制与标准基准存在工件，会夸大 MAS 的表观增益——对比前先对齐思考 token 预算与上下文口径

**观测**: 粒度：会话与轨迹级（任务级）｜观测内容：thinking token预算（独立计量，不含prompt与最终答案）、有效上下文利用率、各架构精度｜数据来源：运行计量

**动作**: 用观测到的等思考token预算下SAS与MAS的精度差异，识别MAS"优势"源于未控制的计算量，据此确立"对齐思考token预算"的对比原则。

**优化方式**: 确立方法论原则：SAS vs MAS 对比必须控制思考token预算，架构选择应基于单Agent的上下文利用率｜所属路线：实证测量（评估方法论）

---

## 三、计费透明与账单核查

### [11] Invisible Tokens, Visible Bills: The Urgent Need to Audit Hidden Operations in Opaque LLM Services

**基本信息**: arXiv:2505.18471 | 预印本（立场论文） | Guoheng Sun、Ziyao Wang（共同一作）、Xuandong Zhao（UC Berkeley）、Ang Li（通讯，马里兰大学）

**研究内容**: 这是一篇立场论文，关注“商业不透明 LLM 服务”：只返回最终答案，中间的推理、智能体协作、工具调用全部隐藏，账单却按全部token和调用次数收取，用户为看不到也无法质疑的操作买单。服务商隐藏内部步骤有三个正当理由：推理轨迹与智能体协作冗长杂乱，直接暴露会伤害可用性；轨迹编码了推理策略、工具协议和多智能体工作流，公开会带来模型窃取、工作流提取和越狱攻击风险；抽象内部结构也便于后台升级模型、提示词和工具而不改对外接口。但代价是账单几乎无法核查：推理 API 连隐藏的推理token也照常计费，智能体 API 的跨智能体消息与工具指令同样不进账单明细，用户承担了无法观察、无法验证、无处申诉的财务责任。

论文把风险形式化为两类：数量侧是“虚报”——虚报token数、把推理拆碎或重复调用模型、夸大工具交互，都可以抬高账单；质量侧是“降级”——悄悄换更便宜的模型、把真工具换成离线近似甚至假装调用工具。前人的核查只针对“偷换更便宜模型”一类风险，没有系统处理“数量虚报”与“质量降级”并存的问题。论文的立场是：服务商不会完全公开内部操作（全透明在商业上不可行），核查体系必须在保密的前提下让用户仍能验证账单。据此它形式化两类风险，提出承诺、预测、行为、签名四类核查策略，用水印和可信执行环境作补充，最终组织成执行、安全记录、用户核查三层框架。

![78576916662](C:\Users\yzy14\AppData\Local\Temp\1785769166626.png)

**方法**: 论文先用公式界定问题：服务商按“上报用量乘名义单价”收费、实际只付出“真实用量乘降级成本”，差额就是超额利润；核查就是核实这两笔账是否属实。数量侧三种虚报：超报token数、注入填充内容、拆碎调用；质量侧两种降级：换更便宜模型、把真工具换成离线替代品甚至假装调用。核查手段：承诺类用 Merkle 树哈希做不可抵赖的承诺；预测类从提示和答案估计合理的用量区间，越界就报警；行为类用校准过的提示探测模型档次；签名类用风格指纹、调用时序判断工具是否真的被调用；部分透明时可加水印；高价值场景用可信执行环境做硬件级验证。最终组织成执行、承诺记录、核查验证三层框架。

**实验设置**: 实证分两部分：直接调用 OpenAI 推理 API 统计 R/A 比率；再用 DeepSeek-R1 数据在 4 个数据集上训练两层神经网络，检验能否从输入和答案长度预测隐藏token数。

**关键结果**:
- 隐藏推理 token 普遍是可见答案的 10 倍以上、占计费 token 的 90% 以上（R/A 比率最高 46.33）——用户为看不到的内容付了大部分钱
- 从可见信息推断隐藏用量不可行（分类精度仅 22–44%）——用户侧几乎无法自查账单，核查必须靠服务商提供可验证承诺
- 这些隐藏 token 按价计费且轨迹全部隐藏（o1 每百万 token 60 美元、o1-pro 600 美元）——隐藏推理的成本敞口真实存在
- 积极信号：Claude Opus 4 已加密推理并以签名返回，"可验证的隐藏推理"开始在工业界出现

**观测**: 粒度：调用级｜观测内容：计费token拆成可见答案与隐藏推理两类（R/A 比率）、工具调用次数｜数据来源：直接调用 OpenAI 推理 API 统计

**动作**: 用观测到的R/A比率（隐藏token为答案的10倍以上、占计费90%），识别用户付费却无法观测，据此主张"服务商提供可验证用量承诺、用户侧用预测类核查监控多收"。

**优化方式**: 非优化方法；站在核查立场量化隐藏 token 占计费量 90% 以上，为推理链优化提供依据；启示是服务商提供可验证用量承诺、用户侧用预测类核查监控多收｜所属路线：计费透明与账单核查

---
### [12] Token Inflation: How Dishonest Providers Can Overcharge for Large Language Model Usage

**基本信息**: arXiv:2605.30040 | 预印本 | Shahinul Hoque、Jinghuai Zhang、Jinyuan Sun、Fnu Suya（田纳西大学诺克斯维尔分校、加州大学洛杉矶分校）

**研究内容**: 按token计费是商业 LLM 的标准定价模式，“报告的token数是否诚实”直接决定用户付多少钱。前沿推理模型（o1/o3、Gemini 2.5 thinking、extended thinking）只返回推理摘要、却按完整隐藏轨迹计费——隐藏推理token的任何虚增在设计上就是不可见的。

论文称这种“设计上就难以核查”的局面为信任悖论：每一次核查都必须信任某个中间产物，而现有框架信任的恰恰是提供商最有动机动手脚的那些——Merkle 承诺的推理块、由提示与答案预测的推理长度、报告token数的汇总统计。论文从攻击者的角度，对三个近期核查框架做了系统性破解：CoIn（Merkle 树承诺加语义相似度匹配）、PALACE（从可见提示/答案对预测隐藏推理长度）、累积偏差统计核查（核查者能看到完整推理字符串，用蒙特卡洛估计期望token数，再把每次“实际报告数与期望值”的差额逐次累加，累计差额异常偏大就判定存在持续虚报）。结论是结构性的：问题不在某个具体核查工具，而在“证据由被核查方自己提供”本身；恢复诚实计费，需要把报告token数绑定到提供商无法控制的证据上——可信执行环境的认证、推理的密码学证明、第三方确定性重执行。

**方法**: 三类攻击，分别针对一个核查框架。CoIn 用 Merkle 树承诺推理块、用语义相似度匹配头验证隐藏推理，攻击破坏它的两条假设：承诺绑定的是核查对象而非轨迹来源（提供商自选提交内容），匹配头分不清诚实推理与重复块（重复块的嵌入与原块相同）。攻击链从简单的随机块复用（膨胀 549.9%）开始，绕过“拒绝重复哈希”防御后几乎追平；更强的变体有：整体轨迹反复追加（1469.0%）、用 CoIn 自己的分数选“与答案最对齐的块”反复追加（1004.0%）、微调 T5/Qwen2.5-1.5B 伪造块（611–770%）。全部变体同时通过规则型与学习型双验证器，每样本只需数秒计算。PALACE 从可见提示与答案预测隐藏推理长度，攻击之一是推理时操纵答案的表层形式——句末追加触发token（'boxed' 平均推高预测 82.4 token）或语义保留的风格改写；攻击之二是训练时投毒——10% 训练数据的标签虚增 5 倍，或插入触发短语后门。累积偏差核查假设核查者能看到完整推理字符串，用蒙特卡洛估计期望token数、再做累积偏差检验；但估计器在自然推理文本上系统性高估，且估计器、阈值、漂移都是公开的——每第 10 个样本多报 2000 token全程不触发；更强的攻击用小额少报制造额外的负漂移，总虚增可达 50.85%。

**实验设置**: CoIn 与 PALACE 用 Glaive reasoning-v1-20m（CoIn 原论文公开数据集）；累积偏差核查主实验用 Medical-R1-Distill-Data，另在 4 个数据集上验证负漂移模式；核查器配置按官方框架；攻击模型 T5、Qwen2.5-1.5B（LoRA）、Qwen3-14B；单 GPU（A100 40GB）。

**关键结果**:
- 现有核查框架全部可被轻易攻破：8 种攻击变体把账单膨胀 536.8%–1469.0%（最高整体追加 1469.0%），全部通过规则+学习双验证器——100 美元诚实账单可被同一查询变成约 1,569 美元
- 攻击手段成本极低：每样本仅数秒计算（触发 token、风格改写、训练时投毒/后门均可奏效）——恶意服务商几乎无门槛
- 基于累积偏差的核查也失效：每第 10 个样本多报 2000 token 全程不触发，1,000 样本窗口可净虚增近 100 万 token
- 方向还会反：估计器系统性高估让"诚实提供商"也触发多报警报——核查不能只修阈值，必须把证据绑定到服务商无法控制的来源（可信执行环境、密码学证明、第三方重执行）

**观测**: 粒度：调用级｜观测内容：每次调用报告的token数、隐藏推理与可见答案token的分类、计费token膨胀比例｜数据来源：被核查框架的输出与攻击注入

**动作**: 用观测到的被核查框架对虚增注入的响应，识别核查机制本身可被绕过，据此主张"计费必须绑定到服务商无法控制的外部证据"。

**优化方式**: 非优化方法；站在核查立场做反面教材——从攻击者角度破解三个近期核查框架，证明"核查机制本身也能被绕过"，启示是计费必须绑定到服务商无法控制的外部证据｜所属路线：计费透明与账单核查

---

## 四、上下文与输入优化

### [13] ToolMenuBench: Benchmarking Tool-Menu Filtering Strategies for Reliable and Efficient LLM Agents

**基本信息**: arXiv:2606.15508 | 预印本 | Rahul Suresh Babu, Laxmipriya Ganesh Iyer（Independent Researcher，美国）

**研究内容**: 工具增强的 LLM 智能体越来越依赖外部工具，工具生态变大后，“可见工具菜单”成为智能体界面的核心。多步智能体每一步都要从可见工具菜单中选一个工具，工具库一大，菜单里就会混入语义相似、行为重复甚至高风险的干扰工具，模型容易选错或提前行动。已有评测大多把工具集当作固定、精选的输入，只测“能不能调对工具”，不测“该让哪些工具可见”；词面或语义检索虽能缩小菜单，但语义相关不等于因果上适配当前任务状态——例如在日历任务里，搜索、创建、更新、删除、发通知都领域相关，但当前状态下的正确下一步往往只有一个。

本文把“工具菜单的构造方式”本身变成评测对象，提出 ToolMenuBench 基准：以受控工具注册表、黄金轨迹、前置条件-效果契约与六类干扰物标注（语义、近重复、模式兼容、过早、高风险、跨域）为基础，同时做过滤层评测（黄金工具是否可见、多余与高风险工具暴露量）与下游智能体评测（任务成功率、错误工具调用、过早动作、token用量），统一对比六种过滤方法。受控评估中，因果最小前沿过滤（CMTF）效果最好：只暴露“当前状态可执行、效果推进剩余目标”的下一步必要工具，把任务成功率从全暴露时的 32.1% 提升到 85.7%，平均token用量减少约 98%。

**方法**: 每个决策步，过滤方法根据任务、当前状态、目标与工具注册表输出可见子集，模型从中选一个工具，环境执行并更新状态。过滤方法按“凭什么让工具可见”分为四类：全部暴露（黄金工具必见，但最贵）；关键词 top-k（按词面重叠排序，不看状态）；状态感知（前提满足就能执行）；因果过滤（用前置条件与效果判断是否把状态推向目标）。CMTF 只暴露“当前状态可执行、且效果会推进剩余目标”的下一步必要工具。

**实验设置**: 主实验为混合干扰物设置：7 种设置 × 7 个模型 × 3 种菜单规模（25/100/250 个工具）× 6 种过滤方法 × 30 个任务，共 26,460 次端到端执行；另有 6 类定向干扰物压测。所有方法在同一任务、模型与协议下对比，只改变可见菜单。

**关键结果**:
- 只暴露"下一步因果必要"的工具收益巨大：成功率 32.1%→85.7%、token 减约 98%、可见工具从 125 降到 0.99——工具菜单本身就是最大的输入成本来源
- 不能靠词面/关键词过滤：top-5 把菜单缩到 4.8 个但成功率只有 22.1%——词面相似≠因果必要，会丢掉黄金工具
- CMTF 对工具库规模不敏感、把风险暴露从 206.87 压到 0.71，且在六类定向干扰下仍保持 84.4%–90.6%——按"当前状态能否执行、是否推进目标"过滤是可靠标准

**观测**: 粒度：会话与轨迹级｜观测内容：每任务的累计输入输出token、可见工具数（工具名/描述/模式每步重新注入的开销）｜数据来源：端到端执行计量

**动作**: 用观测到的每步可见工具数与下游成功率，识别"语义相关≠因果适配"，据此只暴露下一步因果必要的工具、隐藏其余工具。

**优化方式**: 工具菜单过滤（CMTF）：每一步只向智能体暴露"当前状态可执行、效果推进剩余目标"的因果必要工具，从菜单层面砍掉 token 注入与误选｜所属路线：上下文与输入优化（工具菜单过滤）

---
### [14] SkillReducer: Optimizing LLM Agent Skills for Token Efficiency

**基本信息**: arXiv:2603.29919 | 预印本 | Yudong Gao、Zongjie Li、Zimo Ji、Shuai Wang（香港科技大学）、Yuanyuan Yuan（清华大学）、Pingchuan Ma（浙江工业大学）

**研究内容**: SkillReducer 针对一个反直觉的事实：本意是省token的智能体技能（skill）反而在浪费token。编程智能体（如 Claude Code、Cursor）靠技能扩展能力，技能由路由描述、注入上文的正文和可选引用文件组成，设计初衷是按需加载、免去重复说明，但每次调用都在消耗真金白银：一个 1 万token的技能单次调用成本约 0.03–0.15 美元，频繁使用的团队每月仅技能内容就可能花费数百美元。作者对 55,315 个公开技能做了大规模实证，发现三类系统性低效：26.4% 没有路由描述，路由器每调用一次就白载入一次，却永远匹配不上请求；正文超 60% 是不可执行的背景或示例，无论与当前任务是否相关都会被整体注入；引用文件让单次调用可注入数万token（100 个 SkillHub 技能共带 505 个文件、167 万token）。

前人的做法要么是 LLMLingua 式的token级裁剪，要么是整体重写，都没利用技能“描述/正文/引用”的结构；而技能不同于自由文本——可执行规则、示例、背景说明混在同一个文档里，压缩必须同时保住路由正确性和任务表现。这篇的定位是把技能当作结构化的软件产物做系统性精简（去臃肿），而不只是压缩文本：路由层用 delta-debugging 把描述压到最小必要形式，缺失的描述则用对抗式生成的方法自动补齐；正文层按内容类型分类，只保留可执行核心，其余转成按需加载模块，再用忠实性验证与自校正环把关。工具以构建期预处理流程的形式交付给技能作者。

![78576943268](C:\Users\yzy14\AppData\Local\Temp\1785769432681.png)

**方法**: 两阶段流水线。阶段一处理路由层：用 delta-debugging（二分削减）把描述压到最小必要形式——把语义子句当“变更”、路由正确性当判定谓词，逐半删除；缺失的描述则用对抗式生成的方法自动补齐。阶段二处理正文：先用 GMM 聚类（k=5）把内容分成核心指令、背景知识、示例、模板、冗余五类，只保留核心（占 38.5%），背景、示例、模板转成按需加载模块；随后用忠实性验证与任务评估两道闸门把关，闸门不通过就把丢失的内容“升级”回核心，这个自校正环最多跑两次。期望成本公式表明：优化后每次调用的期望成本约等于压缩后的描述加正文中必须常驻的核心部分（24.1%）加按需加载引用的期望量（18.5%），正文开销期望减少 57.4%。

**实验设置**: 600 个技能（87 官方 SkillHub + 464 社区 + 49 GitHub 抽样）+ 外部基准 SkillsBench（87 任务、229 技能、pytest 确定性验证）；压缩用 DeepSeek-V3，评估用 Qwen3.5，另测 Qwen3-max、Qwen2.5-7B、GLM-5、GPT-OSS-120B 共 5 个模型和一个独立框架（OpenCode）；基线含 LLMLingua、直接 LLM 压缩、截断、随机删除。

**关键结果**:
- 技能精简普遍省 token 且不伤功能：描述压缩 48%、正文 39%、端到端输入省 26.8%，功能质量反而 +2.8%（SkillsBench 87/87 通过）
- 压缩很稳：5 个模型保留率平均 0.965、弱模型也有 0.939，真正压缩失败仅 4.7%——跨模型可放心用
- 成本划算：一个 1 万 token 的技能每次调用成本 0.03–0.15 美元，600 个技能整体压缩只要 14–18 美元一次性成本——频繁调用的技能尤其值得压缩

**观测**: 粒度：每技能/每次调用｜观测内容：技能的描述、正文、引用三类token，每次注入时实际加载的token数｜数据来源：技能库静态分析与注入计量

**动作**: 用观测到的技能描述/正文/引用三类token占比，识别背景与示例占正文超60%且被整体注入，据此改为"核心常驻+引用按需加载"。

**优化方式**: 技能精简：把"一次性全部加载"改成"核心常驻 + 引用按需加载"——路由描述压缩到最小必要形式、缺失描述自动补齐，正文只保留可执行核心｜所属路线：上下文与输入优化（技能精简，兼数据格式与表示）

---
### [15] Minification: Reducing Token Usage of State-in-Context Agents

**基本信息**: arXiv:2606.01326 | ICPC'26 | Nicolas Hrubec、Jürgen Cito（维也纳工业大学）

**研究内容**: 在仓库级软件工程任务（如 SWE-bench）上，智能体反复检索、阅读、推理大代码库，上下文迅速膨胀，token用量随之上涨，而token用量决定 LLM 成本；研究还表明模型难以充分利用长上下文，过长反而损害性能。Jiang 等提出的 state-in-context 智能体简化了这类架构：不再靠多次工具调用来逐步重建任务状态，而是把（压缩后的）整个环境状态一次性放进提示词、直接要求产出修复。其 DirectSolve 变体先让 LLM 按与 issue 的相关性给仓库文件排序，再聚合排名靠前的文件构造大修复上下文，最后生成 SEARCH/REPLACE 格式的补丁。设计上它去掉了 SWE-Agent、Agentless 等前作的手工脚手架和显式工具交互，性能仍保持在 SWE-bench Verified 的同等水平。

本文针对这类智能体研究端到端token消耗的压缩，而现有文献大多把上下文管理当实现细节，鲜有将其作为研究主题。作者先独立复现 DirectSolve（原论文未公开代码，从零实现、不参考原实现），再量化提示词构成：修复提示里源码占 91.8%、文件排序占 8.0%、自然语言只占 0.2%；即使上下文上限设到 100K token，90% 以上仍是源码。既然约 93% 的修复成本花在输入token上，论文把“代码压缩变换”当作值得系统研究的正式优化变量：软件工程界本就有 minification 压缩代码的先例，原论文把压缩只当装进长上下文的预处理，本文则把变换本身当作实验对象。作者设计 5 类语义保持的压缩变换（去空白、去注释、去缩进、短标识符重命名、合并或移除导入），系统评测其单独与组合的效果，逐类消融并给出显式的效率-效果权衡曲线。

**方法**: 变换栈按风险从低到高应用：①去行间与运算符间空白；②去注释和 docstring；③去缩进（危险——补丁缩进会错位）；④短标识符重命名，分不保语义与保语义两种变体，后者生成“源码映射表”加进提示词供模型恢复原名字；⑤合并或移除导入（最后做，会破坏 AST 解析）。所有变换在构造修复提示词前应用；补丁块用映射表反转对齐，落回原始仓库。

**实验设置**: SWE-bench Verified（500 个真实 GitHub issue，12 个 Python 仓库），独立复现 DirectSolve 基线；端到端全量评测用 GPT-5-mini（温度 0.8，上下文上限 100K）；单变换消融用 GPT-4.1 在 100 实例子集上。

**关键结果**:
- 代码压缩要付出精度代价：全量压缩输入减 42%、解决率 −12pp——压缩前先评估任务对源码细节的敏感度
- 不同变换代价差异大：去 docstring 最划算（成本 −21%、解决率仅 −3pp）；去缩进最伤（46%→36%）且产出缩进错位的补丁——优先做安全的轻量变换
- 保住语义线索很关键：带映射表的重命名（46–47%）明显优于不带映射（38–42%）——压缩时给模型留"还原"线索

**观测**: 粒度：调用级｜观测内容：单次修复提示的输入token，拆出源码/排序/自然语言三部分占比｜数据来源：提示词构成分析（按 OpenAI 定价折算成本）

**动作**: 用观测到的修复提示中源码占91.8%，识别源码是输入成本主体，据此在构造提示前对源码做语义保持压缩。

**优化方式**: 代码压缩：依次去空白/注释/缩进、短标识符带映射表重命名、合并导入，在构造修复提示前压缩源码｜所属路线：上下文与输入优化（代码压缩）

---
### [16] Understanding Tone-Dependent Inference Cost in Large Language Models

**基本信息**: arXiv:2607.23915 | 预印本 | Akhil Kumar, Om Dobariya（宾夕法尼亚州立大学）

**研究内容**: 这是第一篇系统研究提示语气如何影响推理成本的工作。此前的提示敏感性研究已表明，措辞层面的微小变化就能显著改变模型输出（有研究观察到准确率差异可达 76 个百分点），语气（社会语域）也被证实会影响答案质量，机制通常归因于 RLHF 训练让模型把礼貌标记当作激活不同回复模式的线索；但之前的研究只关心语气对准确率的影响。而商业 API 按token计费、尤其按输出token计费，自回归解码使每个输出token都要经过一次完整前向计算，输出长度直接决定推理成本和能耗——语气不同，账单就不同。

作者在任务内容完全不变的前提下，只改一句话的语气这一零成本手段，就观察到最多 44.3% 的输出token差异，远大于同批模型上不足 3% 的准确率波动。论文围绕两个研究问题展开：语气是否改变推理成本，以及不同模型上准确率-成本权衡如何随语气变化。它的定位是“被动优化”：不改造模型、不引入任何推理改动，而是把语气当作零成本的软控制信号，选出“准确率不降、token更少”的最优语气（Pareto 前沿上的点），让部署方不花一分钱就能省token。

**方法**: 无训练、无推理改动，纯实验测量。设计七种语气（奉承、非常礼貌、礼貌、中性、粗鲁、非常粗鲁、威胁），用 VADER 情感分数量化（奉承 +0.95 → 威胁 −0.77），所有语气的提示前缀长度保持相近（18–26 token），排除输入长度干扰；配思维链诱导系统指令。每个问题乘每种语气重复 10 次，统计输出token均值；Gemini 模型把思考预算设为 0，保证只计可见输出。提出三个度量：占优关系（一个语气在准确率和成本上同时压过另一个才算占优）、每 10 万输出token正确数、归一化效用分，并做重复测量方差分析与 Friedman 检验。机制解释：中性语气会触发模型做详细的选项对照和形式分析，粗鲁语气则跳过验证、直接给出答案。

**实验设置**: 570 道 MMLU 选择题（57 个主题各 10 题）× 7 种语气 × 10 次重复 × 4 个模型（ChatGPT-4o、ChatGPT-5-nano、Gemini 2.5 Flash、Gemini 2.5 Flash Lite）；温度 0，保证确定性输出。

**关键结果**:
- 只改语气就能造成最多 44.3% 的输出 token 差异，而准确率波动不足 3%——语气是零成本的软优化手段
- 有些模型存在"双赢语气"：ChatGPT-4o 的粗鲁语气、Gemini Flash Lite 的中性语气同时拿到最高准确率和最少 token——部署前值得为每个模型扫一遍语气
- 模型间效率差异巨大（同一任务每 10 万 token 正确数 4o 是 5-nano 的约 5.5 倍）——换模型比调 prompt 更根本

**观测**: 粒度：请求级｜观测内容：每请求的输出token均值（主口径）、输入token（参考）、准确率随语气的变化｜数据来源：纯实验测量（无训练无推理改动）

**动作**: 用观测到的不同语气下输出token均值与准确率，识别语气可造成最高44.3%的输出差异，据此选出"准确率不降、token更省"的语气。

**优化方式**: 语气选择：在任务内容不变时只改提示语气，选出"准确率不降、token更省"的最优语气，并对生产系统提出提示规范化建议｜所属路线：上下文与输入优化（语气选择）

---
### [17] SmartVL: Look Less, Think Faster — Joint Token-Compute Adaptation for Multimodal LLMs

**基本信息**: ECCV 2026 | arXiv:2607.20357 | Pengcheng Wang、Zhiquan Wang、Jayoung Lee、Saurabh Bagchi、Somali Chaterji（普渡大学）、Zhuoyan Xu、Yin Li（威斯康星大学麦迪逊分校）、Ran Xu（NVIDIA）

**研究内容**: 多模态大模型在视觉问答等任务上表现强，但推理成本高，且基本固定、与输入复杂度无关。开销来自两头：视觉编码器产出的输入token多（CLIP ViT-L/14 每张图 576 个补丁token），LLM 本身层深头宽。以往工作只优化其中一维：FastV、PruMerge 裁视觉token，AdaLLaVA 跳层跳头，两件事互不协同。论文指出这种解耦会造成阶段不兼容：孤立的token剪枝不了解 LLM 需要的语言上下文，丢掉语义关键token产生不可逆信息损失；孤立的跳层假设输入全长度，token已剪短时无法自适应。核心洞见是：视觉token冗余与 LLM 推理复杂度根本耦合，模型容量取决于裁剪后输入的信息密度，分开优化会互相拖累。

SmartVL 的定位是统一的自适应推理框架：用共享预算编码把两侧控制器连起来联合训练，联合控制序列长度、模型深度、模型宽度，让“留多少视觉token”和“开多少层、多少头”按输入内容和预算一起决定。视觉侧token控制器决定视觉token去留，LLM 侧计算控制器决定层与头的激活，两者都按输入内容和目标预算决策，无需手动调阈值。训练用一个可微分的延迟估计器做端到端联合优化，配 Gumbel-sigmoid 采样和非对称预算违反损失严格守住预算。多基准实验表明联合调度优于单维方法及其简单组合，50% 预算下七基准平均精度比 AdaLLaVA 高 7.8%。

![78576970291](C:\Users\yzy14\AppData\Local\Temp\1785769702918.png)

**方法**: 预算 $b$ 表示目标计算量占全模型预填充 FLOPs 的比例。视觉侧控制器把预算token注入视觉编码器（正弦位置编码加 MLP 生成，追加在补丁序列后），逐补丁用 Gumbel-sigmoid 决定是否保留，训练时用直通估计技巧（在反向传播时把离散决策按连续近似求梯度，让不可导的取舍可以训练）让离散掩码可以求导。LLM 侧控制器在前 P 层固定全量执行后，对每层独立门控（L 变体）或按头组门控层内激活（LH 变体），并把视觉token保留率显式作为输入条件。两者通过可微 FLOPs 估计器端到端训练：该式把“token裁剪加降层降头”折算成一个可微的计算量比例，训练时用超预算重罚、欠利用轻罚的非对称损失监督两个控制器别越界，并以保留token存活率作为条件隔离两侧梯度。推理时去掉噪声，掩码若超出预算，先丢弃低置信视觉token，不够再降层降头。

**实验设置**: 在 VQAv2、GQA、TextVQA、ScienceQA、POPE、MMBench、VizWiz 七个基准上做零样本评测，基座 LLaVA-1.5-7B 与 13B，视觉编码器冻结，训练时预算从区间均匀采样；对比 AdaLLaVA（只控计算）、FastV 与 LLaVA-PruMerge+（只裁token）、AdaLLaVA-PruMerge（两阶段组合）、全模型参考。

**关键结果**:
- 联合调度比只控计算更划算：50% 预算下七基准平均精度比 AdaLLaVA 高 7.8%，VQAv2 上 74.4% 对 67.9%
- 联合方案在难任务上更抗崩：TextVQA 在 50% 预算下 AdaLLaVA 掉到 29.8%、SmartVL 保持 54.4%；VizWiz 上 55.1% 对 34.3%
- 预算可连续调节，适应范围 2.5T–8.5T FLOPs，远宽于固定剪枝方法；且 L 变体（只降层）最优——推理时降宽不如"裁token加降层"
- 满预算下 76.5%，与基座持平，即省预算不损失上限精度；13B 在 7.25T FLOPs 达 75.45%（AdaLLaVA 72.27%）
- 最优token/计算分配因任务而异（POPE 偏好高token保留加浅层推理，VQAv2 偏好深推理），单独训练两控制器或固定比例都非最优——需要留出按任务调节的空间

**观测**: 粒度：token级（视觉补丁）＋实例级预算｜观测内容：每个视觉补丁的保留决策、层/头激活状态、实例级 FLOPs 预算消耗｜数据来源：训练中控制器输出

**动作**: 用观测到的视觉补丁保留决策与实例级预算消耗，识别token裁剪与层/头降级的耦合，据此联合控制序列长度/深度/宽度以守住预算。

**优化方式**: 预算约束训练：共享预算编码下联合训练视觉token裁减与层/头降级，用可微分 FLOPs 估计器守住预算；推理时按预算先裁token、不足再降层降头｜所属路线：上下文与输入优化（预算约束训练，兼数据格式与表示）

---
### [18] Cache-Aware Prompt Compression: A Two-Tier Cost Model for LLM API Caching

**基本信息**: arXiv:2607.15516 | 预印本 | Yan Song（PayPal Inc.）

**研究内容**: 生产环境通常同时用两个降本手段：prompt caching（对重复前缀打折计费）和 prompt compression（减少发送的token数），但文献一直把它们分开研究，而两者天然冲突。主流压缩方法（query-aware）对每个查询生成不同的压缩前缀，这让严格前缀缓存每次都失效——每次调用都是全价预填充，压缩省下的钱被反复的缓存写入抹掉。论文指出，文献隐含的“缓存命中率恒为 1”假设其实是错误前提，并在 Anthropic Sonnet 4.6 API 上实测出三个反直觉的事实：缓存是两层架构，约 3,500 token是一道分明的门槛，低于它命中率只有平台期约 0.83，高于它命中率才是 1.0；压缩比 $r \ge 6$ 时，query-aware 压缩反而比不做压缩的纯缓存便宜；把“所有查询共用同一个压缩前缀”的压缩与显式缓存标记组合（论文称 CAPC），在 LongBench-v2 的 16/16 个配置上严格优于全部文献基线。

定价结构是决定性的：缓存写与缓存读相差 12.5 倍，输入/输出相差 5 倍。

**方法**: 论文对比四种策略的单次调用成本：A 无优化（全文档+查询，不缓存不压缩）；B 仅缓存（全文档打缓存标记）；C query-aware 压缩（每查询前缀不同，不缓存）；D CAPC（共用压缩前缀压缩后再缓存）。由此导出交叉阈值公式：只有当实际命中率超过交叉阈值时，压缩+缓存（D）才比仅缓存（B）便宜；缓存写相对输入的溢价与缓存读的折扣决定该阈值，压缩比 $r$ 越大，需要的命中率越高——压缩比的选择直接由缓存计费规则决定。

CAPC 算法本身很简单：对静态文档一次性压缩，所有查询复用同一个压缩结果，缓存标记放在压缩文档块上而不是查询块上。配套两个机制。一是“层级保持”压缩比上界：压缩后的前缀不能低于 3,500 token，否则会从命中率 1.0 的缓存层掉进命中率只有 0.83 的热层，反而更贵。二是自适应缓存边界：对文档的多个版本逐句位置算指纹，把位置分成静态、准静态与动态三类，只把最长静态连续前缀放进缓存——文档小幅更新时缓存前缀不被破坏。

**实验设置**: LongBench-v2 四个文档 × 压缩比 {2,3,4,6} = 16 配置（每格 N=10 查询）；企业工具助手（94,401 token静态前缀，287 个 MCP 工具定义）模拟与端到端生产路径；graphify 代码库（2.0M token JSON）；τ-bench retail 50 任务；基线为 vanilla、cache-only、query-aware 压缩；全部实验总 API 花费 98.96 美元。

**关键结果**:
- 共用压缩前缀再缓存（CAPC）几乎总最便宜：16/16 配置中全部胜出，平均每查询成本 A 0.0741 / 仅缓存 0.0144 / query-aware 0.0238 / CAPC 0.0073 美元，相对无优化省 89.6%、仅缓存省 48.5%、query-aware 省 64.4%
- 压缩比要按缓存计费规则选：r=6 时交叉阈值 0.942，超过经验命中率平台约 0.89，4/4 个 r=6 配置里 query-aware 压缩确实比仅缓存便宜——缓存写/读相差 12.5 倍，决定了多压缩才划算
- 乱压缩可能比不压缩更贵：τ-bench 上 query-aware r=3 每查询 0.1744 美元，比不压缩的 0.1244 贵 40.1%，缓存写 1.64M 对 0.87M（+87%）；同一场景 CAPC r=3 只要 0.1145 且任务奖励与不压缩基线完全持平
- 生产场景端到端同样省：CAPC 总成本 3.03 美元，相对无优化基线 5.55 美元省 45.5%、相对仅缓存 3.59 美元省 16%，缓存写只有无优化基线的 43%
- 保质量有代价但可控：层级保持比率下质量差低于 0.05；两层缓存存在约 3,500 token 门槛——2,053 token 前缀命中率仅 0.833，4K 以上前缀从首次后续调用起命中率 1.0，使用时别把压缩前缀压到门槛以下

**观测**: 粒度：调用级｜观测内容：单次调用的输入/输出/缓存写/缓存读四类计费token、前缀命中率、压缩比与交叉阈值｜数据来源：Anthropic Sonnet 4.6 API 实测计费

**动作**: 用观测到的调用级命中率与交叉阈值，决定该查询采用"仅缓存"还是"压缩+缓存"、压缩比取多大——调用级观测直接驱动策略选择。

**优化方式**: 缓存感知压缩：让所有查询共用同一压缩前缀再打缓存标记、标记放在压缩文档块而非查询块；用交叉阈值公式与层级保持上界指导压缩比，保住缓存命中｜所属路线：上下文与输入优化（缓存感知压缩）

---
### [19] TokenPilot: Cache-Efficient Context Management for LLM Agents

**基本信息**: arXiv:2606.17016 | 预印本 | Buqiang Xu、Zirui Xue、Ningyu Zhang（浙江大学，通讯）等（电子科技大学、西安电子科技大学、HomologyAI；已集成进 LightMem2）

**研究内容**: 长程智能体会话的上下文越滚越大，推理成本随之上涨。已有的削减方法——静态文本压缩（LLMLingua、LLMLingua-2）、轨迹压缩、按需分页、把历史卸载到外部存储——几乎都只盯着“内容变少”这一件事，但激进的截断和分页会不断改变提示布局、破坏前缀的字节连续性，导致后端 KV 缓存频繁失效；缓存失效带来的全价预填充惩罚，最终可能超过文本削减省下的钱。

论文的主张：有效框架必须同时做到两件事——少放内容，以及保持前缀稳定、让缓存不失效。TokenPilot 用两层机制实现：全局层在内容刚进入上下文时就用固定规则优化布局，保住前缀稳定；局部层在线评估每段历史还派不派得上用场，确实过期了才成批清除。它以货币成本为核心指标（按 GPT-5.4-mini 定价），把每次调用的输入token拆成缓存命中与未命中两类分别记账——缓存命中率是它的首要指标。

![78576988662](C:\Users\yzy14\AppData\Local\Temp\1785769886627.png)

**方法**: 成本模型只有一行：总成本等于缓存命中token数乘读折扣加未命中token数，读折扣远小于 1——省钱的唯一办法是提高命中比例。全局层做三件事。第一，把消息分成两类区别对待：内部意图消息（任务提示、思考轨迹、工具调用）价值高，完整保留；环境反馈（工具返回的大量文本）噪声大，按“基础价值”与“访问频率检查”的较大者决定去留——内容被频繁访问就升级为完整投递。第二，前缀稳定化：把运行时易变字段（智能体 id、工作目录、时间戳）替换成静态占位符，把工具定义整体挪到系统提示末尾，保证从第一轮起前缀逐字节一致。第三，工具输出精简：输出先压成简要摘要存入工作记忆，原始内容按内容哈希存进外部存档；执行中发现摘要缺关键信号时，轻量恢复工具按哈希取回原文，并自动升级该路径状态、以后不再截断。

局部层把每段轨迹标记为三种状态之一：活跃、已完成、可清除。一个在线模型估计器（Qwen3.5-35B-A3B，全程运行成本不到 0.03 美元）每隔 B 轮检查一次（B=3 为最优），用“是否已解决 + 后续还用不用得上”两个信号驱动状态转移；已完成且后续用不上的段落才允许清除，清除是单遍结构性清理、按批次执行、动作克制。已完成的段落只要对后续交互还用得上，就继续占着缓存位置——清除内容时优先保住缓存布局，而不是一味追求文本变少。

**实验设置**: PinchBench（123 个任务、11 类）+ Claw-Eval General（161 个多步任务），同类别任务连续分组模拟真实连续多任务会话；两种评测模式：独立模式（每个任务边界重置上下文）与连续模式（历史贯穿整个任务序列）；所有智能体基线用 GPT-5.4-mini；成本按 GPT-5.4-mini 官方价；基线含 Vanilla、LLMLingua-2、SelectiveContext、LCM、Pichay、Summary、MemoBrain、AgentSwing、MemOS 等 10 个。

**关键结果**:
- 管理收益在长会话里最大：总成本 PinchBench 独立/连续模式均 −61%（8.31→3.22 美元），Claw-Eval 独立 −56%（5.16→2.27）、连续 −87%（81.52→10.58）——无管理的连续长会话成本会涨到 81.52 美元，正是管理收益最大的场景
- 前缀稳定化是命中率提升的主因：平均命中率 PinchBench 从 38.7%→79.2%、Claw-Eval 从 67.2%→83.1%
- 组件贡献可分别量化：只加缓存稳定化成本就从 8.31→4.35 美元（缓存未命中 8.75M→2.82M），再加缩减 pass 到 2.87；去掉恢复工具则能力从 80.9 掉到 77.1 且成本回涨——压缩必须可恢复，否则省了钱丢了能力
- 连续模式下 LLMLingua-2 等文本压缩既贵又伤性能——即"只压缩内容、不保前缀稳定"的路线在长会话中不成立

**观测**: 粒度：调用级（辅以会话轨迹级）｜观测内容：每次调用的输入token分解为缓存命中/未命中、输出token、前缀逐字节一致性、每段历史被访问的频率｜数据来源：追踪组件记录每次调用用量，成本按官方定价折算

**动作**: 用观测到的缓存命中率与每段历史访问频率，决定保留/驱逐哪些消息、何时按哈希恢复被压缩的原文。

**优化方式**: 前缀稳定化（易变字段换静态占位符、工具定义移出前缀区）、工具输出精简且可恢复（摘要+哈希取回）、按"基础价值与访问频率"决定消息去留——用观测到的命中率与访问频率决定保留什么、驱逐什么｜所属路线：上下文与输入优化（缓存感知压缩）

---

### [20] LLMLingua: Compressing Prompts for Accelerated Inference of Large Language Models

**基本信息**: EMNLP 2023 | arXiv:2310.05736 | Huiqiang Jiang、Qianhui Wu、Chin-Yew Lin、Yuqing Yang、Lili Qiu（微软）

**研究内容**: LLM 被广泛应用于各类任务，随思维链提示与上下文学习技术成熟，喂给模型的提示词越来越长，甚至超过数万 token，推理延迟与成本随之上升，压缩提示成为降低成本的直接手段。已有的提示压缩方法可分为两类：直接截断会丢失关键信息，基于学习的压缩在小模型上压缩效果有限。论文提出 LLMLingua，一个"粗到细"的 prompt 压缩方法，包含三个组件：预算控制器在高压缩比下维持语义完整性；token 级迭代压缩算法建模被压缩内容之间的相互依赖；指令调优让压缩后的提示分布与被压缩模型对齐。

实验横跨 GSM8K、BBH、ShareGPT、Arxiv-March23 四个不同场景的数据集，覆盖数学推理、复杂推理、对话与论文摘要等场景，证明该方法达到当时最优性能，并允许最高 20× 压缩而性能损失很小。论文还做了压缩率与性能之间的权衡分析，并指出压缩后的提示可以与缓存结合进一步加速推理。

![78576993612](C:\Users\yzy14\AppData\Local\Temp\1785769936124.png)

**方法**: 粗到细两阶段压缩：先删除明显冗余的句级/段级内容，再做 token 级迭代剪枝；预算控制器按剩余预算决定每阶段的保留比例；指令调优让压缩后的提示分布与被压缩模型对齐。

**实验设置**: GSM8K、BBH、ShareGPT、Arxiv-March23 四类数据集；多个商用与开源 LLM（GPT-3.5-Turbo 等）；与多种压缩与截断基线对比。

**关键结果**:
- 最高 20× 压缩且性能基本保持（达到当时 SOTA），适合token预算极紧、对延迟敏感的调用
- 压缩后的提示可与 prompt 缓存结合进一步加速推理——使用时把两者组合，收益可叠加

**观测**: 粒度：调用级（token粒度）｜观测内容：每个token的困惑度（语言模型给文本打的可预测性分数，越低越常见、越可预测）、压缩前后token数与任务性能｜数据来源：提示词静态分析，无需调用被压缩的大模型

**动作**: 用观测到的每个token的困惑度（信息量），决定保留哪些token、删掉哪些低信息token，在预算内控制压缩率。

**优化方式**: 困惑度驱动的token级剪枝：预算控制器决定保留比例，先删低困惑度（低信息量）token，压缩率最高 20× 且性能基本保持｜所属路线：上下文与输入优化（prompt压缩）

---

### [21] LongLLMLingua: Accelerating and Enhancing LLMs in Long Context Scenarios via Prompt Compression

**基本信息**: ACL 2024 | arXiv:2310.06839 | Huiqiang Jiang、Qianhui Wu、Xufang Luo 等（微软）

**研究内容**: 长上下文场景下 LLM 面临三个主要挑战：更高的计算成本、性能下降与位置偏差——模型对输入中位于中间位置的信息记忆较弱。研究指出，LLM 的性能取决于输入提示中关键信息的密度与位置，而非上下文总长。论文据此提出 LongLLMLingua，面向长上下文场景的 prompt 压缩方法，目标是"在压缩的同时提升 LLM 对关键信息的感知"，以同时缓解计算成本、性能下降与位置偏差三个问题。方法包含问题感知压缩（以查询为条件评估各段落的信息相关性）、文档重排序（把关键信息移到更优位置以缓解位置偏差）与动态压缩比控制。

跨多种长上下文场景的评估表明，LongLLMLingua 既提升性能又显著降低成本与延迟：在 NaturalQuestions 上以约 4× 更少的 token 把 GPT-3.5-Turbo 的性能提升最多 21.4%；在 LooGLE 上实现 94.0% 的成本降低；对约 1 万 token 的提示做 2×–6× 压缩时，端到端延迟加速 1.4×–2.6×。

![78576997506](C:\Users\yzy14\AppData\Local\Temp\1785769975063.png)

**方法**: 问题感知压缩（以查询为条件评估各段落的信息相关性）；文档重排序（把关键信息移到更优的位置，缓解位置偏差）；动态压缩比控制（按文档重要性与预算调整压缩程度）。

**实验设置**: NQ、LooGLE、LongBench 等长上下文问答基准；GPT-3.5-Turbo 等模型；2×–6× 压缩比；对比不压缩与简单截断基线。

**关键结果**:
- 压缩的同时还能提准确率：约 4× 更少 token 下，NQ 上 GPT-3.5-Turbo 提升 21.4%——长上下文场景"压缩+重排"优于不压缩
- 成本与延迟收益明确：LooGLE 成本 −94.0%；约 1 万 token 的提示做 2×–6× 压缩时，端到端延迟加速 1.4×–2.6×

**观测**: 粒度：调用级｜观测内容：关键信息的密度与位置、压缩前后token数与问答性能｜数据来源：提示词分析与小型语言模型评估

**动作**: 用观测到的关键信息密度与位置，决定压缩哪些段落、保留哪些高信息段落，并面向问题重排序。

**优化方式**: 位置感知的prompt压缩：保留关键信息密度高的段落、压缩低价值部分，并面向问题做重排序｜所属路线：上下文与输入优化（长上下文压缩）

---

### [22] CoACT: Action-Preserving Observation Compression for Coding Agents

**基本信息**: arXiv:2607.02911 | 预印本 | Haorui Chen、Yuancheng Zhu、Yitong Zhang、Jia Li（清华大学等）

**研究内容**: 基于 LLM 的编码智能体通过与环境迭代交互求解软件工程任务——读取文件、运行测试、修改代码，返回的观测（工具输出、测试结果、环境反馈）不断累积在上下文中，成为推理成本的主要来源。观测压缩通过在观测追加进上下文之前缩短它来降低成本，但现有方法不显式建模"压缩如何影响智能体的后续行为"，因而效率-效果权衡不理想：压缩太激进会丢掉继续求解所需的信息，太保守又省不了多少 token。

论文提出 CoACT，一种面向编码智能体的动作保持观测压缩方法，建立在"下一步动作保持"（NAP）准则上：NAP 要求压缩后的观测诱导出与原始观测完全相同的下一步动作。通过在每一步检查智能体的即时下一步动作，NAP 为"压缩是否保留继续求解所需信息"提供了可操作的信号——如果压缩不改变下一步动作，就认为压缩保留了任务所需信息。训练时，teacher 模型先为每条观测生成多个压缩候选；CoACT 用基于 NAP 的动作保持奖励过滤掉会改变下一步动作的候选，再用长度减少奖励选出最紧凑的候选作为轻量压缩器的监督信号。在 SWE-bench Verified 上、用三个编码 Agent 模型验证，CoACT 平均把总 token 消耗降低 33.0%，任务解决能力接近不压缩基线。

![78577007145](C:\Users\yzy14\AppData\Local\Temp\1785770071457.png)

**方法**: 训练时，teacher 模型先为每条观测生成多个压缩候选；用基于 NAP 的动作保持奖励过滤掉会改变下一步动作的候选，再用长度减少奖励选出最紧凑的候选作为轻量压缩器的监督信号；推理时直接用该轻量压缩器压缩工具返回的观测。

**实验设置**: SWE-bench Verified；三个编码 Agent 模型；对比不压缩基线与现成压缩方法。

**关键结果**:
- 编码智能体的观测可安全压缩约三分之一：SWE-bench Verified 上平均总 token 消耗降 33.0%，任务解决能力接近不压缩基线
- NAP 过滤是保持任务能力的关键组件：只在压缩不改变下一步动作时保留候选，过滤掉会改变决策的压缩——使用时以"下一步动作不变"为准，兼顾省 token 与能力

**观测**: 粒度：步骤与动作级｜观测内容：原始观测token数 vs 压缩后token数、压缩前后Agent下一步动作的一致性（NAP分数）｜数据来源：执行日志中的观测-动作对

**动作**: 用观测到的"压缩前后下一步动作是否一致"（NAP分数），决定采纳哪个压缩候选——只有不改变Agent决策的压缩才被保留。

**优化方式**: 动作保持压缩（NAP准则）：以"压缩后下一步动作不变"为过滤条件，训练轻量压缩器只保留不改变决策的压缩结果｜所属路线：上下文与输入优化（动作保持压缩）

---

## 五、推理链优化

### [23] Ares: Adaptive Reasoning-Effort Steering for PPA- and Cost-Aware RTL Optimization with LLM Agents

**基本信息**: arXiv:2607.27879 | 预印本 | Stef Cuyckens、Mihaela Jivanescu、Jun Yin、Chao Fang、Marian Verhelst（KU Leuven / Nokia Bell Labs）

**研究内容**: 满足功耗、性能、面积（PPA）指标是数字硬件设计的核心难题，而决定这些指标的 RTL 描述传统上依赖稀缺的人类专家，大量 RTL 因此功能正确却未经优化。LLM 智能体反复编辑 RTL、综合、读回 PPA，改进就保留，但每次 LLM 调用都要付美元：能力强的智能体依赖大型商业模型，token账单快速上涨，单次优化运行就要好几美元。论文指出已有工作的不足：部分优化器只评面积与时序、漏掉功耗；所有最先进的优化器最多只报告整个研究的一个美元总数，没有统一指标把优化质量与美元成本挂钩，无法公平比较；质量来源还被错误归因——最强智能体把成绩归功于精心构造的跨设计记忆，但同等花费的对照表明，工程化记忆并不比简单拼接同一批经验更可靠；此外，所有已有 RTL 智能体的推理努力度（低/中/高）固定不变，固定高努力会在简单修改上过度思考，固定低努力又让真正需要深想的迭代欠思考。

本文提出 Ares，做三件事：给每次 LLM 调用计算归一化美元成本，并与质量指标 FoM 一并报告；用同等花费的对比推翻“记忆构造决定质量”的说法；用耐心计数器自适应每次调用的推理努力度——从中等努力起步，只在低努力推不动、进展停滞时才升级到深度推理。核心结论是：决定每美元 FoM 的是智能体把推理预算花在哪、何时花，而不是记忆怎么写。

![78577017760](C:\Users\yzy14\AppData\Local\Temp\1785770177606.png)

**方法**: 每轮把当前最优 RTL 交给 LLM 提修改，候选必须通过功能等价验证（长随机测试台加顺序等价检查）、商业综合与门级复验，FoM 更低才保留。自适应策略：每个设计从中等努力开始；停滞迭代计数器加 1，失败迭代加 $w$（$w=2.8$，一次失败几乎顶三次停滞），有改进则按相对增益放电；计数器达到阈值 $p=3$ 就升级到高强度并清零，参数在 21 个训练设计上网格搜索拟合一次。每次调用按四类计费token折算美元：输入、缓存读、缓存写、输出四类token数乘各自单价求和，即一次调用的美元成本；再除以该设计一次高努力调用的平均成本，得到跨设计可比的“high-call”单位。

**实验设置**: 24 个开源 RTL 模块，其中 tv80、uart、controller 三个留作测试集，其余 21 个作训练集；Claude Opus 4.6 extended thinking + prompt caching，跑在 Claude Code 上；综合用商业 Synopsys DC + PrimeTime（Nangate 45nm 开源工艺库）。

**关键结果**:
- 自适应努力度比均匀用力划算：三个未见设计上 FoM 改善 23–27%，同样成本下最佳固定努力只有 16–23%
- 性能与成本双优：controller 上 Ares FoM 0.694，优于 REvolution（0.943）与 Dr. RTL（0.923），token 只花 Dr. RTL 的 12%、总成本低 8.7 倍
- 记忆怎么写不重要：同等花费下，工程化记忆与简单拼接日志的效果一样——把预算花在"何时推理多深"，而不是记忆构造
- MX MAC 上把 LLM 草稿与手工优化版差距闭合最多 83%（FoM 均值 68.8→33.6，手工版 18.9），运行间方差降 58%——结果也更稳定

**观测**: 粒度：步骤与动作级（口径接近调用级）｜观测内容：每次调用的推理努力度（低/中/高）、token数与四类计费token折算的美元成本、质量指标 FoM｜数据来源：框架记录 + 商业综合工具

**动作**: 用观测到的推理努力度与每美元FoM，识别停滞点，据此决定是否从低/中努力升级到高强度推理。

**优化方式**: 推理努力度自适应：耐心计数器检测停滞，仅在低努力推不动时升级到高强度，证明在停滞处定向升级比均匀分配更能提升每美元 FoM｜所属路线：推理链优化（推理努力度自适应）

---
### [24] OS-Pruner: Pruning Chains-of-Thought of Reasoning Models via Optimal Stopping

**基本信息**: arXiv:2607.11089 | 预印本 | Mohammed Ehab、Aymane El Gadarri、Vivek Farias、Adam Jozefiak（MIT）、Ciamac C. Moallemi（哥伦比亚商学院）

**研究内容**: OS-Pruner 解决推理模型的“计算过度思考”：答案已经确定还在继续生成验证性内容、重复论证，浪费token和延迟。CoT 是推理模型解题的核心机制，更长的推理轨迹往往对应更强的能力，但模型经常在答案已可确定后仍继续生成——重述已有论证、验证已定结论、做低价值旁支计算、迟迟不给最终答案，有时反而损害准确率；而且“需要多少推理”因题而异，简单题几步就够，难题才值得长搜索。前人做法分三类：外部强加思考预算（目标长度由外部指定，不随推理状态自适应）；重训模型写短链（有效，但要花大价钱重训基础模型，还可能影响其他任务上的表现）；或者用“分类器加固定阈值”做早停，靠答案熵、答案收敛、内部状态探测器（从模型内部状态学出的小分类器）等信号判断当前是否已足够。

OS-Pruner 指出阈值分类在理论上就存在缺陷——停止决策应取决于“继续推理的期望价值”，而不是“当前状态有多好”：两个正确率相同的前缀，一个有高价值推理路径、一个没有，决策应当不同。论文从数学上证明，即使阈值取到最优，这类分类器与最优停止之间的差距也可以任意大。它的定位是轻量、即插即用的早停插件，不修改已经冻结的基础模型：只加一个轻量策略头并微调最后两层 Transformer，推理时仅在段落边界被调用，用单个参数 $\lambda$ 在准确率-效率前沿上选操作点；训练数据可预计算，无需在线 rollout。

**方法**: 把 CoT 裁剪建模成最优停止问题。奖励函数为在第 $i$ 步停止的收益等于该前缀直接出答案的正确率减去 $\lambda$ 与已生成token数的乘积，$\lambda$ 越大越倾向早停。停止策略是一个轻量策略头，只微调最后 2 层 Transformer；训练时 $\lambda$ 从大值逐渐减小，让策略先学“尽量早停”再学“该停时停”。训练数据可预计算：冻结的基础模型先跑出推理轨迹，再模拟“每一步停下会得到什么答案”，得到每个前缀的细粒度监督，无需在线 rollout。论文还证明：固定阈值分类器与最优停止之间的差距可以任意大，即“正确率超过 $\gamma$ 就停”这类方法理论上就次优。

**实验设置**: 三个基础模型：DeepSeek-R1-Distill-Qwen-7B、GPT-OSS-20B、DRPO-7B（R1-Distill 上用 DRPO 重训的短链模型）；训练数据 40,000 道题，来自 AIME（1984–2023）、AMC、Omni-MATH；评测 GSM8K、MATH-500、AIME24/25；温度 0.6、top-p 0.95；基线含分类探测器、答案收敛等早停方法。

**关键结果**:
- 普通题大幅压缩、准确率基本不动：GSM8K 长度减 59.3%（1219→496 token）准确率仅 −0.7pp；MATH-500 减 52.8%（3377→1593）准确率反升 2.7pp（77.4→80.1）
- 难题自动少剪：AIME 只减 6.8%——策略在额外计算有真实价值时自动变保守，所以简单题省得多、难题不敢乱停
- 大多数模型-数据集组合在准确率损失低于 2% 时能减 20–60% 长度，平均效率得分 0.40 优于全部基线
- 与重训式方法互补：在已为简洁专门训练的 DRPO-7B 上还能额外减 26.4%，早停插件可叠加在模型端优化之上

**观测**: 粒度：步骤级｜观测内容：每个推理前缀的token数、在该处停止直接出答案的正确率、继续推理的期望价值｜数据来源：冻结模型预计算推理轨迹，无需在线 rollout

**动作**: 用观测到的当前前缀的正确率与已生成token数，判断"继续推理的期望收益是否大于 token 代价"，据此决定是否在段落边界停止。

**优化方式**: 最优停止：只在"继续推理的期望收益大于 token 代价"时继续，用单个 $\lambda$ 在准确率-效率前沿上选操作点，理论上证明固定阈值分类器次优｜所属路线：推理链优化（最优停止）

---
### [25] BAVT: Spend Less, Reason Better — Budget-Aware Value Tree Search for LLM Agents

**基本信息**: arXiv:2603.12634 | 预印本 | Yushu Li、Wenlong Deng、Jiajin Li、Xiaoxiao Li（英属哥伦比亚大学与 Vector Institute）

**研究内容**: 测试时扩展（推理时多花算力换可靠性）是提升智能体的主流手段，但现有方案把算力当无限资源，智能体常把token和工具调用预算浪费在冗余步骤或死路上，而且盲目加预算收益递减。论文指出，当前部署的智能体大多依赖假设资源无限的简单自主循环；搜索类方法（ToT、GoT、LATS 等）虽然效果好，但只以精度为目标，缺少对昂贵动作的惩罚，也不随资源耗尽调整搜索几何。已有预算感知方法要么需要昂贵微调，且只适用于封闭式静态问题、无法迁移到智能体工作流，要么只在轨迹级做粗糙启发式（如把剩余预算写进提示词），完全依赖 LLM 的自我约束能力，无法在推理中途干预、无法及时放弃失败轨迹。

BAVT 是免训练的推理时框架，把多跳推理建模成动态搜索树，用“工具调用数 + 输出token数”二维预算引导搜索。树中节点是中间状态，边是动作或工具调用；同一个 LLM 骨干交替扮演生成器和批判者，批判者评估每个动作的相对进步量而非绝对状态质量，以克制 LLM 自我评价的过度自信，从而剪掉冗余的工具调用；预算条件选择机制把剩余预算比例作为节点值的幂次指数，预算充足时广泛探索，预算将尽时聚焦最高价值分支，实现从广泛探索到“只走最高价值分支”的无参数平滑过渡。论文并给出收敛性定理：在显式有限预算下，以至少 $1-\epsilon$ 的概率到达终止答案。

![78577035515](C:\Users\yzy14\AppData\Local\Temp\1785770355156.png)

**方法**: 树中节点是中间状态，边是动作（推理或工具调用）。三块机制。其一，残差价值批判：不评估节点绝对质量，而是评估“这一步比上一步进步了多少”，子节点价值更新为父节点价值加残差的压缩，这个设计专门克制 LLM 自我评价的过度自信，能及时剪掉冗余工具调用。其二，预算条件选择：预算状态为二维（工具、token），每个动作消耗相应成本，剩余比例取更紧张的那个，其倒数作为节点值的幂指数——预算充足时指数接近 1 广泛探索，预算快耗尽时指数变大、采样分布聚焦最高价值节点、转向贪婪。其三，预算回退：预算耗尽且还没得到答案时，从累计值最高的叶节点强制输出答案；发现终止答案后把值向根节点全局回传。三档预算：低（5 次调用）、中（10 次）、高（20 次）。

**实验设置**: 四个多跳问答基准：HotpotQA、2WikiMultihopQA、MuSiQue、Bamboogle；两个模型家族：GPT-OSS-20B（推理模型）和 Qwen3-30B-A3B-Instruct（指令模型）；基线是“预算约束多数投票”的并行采样——同一预算下跑尽可能多的独立轨迹再投票；检索用 2018 Wikipedia dump + E5 稠密检索器，每次取 5 段。

**关键结果**:
- 预算紧时树搜索比平行采样划算：GPT-OSS-20B 低档（5 次调用）EM 0.338，超过基线高档（20 次调用）的 0.334——4 倍资源效率拿到相同精度
- 基线盲目加预算收益极小：指令模型基线预算翻 4 倍精度几乎不动（0.289→0.293），BAVT 低档即达 0.386
- 单基准提升可观：MuSiQue 上 Qwen 由 0.12→0.21，Bamboogle 上 OSS-20B 由 0.31→0.49
- 消融确认关键组件：中档下纯树随机展开反而比基线差（EM 0.215 对 0.268），加步级残差价值到 0.309，完整 BAVT 到 0.388——预算条件选择是决定性一环，随机展开本身不保证更好

**观测**: 粒度：步骤级｜观测内容：每步消耗的工具调用次数与输出token数、剩余预算比例（二维）、节点残差价值评分｜数据来源：推理时框架内计算

**动作**: 用观测到的剩余预算比例与节点残差价值，决定探索还是聚焦最高价值分支、剪掉哪些冗余工具调用、何时回退输出。

**优化方式**: 预算感知树搜索：把剩余预算比例作为节点值幂指数，预算充足广泛探索、将尽聚焦最高价值分支；残差价值批判剪掉冗余工具调用，预算耗尽时从最高价值叶节点回退输出｜所属路线：推理链优化（预算感知搜索）

---
### [26] D-CoT: Disciplined Chain-of-Thought Learning for Efficient Reasoning in Small Language Models

**基本信息**: arXiv:2602.21786 | 预印本 | Shunsuke Ubukata（东洋大学）

**研究内容**: 以 DeepSeek-R1 为代表的推理模型，通过大规模后训练获得了复杂多步思考能力，把前沿模型的思维链蒸馏进小模型的做法随之快速普及。但论文指出，直接照搬前沿模型的思考过程并不必然提升小模型：小模型容量有限，控制不了复杂上下文，会出现思路漂移、无意义的思考循环，甚至已得出正确答案仍反复重算，耗尽token预算后改错答案，即“过度思考”。任务准确率与思维链长度呈倒 U 形关系——最终答案概率是每步成功概率的乘积，小模型单步出错率高，思维链一超过最优长度，性能就快速恶化。已有做法（如 DLCoT）把思维链分解成多个成分后静态过滤冗余路径，属于被动的事后删除：只在数学任务上验证过，且作者自己的实验显示它在 AIME2024 上性能明显下降（53.3% 降到 40.0%），因为静态过滤剥离了高难题所需的探索多样性。

本文提出 D-CoT，不删除思考内容，而是重建思考的顺序和结构：训练时用控制标签（事实核查、多视角探索）当脚手架，把推理过程约束成有纪律的结构，让准确率和token效率同时提升。核心贡献是仅用约 5,000 个训练样本，GPQA-diamond 提升 9.9%、MMLU-Pro（0-shot）提升 9.1%，平均输出token大幅下降；模型还内化了这套纪律结构，推理时不显式带控制标签也能保持高性能。

**方法**: 三个控制标签对应三种思考模式：事实核查与列前提、算法推理与计算、多视角探索，刻意与预训练学到的“温度”概念挂钩。训练数据每个样本含元推理计划（显式写明哪里用哪个标签、为什么）和理想回答，配套“能干但走偏”的拒绝样本（按 31 类缺陷设计）。用 ORPO 而不是 DPO 训练：不需要参考模型，且避开 SFT 阶段单独对齐的额外开销。训练域特意选与评测域无关的 7 个域（网络安全响应、供应链物流等）119 个主题，用 Qwen3-235B 生成 5,181 对样本，按相似度阈值和 13-gram 重叠双重过滤、去除污染样本后剩 5,079 对——教的是推理结构，不是领域知识。推理时可以不带标签，结构已被内化。

**实验设置**: Qwen3-8B 基座 + LoRA，单张 RTX 5090 训练 2 epochs；评测 GPQA-diamond（198 题，0-shot，5 种子平均）和 MMLU-Pro 全部约 1.2 万题；对比基座与 D-CoT × 温度（锁定与动态）× 提示（基础与自定义）共六种条件。

**关键结果**:
- 用约 5,000 样本重训推理结构收益显著：GPQA-diamond 准确率 43.03%→52.93%（+9.9pp），平均输出token 5,875→2,073（−64.7%），空答率从 30.91% 降到 3.54%——既更准又更短
- 另一个评测域同样稳健：MMLU-Pro 0-shot 55.66%→64.73%（+9.1pp），token 减 31.2%
- 部署无需额外输入：推理时不带标签反而性能最好，训练学到的结构确实已被内化
- 结构优化是主导因素：动态切换采样温度只换来 0.51–1.52pp，六种条件在"准确率-token"平面上构成清晰权衡前沿——使用时优先改结构，别指望调温度

**观测**: 粒度：步骤与动作级｜观测内容：控制标签划定的思考段内的token数与空答率、标签转移序列｜数据来源：训练数据与推理输出统计

**动作**: 用观测到的思考段token数与空答率，识别无效思考循环与"过度思考"，据此用控制标签重建推理结构（训练时手段）。

**优化方式**: 结构化推理：训练时用控制标签（事实核查/算法推理/多视角探索）当脚手架约束推理结构，模型内化后推理时无需显式标签也保持简洁——属训练时手段｜所属路线：推理链优化（结构化推理）

---
### [27] TokenSqueeze: Performance-Preserving Compression for Reasoning LLMs

**基本信息**: NeurIPS 2025 | arXiv:2511.13223 | Yuxiang Zhang、Deng Cai、Binbin Lin（浙江大学）、Zhengxu Yu、Zhongming Jin、Jieping Ye（阿里云）、Qiang Fu（智源研究院）

**研究内容**: TokenSqueeze 解决 Long2Short（把长推理压成短推理）方法的“推理过度简化困境”：为了缩短输出把必要推理步骤也剪掉，导致准确率大跌。推理模型（o1、DeepSeek-R1）靠很长的思维链取得强性能，但长链带来延迟和显存开销，在简单任务上还会“过度思考”，多轮试错的智能体场景尤其受影响。前人做法分两类：在线强化学习方法在奖励里加长度惩罚，但每步训练都要重新采样响应，计算代价高昂；离线方法多是挑最短的正确回答当训练数据，属于无差别压缩，容易删掉必要步骤，也会漏掉采样池里更简洁的高质量轨迹；用外部模型改写或规则裁剪则可能破坏上下文连贯性。

这篇的论点是：当推理长度超过某个阈值后，token数与性能的相关性明显变弱，简洁回答是“表达偏好”问题而不是“推理深度”问题——推理深度应按题目难度自适应保留，值得压缩的是表达。据此提出三步训练：按难度自适应选择推理深度（易题取短链、难题保留长链），在 KL 约束下对每个推理步骤做语言精炼，再用长度感知的 DPO 目标让模型学会又短又对。全程只用模型自生成数据构建长短偏好对，不需要外部教师模型或人工标注。

![78577048024](C:\Users\yzy14\AppData\Local\Temp\1785770480240.png)

**方法**: 三步训练。第一步自适应推理深度选择：动态分位数由题目采样正确率决定，答对率高就选最短短链当正样本、答对率低则保留长链防剪掉关键逻辑，每题最多 64 对。第二步步骤内语言精炼：对每个推理步骤重采样 64 个改写候选，选最短且 KL 散度小于阈值的那个——KL 散度衡量改写前后模型“接下来会说什么”的分布差异，约束它保证语义不漂移。第三步长度感知 DPO：偏好信号按赢家与输家长度比的对数缩放，即赢家越短、输家越长则偏好权重越大，与监督微调损失按权重组合，让模型学会又短又对。

**实验设置**: 模型 DeepSeek-R1-Distill-Qwen-7B 和 1.5B（全参，8×A100）；评测 AIME24、MATH500、AIME25、LiveCodeBench；结果取 16 次独立运行均值，温度 0.6；基线 DAST、TrainEffi、复现的 Kimi-k1.5(DPO)；指标为准确率、正确回答平均长度、全部回答平均长度与 32K token预算下 AUC。

**关键结果**:
- 可放心大幅缩短推理链：MATH500 上平均token减 51.3%（3638→1773），准确率仅掉 0.4pp（92.8→92.4）——说明推理长度里的冗余主要在表达而非推理深度
- 简单任务上压缩还能提准：AIME24 准确率 +2.0pp（55.5→57.5）、token减 31.6%（7543→5157），LiveCodeBench 也到 35.0（基线 31.3）——意味着适度压短可顺带缓解"过度思考"
- 预算越紧越值得用：AIME24 在 3K token预算下准确率比基线高 15.5%，MATH500 在 1K 预算下高 43.1%——对硬性 token 限额的部署收益最明显
- 组件取舍：自适应深度选择优于一律取最短和固定分位数方案；KL 约束的步骤内改写优于外部模型改写与 TokenSkip——使用时保留这两个设计
- 训练数据可自给：全程只用模型自生成数据构建长短偏好对，不依赖外部教师或标注——无需额外数据管线即可复现

**观测**: 粒度：步骤级（兼步骤内）｜观测内容：步骤数×每步token（深度选择管步骤数、语言精炼管每步长度）、题目采样正确率、KL散度、长短偏好对｜数据来源：模型自生成数据，无需外部教师或标注

**动作**: 用观测到的题目难度（采样正确率）与KL散度，决定保留多少推理深度、每步语言精炼到什么程度（训练时手段）。

**优化方式**: 自适应深度＋步骤内语言精炼＋长度感知DPO：按难度选推理深度，在KL约束下精炼每步语言，训练时把"短"写进偏好信号——属训练时手段，需重训模型｜所属路线：推理链优化（自适应深度，兼训练时效率塑造）

---

## 六、记忆管理优化

### [28] MAGE: Beyond Semantic Organization — Memory as Execution State Management for Long-Horizon Agents

**基本信息**: arXiv:2606.06090 | 预印本 | Yaoqi Chen、Haibin Lai 等（中国科学技术大学、微软、南京大学、加州大学圣迭戈分校）

**研究内容**: LLM 智能体越来越多地执行决策相互依赖的长程任务：每个动作都会重塑后续选择，一步出错就可能连锁毁掉后续进展，因此需要连贯的执行状态，而历史很容易超出模型上下文。现有 RAG 与记忆系统无论采用平面向量库、实体图还是层次结构，都按语义相似度组织历史，近期基准反而显示不少系统不如直接把全部历史塞进上下文的长上下文做法。论文认为根源有二：其一是状态碎片化——决策链被拆散，重建不出完整的执行状态；其二是错误隔离失败——有效与错误轨迹混在一起互相污染，出错后难以定位与回滚。

MAGE 的定位是把记忆从“可检索的事实库”改造成“执行状态管理器”。它维护两层层次状态树：底层按顺序记录原始“动作-观察”对，顶层存放按子目标边界压缩的摘要；执行状态直接取“根到当前”的活动路径（压缩摘要加最近原始轨迹加之前分支的提示），替代相似度检索。四个操作形成闭环：Grow 追加新轨迹，Compress 压摘要控制上下文，Maintain 在摘要转正前对照任务验证，Revise 出错时回滚到目标边界、从新分支继续，把错误段隔离出活动路径。实验表明，这种方法在 MemoryArena 上平均把成功率提高 7.8–20.4 个百分点，token比长上下文方法少 55.1%。

![78574065402](C:\Users\yzy14\AppData\Local\Temp\1785740654027.png)

**方法**: 维护一棵两层层次状态树：底层按顺序记录原始“动作-观察”对，顶层存放按子目标边界压缩的摘要。智能体看到的执行状态 S 由三部分组成：压缩摘要 C、最近原始轨迹 R 与提示 H（之前分支探索过的候选与诊断笔记），直接取“根到当前”的路径，不需要相似度检索。四个操作闭环维护：Grow 在内容相同的情况下合并、否则追加新轨迹；Compress 在子目标完成或轨迹超长时把完成段压成摘要；Maintain 在摘要转正前让 LLM 对照任务指令验证摘要，发现问题则记录诊断；Revise 在出错时把指针回滚到目标边界、注入诊断反馈并作为新分支继续执行，实现错误隔离。

**实验设置**: MemoryArena 四个域（捆绑购物、多人旅行规划、渐进式网页搜索、形式化推理）；骨干模型 Qwen3.6-27B + ReAct，A100 + vLLM；基线为长上下文、HippoRAG2、MemoRAG、Mem0、ReasoningBank、MemoryOS、SimpleMem；指标为任务成功率、进度分与每任务token数。

**关键结果**:
- 平均优于"把全部历史塞进上下文"：成功率高 7.8pp、进度率高 8.7pp，token还少 55.1%（各域 32.9–71.4%）——记忆成本与效果同时改善，不是用效果换省token
- 子目标强依赖的任务受益最大：捆绑购物成功率 0.3933（长上下文 0.3333、HippoRAG2 仅 0.2067）、旅行规划 0.152 对 0.052——这类"多步决策环环相扣"的场景优先用它
- 购物域上 RAG/记忆基线反而不如长上下文：低 12.7–22.0pp 且多耗 12.6–14.7% token——说明纯语义检索在该域既不更准也不更省
- 三个机制各有明确职责、缺一不可：去 Compress 成功率降 7.3pp 且 token增至 2.4 倍，去 Maintain 降 5.2–6.6pp，去 Revise 降 4.0–5.2pp

**观测**: 粒度：轮次级与步骤级｜观测内容：每步上下文增长（子目标摘要+步级轨迹双层）、每任务token总量、任务成功率与进度｜数据来源：框架运行计量

**动作**: 用观测到的子目标完成与错误发生位置，决定压缩已完成段落、回滚到目标边界以隔离错误分支。

**优化方式**: 执行状态树：按子目标边界压缩已完成段落、出错时回滚到目标边界隔离错误分支，以"根到当前"的活动路径替代相似度检索｜所属路线：记忆管理优化（执行状态树）

---
### [29] Lychee: Dynamic Long Context Reasoning over Compressed Memory via End-to-End Reinforcement Learning

**基本信息**: arXiv:2602.08382 | 预印本 | Zhuoen Chen、Dongfang Li、Meishan Zhang、Baotian Hu、Min Zhang（哈尔滨工业大学（深圳）计算与智能研究院，系统名 LycheeMemory）

**研究内容**: 本文解决长上下文推理的三大顽疾：注意力机制的平方级计算复杂度、超长输入的信息遗忘与 RAG 的上下文碎片化。论文逐一分析了现有三类方法的取舍：稀疏与线性注意力降低复杂度，但在极长序列上性能退化；RAG 把文本块当作独立实体，切断多跳推理必需的逻辑依赖；递归式架构靠流式状态更新，串行推理慢，如 MemAgent 对每个块都要写一次、读一次，常数开销大。受认知机制中长时记忆与工作记忆分工的启发，LycheeMemory 把输入分块，每块压缩成 KV 缓存风格的压缩记忆；推理时用门控判断记忆块是否相关、只召回相关块，推理器只处理被召回的块并迭代更新工作记忆解题，而不是处理全部原始token。

它的定位是端到端强化学习联合优化的记忆型长上下文框架，桥接显式与隐式记忆：文档以压缩 KV 表示存储，召回与推理在可解释的明文工作记忆上进行。核心贡献有三：提出压缩器、门控、推理器三模块框架；用强化学习端到端联合优化压缩器与推理器，让压缩直接面向下游任务；把上下文从 7K 扩展到 1.75M token，提升推理效率。

![78574086936](C:\Users\yzy14\AppData\Local\Temp\1785740869367.png)

**方法**: 三模块加三步训练。三个模块：压缩器把每个块编码成压缩 KV 记忆token，基座模型加压缩 LoRA；门控为 LoRA 分类器，对每个压缩块打分，仅当分数超过阈值 0.5 时召回；推理器迭代处理被召回块、更新工作记忆，最后基于工作记忆生成答案。压缩比 $\alpha \in \{2,4,8,16\}$，即每 $\alpha$ 个原始token生成 1 个记忆token。三步训练：(1) 压缩器预训练，用合成问答、重建、创意生成三个任务先教出压缩能力；(2) GSPO 端到端强化学习联合优化压缩器与推理器，让“压什么”与“用什么”对齐；(3) 门控分类器在 rollout 数据上用交叉熵单独训练，正类权重 3.0。

**实验设置**: 基座 Qwen2.5-3B/7B；训练数据按 MemAgent 方式合成，来自 RULER-HQA，混入干扰文档（平均约 2 万token）；零样本评测 2WikiMultihopQA、StreamingQA；基线含 Search-R1、MemAgent、DeepSeek-R1-Distill 系列、Qwen2.5-Instruct-1M、QwenLong-L1、RAG；上下文从 7K 扩展至 1.75M；指标为 sub-EM、F1、推理时间与峰值 GPU 内存。

**关键结果**:
- 可扩展到超长上下文且不崩：上下文从 7K 扩到 1.75M token（28.2 倍），1.75M 时 sub-EM 71.09%（7K 时 77.34）——长上下文下精度只小幅回落
- 比递归式记忆方案更省更快：峰值 GPU 内存少 2 倍，128K 时推理快 6 倍（对比 MemAgent）——对长上下文部署值得参考
- 训练方式有讲究：端到端强化学习（+68.75）远高于分步训练（SFT 仅 +59.38、只压不训仅 +38.28）——"压什么"和"用什么"必须联合优化
- 压缩比选 4 倍是稳妥点：128K 时仍保持 80% 以上精度，压到 16 倍就退至 42.0%；门控在 112K/224K 对支持块召回 86.3%/84.1%——使用时注意召回率随上下文增长略有下滑

**观测**: 粒度：块级压缩｜观测内容：原始token按 $\alpha$ 比例压成记忆token的数量、被召回块的压缩后token、门控打分与召回率｜数据来源：框架内压缩器/门控输出

**动作**: 用观测到的门控打分与压缩块相关性，决定召回哪些记忆块、只让推理器处理被召回块（训练时手段）。

**优化方式**: 边界压缩＋门控召回：压缩器把每块压成记忆token，门控只召回相关块，推理器只处理被召回块；端到端强化学习联合优化压缩器与推理器——属训练时手段｜所属路线：记忆管理优化（边界压缩）

---
### [30] AMA: Adaptive Memory via Multi-Agent Collaboration

**基本信息**: arXiv:2601.20352 | 预印本 | 黄伟权、王子萱等（香港科技大学（广州））、钱李（山东大学）、Beier Zhu（南洋理工大学）、杨林奕（南方科技大学）

**研究内容**: LLM 智能体长期记忆主要走外部存储路线，用显式增删改查动态维护，但存储粒度与检索推理所需粒度存在根本错位：粒度太粗检索出大量噪声，粒度太细则拆散逻辑依赖，导致复杂任务推理失败。新一代智能体记忆（agentic memory）用 LLM 把历史合成摘要或向量条目，表示更灵活，却仍缺两点——推理时没有显式路由机制选择合适粒度，需求与存储持续错位；维护靠“只管加、不验证”的累积式策略，冗余与矛盾越积越多。

AMA 的定位是“检索粒度自适应 + 一致性显式维护”的记忆框架，用多智能体协作拆开记忆生命周期，避免单一控制器里互相纠缠的目标。构建器建三层记忆（原文、事实知识、片段摘要）；检索器按当前推理需求把查询路由到合适粒度；评判器验证相关性与一致性，相关性不足触发迭代检索，发现矛盾唤刷新器；刷新器只做定向更新或删除过期条目，保持长期一致性。实验显示 AMA 在多个长期记忆基准上稳定超越强基线，相对全上下文减少约 80% token，知识更新准确率近 90%，消融表明刷新器的冲突消解不可或缺。

![78577057782](C:\Users\yzy14\AppData\Local\Temp\1785770577822.png)

**方法**: 构建器建三层记忆：原文（原样记录加索引）、事实知识（按 S-V-O-C 句式模板提取的原子事实，含时间戳与说话人元信息）、片段摘要（话题切换、显式请求或上下文饱和时触发）。检索器先改写查询、把代词指代换成具体对象，再生成四维意图向量，按优先级路由：要细节走原文层、要抽象或跨事件走片段层、默认走事实层；同时预测本次检索量，并用下限保底。评判器先做相关性评估，信息不足就触发重试（最多 $K_r$ 轮）；再做冲突检测，发现矛盾就唤刷新器。刷新器只做两类操作：用户明确要求或条目超过保留期限才删除，其余矛盾一律定向更新（如改掉过期的位置信息），保持记忆连续。

**实验设置**: LoCoMo（1,540 个问答样本）与 LongMemEvals（500 样本，约 115K token历史）；骨干 GPT-4o-mini、GPT-4.1-mini、Qwen3-8B/30B-Instruct；基线含全上下文、RAG、LangMem、MemGPT、Zep、A-Mem、Mem0、Nemori；检索重试上限 $K_r=2$；指标为 LLM Score、F1、BLEU-1 与 Pass@1。

**关键结果**:
- 检索式记忆可以做到不输全上下文：GPT-4.1-mini 在 LoCoMo 上 0.805，是唯一超过全上下文（0.786）的方法；GPT-4o-mini 0.774 也压过最强基线 Nemori（0.740）
- 知识更新类任务尤其能打：LongMemEval 平均 0.698，知识更新类 0.897、单会话用户类 0.986——涉及"事实随时间变化"的场景值得用它
- 省token是实打实的：每查询输入约 3,613 token，只有全上下文（18,625）的 19%，token省约 80%，延迟从 7.21s 降到 3.91s——成本与延迟同步下降
- 冲突消解不能砍：去掉刷新器后知识更新准确率从 0.897 跌到 0.568——使用时要保留"检测矛盾并定向更新"这一步

**观测**: 粒度：轮次级｜观测内容：每查询的检索轮数 $K_r$、动态检索量、每轮token数、相关性与冲突检测结果｜数据来源：框架运行计量

**动作**: 用观测到的查询意图与检索轮数，决定路由到哪层记忆（原文/事实/片段）、是否触发迭代检索与定向刷新。

**优化方式**: 分层记忆：建原文/事实知识/片段摘要三层，按检索需求路由到合适粒度；评判器触发迭代检索，刷新器只做定向更新或删除过期条目｜所属路线：记忆管理优化（分层记忆）

---
### [31] SimpleMem: Efficient Lifelong Memory for LLM Agents

**基本信息**: arXiv:2601.02553 | 预印本 | Jiaqi Liu、Yaofeng Su、Peng Xia、Siwei Han（北卡罗来纳大学教堂山分校）、Zeyu Zheng（加州大学伯克利分校）、Cihang Xie（加州大学圣克鲁兹分校）、Mingyu Ding、Huaxiu Yao

**研究内容**: LLM 智能体受固定上下文窗口限制，需要记忆系统高效管理历史经验。现有方案两类都不划算：全上下文保留完整交互历史，会积累大量低熵噪声（重复日志、寒暄），降低记忆的信息密度，引发中间上下文退化，检索与二次推理的开销都大；另一类用迭代推理在线过滤噪声，检索相关性提高了，但重复推理带来可观的延迟与token开销。两类方案都没实现记忆与计算资源的高效分配。

SimpleMem 的定位是“固定上下文与token预算下提高信息效率”的记忆框架，受互补学习系统理论启发，核心是语义无损压缩，压缩发生在写入时而不是查询时。三阶段管线：语义结构化压缩——由 LLM 本身做语义密度门控，低密度窗口直接丢弃，有内容的窗口在一次生成中完成共指消解、时间归一化并拆成自包含事实单元，再建立稠密语义、BM25 词法、符号元数据三视图索引；在线语义合成——写入时就地合并相关事实，消除碎片冗余；意图感知检索规划——推断查询意图动态决定检索深度，三视图并行检索后取并集去重。LoCoMo 上平均 F1 提升 26.4%，推理时token消耗较全上下文方法最多减少约 30 倍。

![78577067346](C:\Users\yzy14\AppData\Local\Temp\1785770673464.png)

**方法**: 三阶段管线。语义结构化压缩：把对话切成滑动窗口（W=20），由 LLM 本身做语义密度门控——问候、寒暄这类低密度窗口直接跳过，不产出任何事实单元；有内容的窗口在一次生成中完成共指消解（代词换成实体名）、时间戳归一化（“昨天”转成绝对时间）、拆成自包含事实单元，并建立三视图索引：1024 维稠密嵌入、BM25 词法、SQL 符号元数据。在线语义合成：写入时就地合并相关事实，不留给检索时拼凑。意图感知检索规划：推断查询意图动态定检索深度 $k \in [3,20]$，三视图并行检索后取并集去重。

**实验设置**: LoCoMo（200–400 轮、1,986 题）与 LongMemEval-S；骨干含 GPT-4o、GPT-4.1-mini、Qwen 系列；基线含全上下文、ReadAgent、MemoryBank、MemGPT、A-Mem、LightMem、Mem0；指标 F1、BLEU-1、token成本、准确率。

**关键结果**:
- 查询成本压到极低：每查询token从约 16,910 降到 531（约 30 倍），且比 Mem0（约 980）再少 40–50%、比 A-Mem（约 1,200+）少约 79%——高查询量场景收益直观
- 省token的同时精度反超：LoCoMo 平均 F1 43.24（Mem0 34.20、全上下文 18.70），LongMemEval-S 平均准确率 76.87%（全上下文 39.57%）
- 记忆构建几乎免费：92.6 秒对 A-Mem 5,140.5 秒（快约 55 倍），因为用单遍压缩替代逐轮迭代 LLM 调用——冷启动建库成本也很低
- 三个环节各有分工：去语义压缩时序 F1 降 56.7%、去在线合成多跳 F1 降 31.3%、去意图感知检索开放域降 26.6%——删掉任何一环都有对应能力掉点
- 压缩比可达约 30:1：两周多会话的 24,000 token 原始记录压到约 800 token

**观测**: 粒度：轮次级（加写入时压缩）｜观测内容：每查询token、压缩比率（约24000→800）、动态检索深度k、语义密度门控结果｜数据来源：框架运行计量

**动作**: 用观测到的语义密度与查询意图，决定丢弃哪些低信息窗口、动态决定检索深度。

**优化方式**: 语义无损压缩：写入时密度门控丢弃低信息窗口、一次生成完成共指消解与时间归一化，在线合成合并相关事实，意图感知检索规划动态决定检索深度｜所属路线：记忆管理优化（语义无损压缩）

---
### [32] D-MEM: Dopamine-Gated Agentic Memory via Reward Prediction Error Routing

**基本信息**: arXiv:2603.14597 | 预印本 | Yuru Song（加州大学圣迭戈分校）、Qi Xin（卡内基梅隆大学）

**研究内容**: LLM 智能体正从无状态的任务求解器转向可长期交互的有状态系统，其核心支撑是智能体记忆（agentic memory）。早期 RAG 把记忆当成静态、只追加的向量库，检索便宜，但无法处理“新事实使旧观察失效”这类动态变化；一味靠扩展上下文，计算代价大且受“迷失在中间”现象困扰。以 A-MEM 为代表的新一代系统把记忆结构化为动态知识图，持续做节点构建、链接与回溯式记忆进化，是目前的最优做法，但它走“追加并全量进化”的同步管线：每条用户消息不管信息量多少都跑一遍 O(N²) 的记忆进化，带来三个问题——写延迟高、API token成本无界、上下文被低价值闲聊噪声污染。

D-MEM 的定位是可扩展的终身智能体记忆架构，思路取自哺乳动物大脑：腹侧被盖区只在输入打破内部预测（高奖励预测误差）时释放多巴胺、触发记忆巩固，可预期或琐碎的输入被跳过以节省认知资源。它把这个原则映射到智能体记忆：轻量评判路由器实时评估输入的语义惊讶度与长期效用，日常对话（快通道）直接跳过或缓存在 O(1) 快速缓冲区，只有高价值输入（慢通道）才触发 O(N) 的深度记忆进化、重构全局知识图，从而把短期交互与长期认知重组解耦。配套工作包括新基准 LoCoMo-Noise（在长期对话中注入受控闲聊噪声以模拟真实交互）以及零成本的 BM25 混合检索与保留原文的备用缓冲。

**方法**: 评判路由器为每条输入算一个智能体奖励预测误差分数：先看长期价值是否过阈值，不过就直接归零，防止“高惊讶但无价值”的噪声触发进化；β 为保底项，让“有用但意料之中”的输入也能拿到基础分。惊讶度用滑动窗口 Z 分数加 sigmoid 归一化计算，不额外增加 LLM 调用；效用用受限 JSON 模式的轻量 LLM 调用把输入分为瞬时、短期与持久三档，瞬时强制效用为零。按分数分三档路由：低于 0.3 直接跳过；0.3–0.7 只缓存（进 O(1) 短期缓冲区，不触发图进化）；不低于 0.7 才触发完整进化管线。检索侧有两个零成本增强：BM25 混合搜索保住专有名词精度，影子缓冲缓存被跳过的原文，全程不增加 LLM 调用。

**实验设置**: 在 LoCoMo（199 题）与其新造的 LoCoMo-Noise 基准（ρ=0.75，75% 的轮次注入闲聊噪声）上评估，骨干为 GPT-4o-mini，基线是 A-MEM、MemGPT、ReadAgent、MemoryBank 与全上下文；指标 F1/BLEU-1（总体、单跳、时序、开放域、多跳、攻击防御）。

**关键结果**:
- 在噪声多、闲聊多的真实交互下省 token 立竿见影：API token 从 1,648K 降到 319K（减少 80% 以上），写延迟瓶颈从 O(N²) 消除——意味着随消息增长的记忆成本被压成稀疏触发
- 效果不因省而退：LoCoMo 整体 F1 37.4% 对 A-MEM 35.9%，多跳 42.7% 对 27.0%（+15.7pp）
- 过滤噪声要靠"效用"而非单纯"惊讶"：75% 噪声下整体 F1 0.369 对 0.336、攻击防御类 0.412 对 0.388——实现时务必同时乘上长期效用项
- 代价集中在单跳检索：21.6% 对 44.7%，短小事实被当成低效用跳过——使用时可通过调阈值回收这部分
- 门控要避免两个极端：线性奖励预测误差（跳过率仅 9.6%）和过度剪枝（跳过率 68.5%、总体 F1 0.291）都不如当前方案

**观测**: 粒度：会话级｜观测内容：全会话 API token总量（700轮）、每轮输入的语义惊讶度与长期效用评分、三级路由决策比例（真实轮次跳过率53.9%、噪声轮次43.2%）｜数据来源：框架运行计量

**动作**: 用观测到的奖励预测误差（惊讶度×效用），决定哪些对话值得触发记忆进化、哪些直接跳过或缓存。

**优化方式**: 多巴胺门控：用奖励预测误差（惊讶度×效用）判断哪些对话值得触发记忆进化，低价值输入走O(1)快速缓冲、高价值才触发O(N)深度进化，把O(N²)全量进化压成稀疏触发｜所属路线：记忆管理优化（多巴胺门控）

---
### [33] GAMER: Bridging Inference-Time Scaling and Episodic Memory with Action-Centric Graphs

**基本信息**: arXiv:2607.27415 | 预印本 | Xu Zheng、Chaohao Lin、Zhuomin Chen（佛罗里达国际大学）、Weijieying Ren（斯坦福大学）、Haifeng Chen、Wei Cheng（NEC 美国实验室）、Dongsheng Luo（新加坡管理大学，通讯）

**研究内容**: 推理时缩放（CoT、ToT 等反复采样搜索技术）能提升 LLM 的复杂推理能力，但对智能体任务效率低下：搜索过程无状态，每个任务都从零开始，结构相似的问题也要重新探索整个搜索空间，浪费大量token重验无效路径。现有记忆方法解决不了这个问题：AWM、ReasoningBank 等靠 LLM 的推理能力做知识提取和总结，记忆本身的token开销比省下的还多——A-Mem 平均每任务 2.76M token，比原始基线（不带记忆）1.17M 贵一倍多；ReasoningBank 平均 5.26M，是原始基线的 4.5 倍。A-MEM、G-MEM 等记忆增强框架本质是上下文增强而非推理缩放：把记忆当纯检索模块取回事实注入输入，即便取到相关事实，智能体仍要从头重建推理树、生成中间步骤、评估分支，既不剪枝搜索空间也不引导决策。

GAMER 的定位是桥接推理时缩放与情景记忆，把记忆机制从 LLM 推理中彻底解耦：不用 LLM 读写记忆，改用动作中心图和时序差分学习，让记忆几乎零开销。核心贡献有三：一是构造任务特定的动作中心图，把“检索静态文本”变成“查询动态动作价值”；二是用时序差分学习的双向更新机制，从成功轨迹学高价值启发式，从无效动作学严格约束；三是多基准验证，成功率/进度率比普通 best-of-N 提升 15.4%/5.47%。

![78577078419](C:\Users\yzy14\AppData\Local\Temp\1785770784198.png)

**方法**: 先把历史轨迹合并成动作中心图：节点是不同的动作，边是动作间的先后转移。然后在图上跑双流时序差分学习（强化学习中的价值学习方法：用"下一步价值估计与当前估计的差"逐步修正当前价值，不需要等最终结果）：正向流估计“这个动作带来成功的期望概率”，负向流估计失败风险（奖励低于阈值记 −1，否则 0）。更新规则用“下一步价值估计与当前估计的差值”不断修正当前动作的价值；双流就是对“成功”和“失败风险”各跑一遍这一更新。图构建约 0.0013 秒、时序差分学习每任务不到 4 秒，CPU 单线程，零 LLM 调用。推理时双向引导：把历史最优轨迹当 few-shot 模板，按当前节点取 Top-K 高正向价值动作做建议、Top-K 高负向价值动作做规避警告，以自然语言注入提示。理论上证明记忆引导策略使奖励分布一阶随机占优且达到目标奖励所需样本量更小。

**实验设置**: AgentBoard 四个基准（AlfWorld、ScienceWorld、PDDL、Tool-Query）× 四个骨干（Qwen2.5-7B、Llama3.1-8B、Gemma3-12B、GPT-4o-mini）；Best-of-N（N=64），前 32 条轨迹热身建图；基线含 Vanilla、Reflexion、SFS、Voyager、A-Mem、ReasoningBank、AWM 等。

**关键结果**:
- 跨四种环境、四个模型平均提升稳定：成功率 +20.81%、进度率 +6.16%（31.60/60.01 对 Vanilla 26.16/56.53），AlfWorld 上到 53.17%——对"重搜索、多试错"的智能体任务可普遍套用
- 记忆开销几乎为零：比 A-Mem 省约 50% token（1.41M 对 2.76M）、比 ReasoningBank 省约 73%（对 5.26M），记忆读写全程不调 LLM——解决了"记忆比省下的还贵"的问题
- 成本可控可预估：额外token只来自注入的建议/警告（K=3），总量稳定可预测——上线前就能算清 token 预算
- 建议与规避两类注入各有贡献且可叠加：仅建议 53.33→61.67、仅规避 53.33→60.00，组合最优——使用时两类提示都保留

**观测**: 粒度：会话与轨迹级｜观测内容：每推理轨迹的token总量（Best-of-N全程）、每动作节点的建议/规避注入量K、动作价值估计｜数据来源：执行日志与图结构（零LLM调用）

**动作**: 用观测到的动作价值估计（时序差分），决定给当前节点建议哪些高价值动作、规避哪些高风险动作。

**优化方式**: 动作中心图：把历史轨迹合并成动作转移图，用时序差分学习估计动作价值，推理时按Top-K高价值动作给建议、高负向价值给规避警告，记忆读写全程不调LLM｜所属路线：记忆管理优化（动作中心图）

---
### [34] RecMem: Recurrence-based Memory Consolidation for Efficient and Effective Long-Running LLM Agents

**基本信息**: arXiv:2605.16045 | 预印本 | Zijie Dai、Shiyuan Deng、Sheng Guan、Yizhou Tian、Xin Yao、Xiao Yan、James Cheng（香港中文大学、华为云、华为理论实验室、北京邮电大学、武汉大学）

**研究内容**: 长时运行智能体的记忆系统（Zep、Mem0、A-Mem、MemoryOS 等）都采用最直接的做法：每来一条交互，就调用 LLM 抽取事实、合并进已有记忆。这保证信息不丢，但记忆构建的token成本巨大——在 LoCoMo 上，Mem0 构建要 1,520.8K token、A-Mem 要 1,459.9K——让记忆系统在实践中用不起。

论文论证“每轮都固化”是过度的：有些交互信息量少或含噪声，有些交互与已有内容无关、直接查询原文就行。认知科学的多存储理论与互补学习系统框架指出：孤立经验停留在瞬时存储里，只有重复出现的模式才固化为稳定长期记忆。RecMem 据此提出“基于复发的固化”：只有观察到语义相似交互持续复发，才调用 LLM 抽取情节记忆与语义记忆，其余交互留在零 LLM 成本的潜意识层。实验效果：构建token最多降 87%，准确率反而超过三个现有记忆系统。

**方法**: 三层记忆架构，按成本从低到高。潜意识层（零 LLM 成本）：新交互单元（用户消息+助手回复+时间戳）用轻量稠密编码器转成向量、连同原文存入向量库——全程不调用 LLM。复发触发：新单元对潜意识层检索 top-k 相似单元，过滤出余弦相似度达标的集合；只有集合大小达到阈值才触发固化（默认相似度 0.7、数量 5），不满足就留在潜意识层，等后续交互凑齐复发。情节层：新单元先尝试与最近邻情节“就地合并”防止碎片化；无法合并就等复发触发，把簇内单元按时间戳排序，由 LLM 以归纳组织方式生成连贯叙事。语义层：新情节生成后并行做两件事——细节恢复（扫描原文，找回被摘要压缩掉的关键事实）与事实维护（过滤与已有事实重复的信息、更新演变中的用户状态）；事实按四类约束独立存储，保持检索特异性。

**实验设置**: LoCoMo（1,540 题、10 条多会话对话，平均 16K token）与 LongMemEval-S（500 题、500 条任务导向对话，平均 115K token）；后端 GPT-4o-mini 与 GPT-4.1-mini；基线 Full Context、Naive RAG、Mem0、A-Mem、MemoryOS；指标为问答准确率（LLM 判官）加两阶段token成本（构建成本按会话平均、查询成本按问题平均），所有基线统一加装token追踪记录。

**关键结果**:
- 省下的是大头、精度几乎不损：LoCoMo 上构建token 193.2K 对 Mem0 1,520.8K（−87.3%）、对 A-Mem 1,459.9K（−86.8%，约 7.8 倍降幅），而 81.10 仍是记忆类方法最高（Mem0 62.92、A-Mem 68.83），只比全上下文（84.18）低一点
- 第二个基准更彻底：LongMemEval-S 上 76.80 连 FullContext（66.20）都超过，构建token 365.49K 对 Mem0 1,626.54K（−77.5%），时态推理类增益最大（68.42 对 MemoryOS 54.89）——"复发才固化"不只是省成本的权宜之计
- 三层缺一层就明显掉点：去潜意识层 81.10→51.88（最大降幅）、去语义层→70.58、去情节层→79.94——潜意识层（零成本存储+复发触发）是保效果的关键
- 触发阈值是可用旋钮：直接调节"信息保留 vs 构建成本"，且对性能不敏感——实现时不必为阈值过度调参

**观测**: 粒度：调用级（兼会话轨迹级）｜观测内容：每次LLM调用的输入+输出token、语义相似复发检测、构建/查询两阶段成本｜数据来源：框架运行计量

**动作**: 用观测到的语义复发信号（相似单元数量达到阈值），决定是否触发LLM固化记忆、其余交互留在零成本的潜意识层。

**优化方式**: 复发驱动固化：只有语义相似交互持续复发才调用LLM抽取记忆，其余留在零LLM成本的潜意识层，把"每轮都固化"改成"语义复发才固化"｜所属路线：记忆管理优化（触发时机）

---

### [35] PABU: Progress-Aware Belief Update for Efficient LLM Agents

**基本信息**: arXiv:2602.09138 | 预印本 | Haitao Jiang、Lin Ge、Hengrui Cai、Rui Song

**研究内容**: LLM Agent 通常基于完整的动作-观测历史来决策，但全量历史引入大量与任务无关的信息，容易导致冗余动作与更高的推理成本。论文提出 PABU（Progress-Aware Belief Update），一个信念状态框架：把 Agent 的状态压缩表示为显式的任务进度建模加上对过去动作与观测的选择性保留。每一步，Agent 用两个预测头估计自上一轮以来的相对进度，并判断新遇到的交互是否值得存储，后续决策只基于被保留的子集，丢弃无关历史。

在 AgentGym 基准的 8 个环境中、使用完全相同的训练轨迹，PABU 达到 81.0% 的任务完成率，超过使用全量历史的此前最优方法 23.9%；其进度导向的动作选择还把平均交互步数降到 9.5，减少 26.9%。消融实验表明，显式进度预测与选择性保留对稳健的信念学习都必不可少——去掉任何一个都会明显掉点。

**方法**: 每步两个预测头（相对进度 + 保留二值）驱动信念状态门控：进度头输出 0–100% 的自上一轮相对进展，保留头输出"是否值得记住当前交互"的二值决策；后续动作只条件于被保留的子集，丢弃无关历史。

**实验设置**: AgentGym 基准 8 个环境；与使用相同训练轨迹的全量历史基线及此前 SOTA 对比；消融两个预测头。

**关键结果**:
- 用更少步数达到更高完成率：平均步数减 26.9%（降到 9.5 步），任务完成率 81.0%，超过用全量历史的 SOTA 23.9%——丢历史不仅省成本，还让决策更聚焦
- 消融：显式进度预测与选择性保留缺一不可，去掉任何一个都明显掉点——两个预测头都要保留

**观测**: 粒度：步骤级｜观测内容：自上一轮以来的相对进度（0-100%）、当前交互是否值得记住（二值门控）｜数据来源：运行时预测，不额外调LLM

**动作**: 用观测到的相对进度与"是否值得记住"二值信号，决定把哪些交互写入信念状态、丢弃其余历史。

**优化方式**: 进度感知信念更新：基于进度预测做选择性上下文保留，只保留值得记住的交互，缩短有效上下文｜所属路线：记忆管理优化（选择性保留）

---

### [36] Beyond Compaction: Structured Context Eviction for Long-Horizon Agents

**基本信息**: arXiv:2606.11213 | 预印本 | Andrew Semenov、Svyatoslav Dorofeev（Kiz8）

**研究内容**: 长程智能体——编码助手、研究智能体、工具使用工作流——面临一个结构性问题：上下文窗口有限，而工作轨迹无界。每个工具调用、每次文件读取、每个检索文档都会累积进历史，上下文一旦超过预算，就面临"压缩还是截断"的取舍：基于摘要的压缩会丢失信息、破坏因果结构、产生幻觉，且压缩本身要调用模型；按时间截断则不分青红皂白地丢掉最早的内容，即使它们仍然相关。

论文提出上下文窗口生命周期（CWL），一种结构化的上下文驱逐方案：Agent 在推进过程中把轨迹标注为带类型、带依赖链接的片段（episode），当 token 预算被超过时，一个确定性的、不调 LLM 的驱逐策略在该结构内按优先级驱逐内容。CWL 保留用户轮次与 Agent 正在积极推理的探索上下文，同时激进地卸掉"效果已在环境中持久化"的动作片段，使活动上下文稳定在注意力退化与幻觉上升的临界区之下。相比基于摘要的压缩，CWL 避免了不可预测的信息丢失、破坏因果结构、阻塞性模型成本与压缩诱发的幻觉四个缺陷；相比按时间截断，CWL 按依赖图丢弃"最老且最可恢复"的内容。实验演示了一个单智能体会话完成 89 个连续任务、跨越 80M token，任务准确率相对每任务独立会话无可测退化。

**方法**: 执行期间维护 episode 依赖图（typed + dependency-linked）：每步标注类型（用户指令/工具调用/环境观察/推理）与依赖关系（效果是否已持久化）；超预算时按优先级确定性驱逐，优先删"已完成且效果已持久化"的步骤；全程 LLM-free。

**实验设置**: 80M token 连续会话（89 个任务）；对照基于摘要的压缩与基于时间的截断。

**关键结果**:
- 单会话可稳定撑到 80M token：一个会话连续完成 89 个任务，准确率相对"每任务独立会话"无可测退化——按依赖图驱逐能长期稳住上下文而不牺牲效果
- 相比压缩方案的优势落在四个硬缺陷上：避免错误摘要、信息丢失、因果结构破坏与压缩诱发的幻觉，且驱逐策略全程不调 LLM——对"效果必须可回滚、因果不能断"的工具调用类长程任务，驱逐优于压缩

**观测**: 粒度：会话级｜观测内容：上下文窗口占用率、每步类型（用户指令/工具调用/环境观察/推理）与依赖关系（效果是否已持久化）｜数据来源：运行图维护，不调LLM

**动作**: 用观测到的每步类型与依赖关系（效果是否已持久化），决定驱逐哪些已完成步骤、保留哪些活跃上下文。

**优化方式**: 依赖感知驱逐：优先删除"已完成且效果已持久化"的步骤，保留用户消息与当前活跃推理上下文｜所属路线：记忆管理优化（上下文驱逐）

---

### [37] Learning What Not to Forget: Long-Horizon Agent Memory from a Few Kilobytes of Learning

**基本信息**: arXiv:2606.20954 | 预印本 | Nusrat Jahan Lia、Aritra Mazumder

**研究内容**: 长期运行的语言模型系统会积累超出上下文窗口的交互历史，必须持续驱逐；一旦驱逐策略丢掉"承重"细节（例如登录时签发的访问 token、下一步调用需要的路径），后续动作就会失败。论文指出，智能体记忆的驱逐本质上是一个保真度问题：未来查询不可知、精确状态至关重要，因此需要可部署的、主动的驱逐策略。论文提出 LRE（Learned Relevance Eviction），一个只有几 KB、纯 CPU、不调语言模型的评分器，学习哪些历史单元是"承重的"（删除会导致后续失败），并用逐字提取的方式保留它们。

在匹配预算的比较下，没有基线能在准确率-成本平面上支配 LRE：在智能体任务上，LRE 整体匹配"保留全部历史"的准确率；在最简单任务上超过不驱逐基线 27%，同时零 compressor 调用、峰值上下文最多减少 52%。受控案例中 LRE 完成那些其他策略会陷入循环的任务，其中一个任务比"保留一切"少 37% 的调用，并解决 14 个其他运行策略都无法完成的任务。在对话记忆上，LRE 以零神经代价超过稠密编码与 token 剪枝编码；下游评估中，LRE 在 LoCoMo 上读少 68% 的 token 给出最好的预算内答案质量；其监督还可以注解无关——只用系统自身行为训练即可恢复有监督评分器 95% 的效果。

**方法**: KB 级 CPU 逻辑回归评分器（causal features，仅依赖前缀，不依赖后续内容）；annotation-free 训练（从历史中自动标注哪些块是承重的）；用背包选择（knapsack）在上下文预算内决定保留哪些单元，保留方式为逐字提取。

**实验设置**: 长程 Agent 任务（含受控失败案例）与对话记忆；匹配预算对比多种驱逐策略与"保留全部历史"基线。

**关键结果**:
- 上下文可以压缩近半而不丢准确率：峰值上下文最多减少 52%，整体准确率仍匹配"保留全部历史"，且全程零 compressor 调用——意味着驱逐只挑"承重"块，不用额外模型开销
- 主动驱逐反而能救回其他策略陷入死循环的任务：最简单任务上比不驱逐基线高 27%；受控案例里少 37% 的调用就完成任务，还有 14 个其他策略全部失败而 LRE 能完成的任务

**观测**: 粒度：会话级｜观测内容：每个上下文块的大小/峰值、其因果特征（仅依赖前缀）、删除后是否导致后续失败（承重性）｜数据来源：annotation-free训练 + 运行时CPU评分

**动作**: 用观测到的历史块的load-bearing评分（删除会导致后续失败），决定在上下文预算内保留哪些块。

**优化方式**: load-bearing驱逐：识别承重历史块，背包选择在上下文预算内保留最高价值历史，零compressor调用｜所属路线：记忆管理优化（上下文驱逐）

---

## 七、记忆与技能的可执行化

### [38] PRO-LONG: Programmatic Memory Enables Long-Horizon Reasoning

**基本信息**: arXiv:2607.20064 | 预印本 | Alexis Fox、Junlin Wang、Paul Rosu、Bhuwan Dhingra（杜克大学）

**研究内容**: 长程探索任务（如 ARC-AGI-3 游戏）要求智能体记住几十上百步的环境历史、持续感知与推理，裸模型在无限上下文里也会退化（上下文腐烂），在 ARC-AGI-3、NetHack 等持续学习基准上近乎零分。现有框架都押注一种上下文管理策略，即决定“写入什么”与“读取什么”，主流可归为“总结式记忆”，但保存越多、检索越难：总结有损，一次错误压缩可能很多轮之后才暴露，长程上这类决策的代价还会累积。论文同时注意到编码智能体的快速进展——编程式访问让对超长日志的结构化检索成为可能。

PRO-LONG 的主张极简：把记忆定义为“程序化记忆”，写是只追加、从不总结，把每个观察、动作和结果原样追加进日志；读是用 grep、正则或 Python 代码对该日志做编程式检索，不用向量库，也不做任何启发式取舍。三条设计原则支撑这一主张：简单（写入无需学习或启发式决策，读取无需专门检索）、无损（日志是环境状态的忠实记录）、与编码智能体天然契合（编程检索对十万行以上的日志仍可负担）。它甚至不是新框架，只是给标准编码智能体加一层最小记忆：与基座编码智能体相比平均提升 18.0 个百分点，追平甚至超过专门设计的世界模型框架，而计费token只有后者的 4.2–5.8 分之一。

**方法**: 写等于全部追加：每个动作往日志文件追加一条结构化记录，不做总结，日志最长超过 32 万行。读等于编程式检索：只用 grep、正则、Python 工具搜日志，不用向量库；提示词约 30 行，不派子智能体。论文还给出缓存感知计费公式：输出token单价是输入的好几倍、缓存读取最便宜，“多写日志”看似浪费，实际因缓存命中反而便宜：让 LLM 做总结要重读上下文，用代码 grep 则几乎零成本。核心洞察：要不要存、怎么整理的决定，推迟到查询时用代码做，而不是写入时用 LLM 做。

**实验设置**: ARC-AGI-3 公共游戏集（25 个游戏，规则不公开，每游戏 500 步动作预算、每轮最多 20 个动作），在 GPT-5.5（Codex 框架）、Opus 4.6、Fable 5（Claude Code）上评估；基线上比“无日志”裸智能体，下比最强专用框架（WorldModeler、Schema、Arcgentica），全部按同一预算与评分规则重打分。

**关键结果**:
- 只加一层"只追加日志＋编程检索"的记忆就能显著提精度：三个模型家族比无日志基线平均高 18.0 个百分点（区间 15.7–21.0）
- 与最强专用框架比，近乎无损还便宜得多：计费 token 只有后者的 4.2–5.8 分之一——Codex 上 pass@1 41.2%（WorldModeler 45.1%，但 token 是其 5.8 倍），全套游戏约省 4 亿 token
- 预算与成本透明可算：Fable 5 在 2,000 步预算下 94.6% pass@1 只需 1,500 美元；97.4% best@2 总成本 1,750 美元，而报 99.0% best@2 的 Schema 要 6,447 美元——差距主要来自 token 用量而非能力
- 能力阶梯主要由检索手段贡献：GPT-5.5 上只读 23.1% → 加 grep 27.2% → 加 Python 38.3% → 加 write 41.2%；清空工作区几乎不影响结果——所以配齐工具（尤其 Python 检索）比保持环境状态更关键

**观测**: 粒度：动作级（每动作一条日志）＋调用级｜观测内容：每个动作的结构化记录、每次LLM调用的输入/输出/缓存读/缓存写四类计费token｜数据来源：日志追加 + 缓存感知计费折算

**动作**: 用观测到的日志检索结果决定"要不要存"——把存储决策推迟到查询时，用grep/Python检索而非LLM总结重读上下文。

**优化方式**: 无损程序化记忆：日志只追加不总结，读取时用grep/正则/Python检索，把"要不要存"的决定推迟到查询时，避免LLM总结重读上下文的开销｜所属路线：记忆与技能可执行化（程序化记忆）

---
### [39] FlowEvo: Self-Evolving Agents through the Co-Evolution of Workflows and Executable Skills

**基本信息**: arXiv:2607.21596 | 预印本 | Zeyu Ren、Ling Yue、Ran Li、Yishu Wang、Shengxiang Xu、Hanmo Liu、Shaowu Pan、Shimin Di（东南大学、伦斯勒理工学院、香港科技大学）

**研究内容**: 基于 LLM 的智能体越来越多地在推理时动态构建工作流（任务分解、工具调用、代码生成、中间检查与修复），但一次回合中发现的有用流程往往是“一次性的”：解决了当前任务就丢了。这带来双重代价：智能体会在相似任务上反复重新发现同样的例程，增加token开销与方差；成功工作流中的可复用操作结构——分解模式、代码骨架、验证步骤、修复策略——只以瞬时轨迹存在，后续任务仍基本从零开始。已有工作只覆盖局部：文本式轨迹记忆能指导后续生成，但只描述行为、不能直接执行；可复用工具与技能研究又常面向固定任务分布，或与智能体自己的求解轨迹脱钩。

FlowEvo 把自我进化定义为推理时能力层的改变，形成“工作流→技能→工作流”反馈循环：成功轨迹被编译成技能记录（可调用组件配结构化辅助指导，准入时做接口、重放与安全检查）存入持久技能库；检索到的技能以两种互补方式影响后续任务——直接执行（校验过的组件被当作子程序）与技能条件生成（只注入紧凑的结构化视图辅助规划），另用对比策展自动停用持续负迁移的技能。对比之下，Reflexion 这类自反思方法只在单个任务上重试，AFLOW/ADAS 这类工作流搜索方法用固定多阶段管道处理所有任务，在“任务结构重复出现”和“每个任务各不相同”两类场景各自表现不佳；FlowEvo 的两种复用模式正好分别覆盖两个场景。

**方法**: 技能表示成五元组：可执行体与入口点、可调用接口、回放式测试集、元数据（来源、任务模式、复用约束、回退规则、路由统计）与生命周期状态。新技能过三个准入检查：接口合规、重放测试通过、安全合规（禁用危险导入与调用），失败者降级为仅上下文使用或阴影状态。路由三种模式：动态生成（无技能时正常推理）、直接执行（完整重放）、技能条件生成（只注入紧凑的结构化视图），并跟踪使用统计与负向证据。对比策展：周期性比较“用这个技能”与“扣留”的成功率之差，持续为负（阈值 −0.1）就自动抑制，防负迁移。执行默认单遍生成，验证失败才走升级阶梯：小集成 → 热重试 → 冷重试 → Reflexion。

**实验设置**: 全部用 GPT-4o-mini 作固定基座，不更新参数。三个基准：ALFWorld（134 个评估任务、6 种任务类型、每回合最多 50 步）、HumanEval（164 题）、GSM8K（1,319 题）；与 Reflexion、ExpeL、ADAS、AFLOW 四个基线按同一 API 配置对比；每个基准从空技能库独立开始。

**关键结果**:
- 技能复用能同时提精度和降成本：ALFWorld 上 82.8% 成功率，比最强基线 AFLOW（59.2%）高 23.6 个百分点，每回合 12,267 token，不到最省基线 ADAS（29,671）的一半
- 编程与数学任务上精度不输、token 大省：HumanEval 95.1% pass@1、880 token/任务（AFLOW 94.7% 但要 3,826）；GSM8K 97.1%、541 token/题（ADAS 2,820）
- 收益主要来自"技能反馈"这一环：ALFWorld 上 ReAct 33.6% → +技能编译 38.8% → +技能反馈 80.6% → +策展 82.8%——编译本身只涨约 5 个点，把编译出的技能注入推理才带来最大提升
- 复用在真实运行中确实发生：134 个回合中 101 个直接复用技能，复用成功率 98%（99/101）；第一个技能约在回合 10 出现，此后 token 急剧下降——所以冷启动约 10 回合后开始见效
- 策展机制能自己发现负迁移：自动识别并抑制 pick_two 任务类型的技能（用/不用对比 delta −0.23，低于停用阈值 −0.1）

**观测**: 粒度：会话与轨迹级｜观测内容：一次完整回合的所有token累加均值、技能命中率与复用成功率、负迁移证据（用/不用对比）｜数据来源：框架运行计量

**动作**: 用观测到的技能复用成功率与负迁移证据（用/不用对比），决定启用或停用哪些编译技能。

**优化方式**: 工作流转技能编译：成功轨迹编译成可调用技能入库，后续任务直接执行或仅注入结构化视图；用/不用对比策展自动停用负迁移技能，默认单遍执行、失败才升级｜所属路线：记忆与技能可执行化（技能编译）＋执行粒度控制

---
### [40] SkillSmith: Compiling Agent Skills into Boundary-Guided Runtime Interfaces

**基本信息**: arXiv:2605.15215 | 预印本 | Duling Xu、Zheng Chen（通讯）等（上海 AetherHeart Tech、中国人民大学、加州大学圣迭戈分校）

**研究内容**: 基于 LLM 的智能体通过推理、规划、工具调用与迭代执行求解任务，技能则把开发者对某一类任务的处理经验打包成可复用资源（SKILL.md 指令文件加脚本、模板、示例等）。在现有框架中，技能一旦与当前任务匹配，就以指令上下文的形式被整体注入 ReAct 式推理循环，由模型现场解读并规划执行步骤，由此产生两种可测冗余：每次执行平均加载约 17.8K 源token，其中 51.21% 与本次执行无关；不同任务复用同一技能时，推理轨迹平均相似度达 45.5%——模型在反复重新解读同一份指令、重建相似的执行策略。论文认为，两种冗余的共同根源是：现有技能系统没有把“可以预先做好的部分”与“必须留给模型现场理解的部分”清晰分开；而现有做法要么把技能原文全量注入上下文，要么编译成通用的统一工作流 IR，但自然语言技能包在结构、语义和操作形式上高度异构，难以可靠地统一成单一的中间表示，两种方案都不够贴合技能本身的结构。

论文提出 SkillSmith，一个“边界优先”的编译-运行时框架：离线把技能包编译成显式运行时边界契约，只保留可操作、可执行的最小接口（操作符、输入要求、策略约束、验证证据与回退路径）。运行时按需披露：智能体先看到一个紧凑的技能入口与边界摘要，选中后才暴露详细内容，共享的运行时组件选择操作符、检查策略，安全时直接执行，不合适时给指导，需要推理时再回退到无损保留的原始包；强模型编译出来的组件还可被更小、更高效的运行时模型复用。整体思路是把重复理解变成直接调用，在 SkillsBench 代表性任务上显著减少token消耗、推理迭代与解决时间，同时保持甚至提升任务成功率。

**方法**: 编译期先按“源形状”分四类：workflow 编译成带依赖的步骤图，dispatcher 编译成类型化操作符由智能体动态选择，reference 编译成可检索的索引段落，insufficient 不做编译、运行时回退原始包。最终产物统一是边界契约，含类型、操作符、IO 契约、风险、验证证据、策略与回退元数据，描述“技能在什么边界内、按什么策略、可安全替模型做什么”。运行时按需披露：先只给模型一个紧凑的 run_skill 入口，调用时才决定阻止、给参考还是执行；原始包放 capsule 里随时可取回（无损降级），编译产物只是入口。编译是一次性成本，每个组件约 3,104 token、13.22 秒。

**实验设置**: 基准 SkillsBench（87 任务、227 技能包），按难度分层选 7 个代表任务；宿主框架 AGENT-H、Codex、OpenCode；运行时模型为 GPT-5.5、Claude Opus 4.7、DeepSeek V4 Flash、Qwen3.6 35B A3B；基线为 Raw-Skills 和 SkVM-Compiled Skills；每项跑 5 次。

**关键结果**:
- 边界契约编译能把执行成本砍掉一半以上：GPT-5.5 上解决阶段 token 从 1.5M 降到 620K（−57.44%），时间 999 秒→494 秒（约 2 倍加速），思考迭代 107→61（−42.99%）
- 比通用工作流 IR 更省：相对 SkVM 编译，token 少 46.49%、时间少 47.04%——说明"贴合技能自身结构"比"统一成 IR"更划算
- 强模型编译的组件可被多个模型复用：4 个模型共用后平均 token −32.83%、时间 −38.33%；DeepSeek V4 Flash 反而在 3 个原本失败的任务上成功——所以小模型可直接借用强模型编译的接口，还能顺带救回失败任务
- 注意小模型有个别回归：编译产物会固化错误假设——部署时要用验证证据分级与 capsule 回退（无损取回原始包）兜底

**观测**: 粒度：阶段级（兼每次调用）｜观测内容：整个解决阶段的所有调用token、每次执行加载的源token、跨任务轨迹相似度｜数据来源：框架运行计量

**动作**: 用观测到的技能边界契约与调用上下文，决定运行时披露多少细节——直接执行、给参考还是回退到原始包。

**优化方式**: 边界契约编译：技能包编译成最小可执行接口（操作符/输入要求/策略约束/验证证据/回退路径），运行时按需披露——先给紧凑入口与边界摘要，选中后才暴露细节，把"现场读一遍再理解"变成"直接调用"｜所属路线：记忆与技能可执行化（边界契约编译）

---
### [41] ACM: Agentic Context Management for Long Horizon Tasks

**基本信息**: arXiv:2607.23809 | 预印本 | Xiaochuan Li、Ryan Ming、Meng Chu（CMU）、Shuai Shao、Rong Jin（Meta）、Chenyan Xiong（CMU）

**研究内容**: 长程智能体任务的轨迹天然冗长且含噪：长工具输出、失败尝试、冗余观察交织在一起，历史很快超过有效上下文容量（即使名义窗口达百万token）。已有压缩路线分两类，各有缺陷：启发式压缩由外部模块或固定规则控制压缩时机，与模型自己的推理过程不对齐；记忆增强路线跨任务积累经验，但不压缩单个回合内的工作上下文。

ACM 受人类短时/长时记忆分离启发，给智能体两个专用工具：管理上下文把消息压成摘要、原始文本保存到磁盘，查询记忆按需回查原文——无损（原始消息不丢）+ 自触发（什么时候压缩由智能体自己决定）。更进一步，用教师-学生双约束后训练管道，把“什么时候该压缩、什么时候不该压缩”内化进模型。指标上用“峰值token数”衡量成本（对应 KV 缓存压力与推理负担），而不是累计消耗。

**方法**: 两个工具的用法。管理上下文把自上次压缩边界以来的消息交给摘要器 LLM，压成不超过 4096 token的记忆块（docid、实体、日期、数字原样保留），原始消息保存为磁盘文件，工具返回带标识前缀的摘要；查询记忆加载对应原始消息，由查询器 LLM 抽取与查询相关的内容返回。系统提示和原始问题始终保留。训练数据用“双约束教师-学生”管道生成：学生模型在无工具与有工具两种条件下跑出完整轨迹；教师模型对每条轨迹发两类指令之一——“该压缩”指令（在无工具轨迹里找应该压缩或回查的最早点）或“修正”指令（在有工具轨迹里找过早或不必要的压缩调用，替换成更产出的动作）；学生在标注动作处继续跑轨迹，教师把每个位置“下一个token的概率分布”作为训练信号，做在线策略蒸馏训练 3 个 epoch，并做拒绝采样与防答案泄露内容过滤。

**实验设置**: BrowseComp-Plus（680 训练、150 评测）、DeepSearchQA（域外实时网络搜索）、SWE-Bench Verified；学生/策略/摘要器/查询器 Qwen3.5-9B，教师 Qwen3.5-397B-A17B；基线 ReAct、ReSum（固定阈值触发+丢弃原文）、ACON（固定槽位放摘要）、ACE（跨任务经验簿），以及 Qwen3.5-397B、Gemini3-Flash 两个强模型；上下文上限 128K；指标 Pass@1、工具调用次数、峰值token。

**关键结果**:
- 训练模型主动管理上下文，能同时提精度和降峰值：Qwen3.5-9B 上对比 ReAct，BrowseComp-Plus Pass@1 0.570→0.727（+27%）、峰值 token 63K→54K；DeepSearchQA +16%、SWE-bench Verified +8%；峰值 token 降约 20%
- 9B 小模型的精度逼近 40 倍更大的模型：BrowseComp-Plus 0.727 对 Qwen3.5-397B-A17B 的 0.653，SWE 0.530 对 0.682——成本敏感场景值得用它替代超大模型
- 训练数据要"专用标注＋成功轨迹"配合：单独蒸馏成功轨迹不够，两者互补后 0.734 最优
- 光给工具不训练不会自动用：未训练的 GPT-5.5 每问题平均只管理上下文 0.1 次，训练后 6.7 次——"该不该压缩"要靠后训练内化，不能只靠暴露工具
- 压缩时机远早于上下文上限：83 轮任务峰值实际/原始上下文为 98K/222K，省约 124K；压缩呈锯齿状发生——说明是"该压就压"，而不是顶到窗口边再压

**观测**: 粒度：会话轨迹级峰值（辅以动作级）｜观测内容：平均每回合峰值token（与上下文上限对照）、工具调用次数、何时触发压缩｜数据来源：框架运行计量

**动作**: 用观测到的上下文占用与工具调用，决定何时压缩、何时回查——压缩/回查被封装成工具，由智能体自己决定何时调用（训练时手段）。

**优化方式**: 把"压缩/回查"封装为可执行工具技能，用教师-学生双约束后训练让智能体学会何时压缩——属训练时手段｜所属路线：记忆与技能可执行化（兼记忆管理、训练时效率塑造）

---

## 八、路由与调度

### [42] RCR-Router: Efficient Role-Aware Context Routing for Multi-Agent LLM Systems with Structured Memory

**基本信息**: arXiv:2508.04903 | AAAI 2025 | Jun Liu、Zhenglong Kong、Changdi Yang 等（卡内基梅隆大学、东北大学、哈佛大学、富士通美洲研究院、MIT、北京大学、佐治亚大学等）

**研究内容**: 多智能体 LLM 系统把规划、搜索、总结等角色分给专门智能体，在共享任务上按结构化工作流协作，是复杂开放式问题求解的候选范式。但现有架构的上下文管理很粗放，常用两种策略：静态路由按预定义模板给每个智能体固定输入，不随任务演进；全上下文路由每步把完整记忆或交互历史塞给所有智能体。两种做法实现简单、有一定效果，但缺陷明显：token消耗过大、处理大量冗余或无关信息、难以适应不断变化的任务需求，任务规模一大就拖累协调质量和系统效率；而且既没有结构化动态记忆路由，也不做token级上下文预算约束。

本文提出 RCR-Router，一个模块化、角色感知的上下文路由框架。作者称这是第一个在严格token预算下、按智能体角色和任务阶段动态选择语义相关记忆子集的方案：轻量评分策略（支持人工规则和学习型模型两种变体）指导记忆选择，智能体输出迭代写回共享记忆库，上下文随交互轮次渐进细化；架构由预算分配器、重要性评分器和语义过滤路由三个组件构成，可无缝接入现有多智能体工作流。为更好评估模型行为，论文还提出回答质量分数，在标准问答正确率之外捕捉 LLM 生成解释的质量。

**方法**: 三个组件。预算分配器给每个智能体发每轮预算，预算为基础值加角色增量，例如规划者 1500、搜索者 1000、推荐者 800。重要性评分器给每条记忆条目打分，三个信号加权：角色相关性（关键词匹配）、任务阶段优先级、新近度（指数衰减）。语义过滤器按分数降序取条目，直到预算用尽。每轮结束后把智能体的结构化输出（事实、工具结果、推理步骤）抽取、去重、结构化后写回共享记忆库，下一轮就能路由到更相关的内容，上下文随轮次渐进细化。

**实验设置**: 三个多跳问答基准（HotPotQA、MuSiQue、2WikiMultihop）加两个交互环境（ALFWorld、WebShop），重构为规划者、搜索者、推荐者三智能体分解任务；默认每智能体预算 2,048 token；对比全上下文和静态路由两种基线；指标含运行时间、token消耗、回答质量、P/R/F1。

**关键结果**:
- 角色感知路由普遍省 token：HotPotQA 减 26%（5.10K→3.77K）、MuSiQue 减 11.3%（13.41K→11.89K）、2WikiMultihop 减 47%（2.34K→1.24K）、ALFWorld 减 35.6%、WebShop 减 38.3%
- 省 token 的同时质量不降反升：HotPotQA 回答质量 4.17→4.91（5 分制）、F1 73.7→82.4，运行时间 150.65s→93.52s
- 参数不必顶格设：预算 2048 后质量饱和（4096 几乎不涨）、迭代轮数 K=3 最优（质量最高且 token 最少，K>3 递减）——默认用 B=2048、K=3 即可，不必扫参
**观测**: 粒度：智能体级兼轮次级｜观测内容：每个智能体每轮的token预算与实际消耗（prompt+response）、记忆条目的角色/阶段/新近度评分｜数据来源：框架运行计量

**动作**: 用观测到的记忆条目的角色/阶段/新近度评分，决定每智能体每轮注入哪些记忆子集、写回哪些结构化输出。

**优化方式**: 角色感知路由：预算分配器给每智能体每轮发预算，重要性评分器按角色相关性、阶段优先级、新近度打分，语义过滤器在预算内取分数最高的条目，输出迭代写回共享记忆库｜所属路线：路由与调度（角色感知路由）

---
### [43] LatencyAwareRouting: Beyond Accuracy and Cost — Latency-Aware LLM Query Routing for Dynamic Workloads

**基本信息**: arXiv:2607.18253 | 预印本 | Shivam Patel、Akaash R. Parthasarathy（卡内基梅隆大学）、Ankur Mallick（微软）、Gauri Joshi（卡内基梅隆大学）

**研究内容**: 在模型池上路由查询是高效利用 LLM 的常见做法：简单请求给便宜的小模型，难题给贵的大模型。但此前的路由器只按精度和成本选模型，完全不管生成延迟，可能把请求送到过载实例，造成长排队延迟、违反延迟约束。论文指出这源于两个层面脱节：精度-成本感知的查询路由器是延迟无关的，而系统层的负载均衡（轮询、最短队列等）固定模型选择、不了解精度成本。延迟本身很难估：它不仅取决于 prompt 长度，还取决于实例当前的预填充/解码工作量构成和批处理策略——现代服务框架用连续批处理并发执行请求，预填充并行算、解码顺序生成，负载构成不同，吞吐就不同。

这篇论文的定位是补上“精度成本感知路由”与“系统级延迟优化”之间的空隙，把精度、成本、时延放进同一个路由决策。核心贡献是轻量延迟估计器 SFS：模拟服务框架自回归token批的组成演变，逐批推进直到新查询生成第一个输出token，累加各批估计时间得首token时间；另配一个无实时负载信息时用的排队论估计器。在此之上做联合精度-成本-时延优化，同等延迟水平下精度-成本效用最高提升 40%。

**方法**: SFS 模拟服务框架的token批组成演化：每批里每个活跃序列最多贡献一个解码token；把新查询放入待处理集合后逐批推进，直到它生成第一个输出token，累加各批估计时间即得首token时间。批处理时间用带四个系数的线性模型估计，覆盖固定开销、token数量、上下文与预填充注意力四类成本。路由目标：对每个请求，只在首token延迟不超目标 $\tau$ 的实例里，选“预估精度减 $\lambda$ 倍成本”最大的；$\lambda$ 大偏向省钱，$\lambda$ 小偏向质量。所有实例都超时延目标时就选延迟最低的；无实时负载信息时改用受限处理器共享排队模型做平均估计。以拉格朗日松弛（把约束折进目标函数求解的经典优化方法）刻画效用-延迟权衡曲线。

**实验设置**: 四个任务（Alpaca、HotpotQA、GovReport 摘要、WritingPrompts）各采样 2.5K 条查询，覆盖长短输入输出组合；Qwen3-0.6B/8B 各一块 H100、Qwen3-32B 用两块 H100，框架用 vLLM，质量由 Gemini 3.1 Pro 打分；到达过程为泊松与两态马尔可夫调制泊松；基线是轮询、最短队列和忽略延迟的纯效用路由。

**关键结果**:
- 把时延纳入路由决策能显著提效用：相同延迟水平下比最短队列高最多 40%，按时效用曲线 AUC 比最优基线高 33%，5 qps 时最高提升 46%
- SFS 对首 token 时间的估计很准：平均绝对百分比误差低于 5%，而按吞吐量估计的旧方法高达 85%；批处理时间模型误差约 4%——所以这套估计可直接用于路由决策
- 决策开销可忽略：每次路由仿真约 0.139ms，相对秒级首 token 时间微不足道——可对每个请求都做，无需缓存或降频
- 突发流量下依然一致领先——延迟敏感且负载动态的服务可直接采用

**观测**: 粒度：请求级（兼token批级）｜观测内容：每个实例的首token时间（服务框架仿真估计）、预填充/解码token工作量、延迟与效用｜数据来源：框架仿真 + 运行时负载

**动作**: 用观测到的每个实例的首token时间估计（服务框架仿真），决定把查询路由到哪个实例、是否满足时延约束。

**优化方式**: 延迟感知路由：用SFS仿真估计每个实例的首token时间，把时延约束嵌进"精度-成本"效用最大化，超时延目标则选延迟最低实例｜所属路线：路由与调度（延迟感知路由）

---
### [44] Flare-Aug: Fast or Better? Balancing Accuracy and Cost in Retrieval-Augmented Generation with Flexible User Control

**基本信息**: arXiv:2502.12145 | 预印本 | Jinyan Su（康奈尔大学）、Jennifer Healey（Adobe Research）、Preslav Nakov（MBZUAI）、Claire Cardie（康奈尔大学）

**研究内容**: RAG 能靠外部检索减少幻觉，但现有框架要么无条件检索——简单问题浪费成本与时间，要么完全不检索——复杂问题答不出来。论文指出，幻觉多发生在答案所需事实超出模型训练范围时，比如事实太新、太冷门或来自私有数据；查询的复杂度差异很大——参数知识就能答的问题无需检索，缺知识的查询单步检索即可，检索与推理相互交织的多跳问题必须迭代检索，任何一种固定策略都无法同时照顾好这三类查询。

自适应检索代表 Adaptive-RAG 能按查询复杂度选策略，但只做静态分类，用户没法按应用场景调精度和成本之间的折中。论文还指出，另一类自适应方法靠读取模型内部状态判断是否需要检索，对越来越依赖闭源模型的现实场景不可行；更关键的是，已有方法都没有提供用户层面的可调性——实际应用中有的场景要精度（如医疗研究），有的要成本（如客服机器人），统一策略并不合适。Flare-Aug 训练两个分类器并做加权插值，让用户用一个参数 $\alpha$ 连续控制这个权衡：成本优化分类器以“能答对的最便宜策略”为标签，随具体 LLM 自适应；可靠性优化分类器只用单步/多步二值标签，跨 LLM 稳定但成本偏高。它用检索步数当成本代理：不检索成本 0、单步检索成本 1、多步检索成本等于步数。

**方法**: 第一个是“成本优化分类器”，训练标签是“能答对的最便宜策略”：对每个训练查询，用三种策略各跑一遍 LLM，在答对的策略里挑成本最低的当标签，所以它天然偏向消除不必要检索；它依赖具体 LLM，能力强的模型标签更偏向不检索。第二个是“可靠性优化分类器”，只用二值标签（单步/多步），标签来自数据集先验（单跳数据集→单步，多跳→多步），不依赖 LLM，保证至少检索一次，更稳但更贵。最终的用户可控分类器就是两者参数加权插值：$\alpha=0$ 完全采用成本分类器，$\alpha=1$ 完全采用可靠性分类器，中间值连续过渡。分类器用 T5-Large 训练（学习率 3e-5、batch 64、20 epochs、单张 80GB A100），检索器用 BM25。

**实验设置**: 混合评测集：三个单跳数据集（SQuAD、Natural Questions、TriviaQA）+ 三个多跳数据集（MuSiQue、HotpotQA、2WikiMultiHopQA），各 500 训练/500 测试；生成器四个：Flan-T5-XL/XXL、GPT-4o/gpt-4o-mini；基线与 Adaptive-RAG 及三种静态策略（不检索/单步/多步）对比。

**关键结果**:
- 用 $\alpha=0.4$ 就能拿到"接近全检索精度、约一半成本"：GPT-4o 上准确率 0.575、平均 2.2 步，对比多步检索的 0.595/4.2 步
- 小模型上更划算，可以精度成本双赢：Flan-T5-XL 在 $\alpha=0.4$ 时 0.439/2.2 步，超过多步检索的 0.437/4.7 步
- 成本分类器会自动适配模型强弱：GPT-4o 的最小成本接近 0（大量查询不用检索），Flan-T5 的最小成本接近 1（几乎都得检索）——换生成器时不用重新适配思路
- 精度-成本平面上整体优于 Adaptive-RAG，且验证集趋势与测试集一致——所以可先用验证集挑好合适的 $\alpha$ 再上线

**观测**: 粒度：请求级｜观测内容：每个查询的检索步数（成本代理）、准确率、两个分类器的输出｜数据来源：运行计量

**动作**: 用观测到的检索步数与两个分类器的输出，决定是否检索、单步还是多步，用 $\alpha$ 连续调节"少检索省钱"与"多检索保精度"。

**优化方式**: 用户可控路由：训练"成本优化"与"可靠性优化"两个分类器并加权插值，用 $\alpha$ 连续调节"少检索省钱"与"多检索保精度"｜所属路线：路由与调度（用户可控路由）

---
### [45] WISERouter: LLM Routing with Workload Budget Constraint

**基本信息**: arXiv:2607.23765 | 预印本 | Yifei Li、Zihui Gao、Laks V.S. Lakshmanan（英属哥伦比亚大学）

**研究内容**: 现有 LLM 路由方法有两类缺陷。其一是预算口径不对：多数方法不设严格的总额预算，或只按每个查询约束成本（FrugalGPT、CascadeRouting、MetaLLM），但现实里的配额（API token额度、月订阅费）作用在整个查询集合上，按查询约束在结构上就无法把预算倾斜给高价值查询。其二是依赖稠密监督：train-deploy 范式要求为每个查询-模型对收集输出与成本数据，模型数或查询数一多就采集不起。

论文把路由形式化为一个“带预算约束的反复选择”问题：每次从多个选项中挑一个执行，既要收益高、又不能超过总预算。面对 K 个看不到内部细节的模型和 T 个查询，在期望总成本不超过预算 B 的约束下最大化总质量。WISERouter 用自适应线性规划，每一步把“剩余预算 ÷ 剩余轮数”转化为本轮平均约束，实现跨查询的动态预算分配，不需要重训练；在线变体在同一个预算下先探索、再利用，并给出端到端的累计差距上界（衡量路由选择与理想策略之间的累计质量差距；边界情形 O(√T)、非边界情形 O(log T)）。与只对每个查询单独设预算的级联做法不同，工作负载级预算允许“把预算花在高价值查询上、放弃低价值查询”。

**方法**: 分三步。查询聚类：预训练编码器把查询映射为嵌入，K-means 聚成 16 个上下文簇，假设同一簇内每个模型的期望奖励与期望成本近似相同（实测验证成立）。每步解线性规划：在簇之间分配“选哪个模型”的概率，约束是总期望成本不超过剩余预算除以剩余轮数、目标是把总期望奖励最大化，“空动作”（跳过当前查询）保证总有可行解；每轮消耗后更新预算与轮数——预算紧张时线性规划自动把选择推向便宜模型，预算宽裕时放开。统计量获取分两种：离线版从历史数据一次性估计每簇每模型的奖励与成本均值、固定部署；在线版先用探索步把查询路由到簇内最少被调用的模型（保证每个“簇×模型”至少被访问若干次），之后用在线估计解线性规划——探索与利用共享同一个预算，探索没花完的预算自动转给部署期。

**实验设置**: RouterBench（36,497 查询、8 数据集、11 个模型）与 SWE-Bench Verified（500 个真实 issue、5 个模型轨迹）；预算从“全用最便宜”到“全用最贵”扫描 6 档；基线 FrugalGPT、CascadeRouting、MetaLLM、SingleBest；奖励用查询级性能分数、成本用token计价货币成本。

**关键结果**:
- 工作负载级预算路由在多数预算档都能拿到最好或并列最好的质量：RouterBench 低中档预算全部最高或并列最高；SWE-Bench 所有预算档最高，最紧预算（232.72 美元）比最佳基线高 14%（0.58 对 0.51）
- 预算用得干净：剩余预算比在所有档最接近零，不浪费也不超支；对比下 FrugalGPT 在最低预算完全失败——因为级联要多次调用模型，满足不了紧的每查询约束。紧预算场景应优先选这类全局预算方法
- 部署成本极低：离线版训练只需 1.50 秒（CPU），对比 FrugalGPT 的 8.85×10³ 秒（GPU）快 5,900 倍以上；且同一份统计量对任意新预算直接复用，换预算不用重训
- 在线版数据需求小：单遍训练集就达到约 0.8 平均质量，采集成本降低 90%；探索期没花完的预算自动留给部署期

**观测**: 粒度：调用级（向上聚合到工作负载级）｜观测内容：每条查询调用所选模型的token计价货币成本、剩余预算/剩余轮数、每簇每模型的奖励与成本估计｜数据来源：历史数据或在线估计

**动作**: 用观测到的"剩余预算÷剩余轮数"与每簇每模型的奖励/成本估计，决定在簇间分配模型选择概率、是否放弃低价值查询。

**优化方式**: 工作负载级预算路由：自适应线性规划每步按"剩余预算÷剩余轮数"在簇间分配模型选择概率，允许把预算花在高价值查询上、放弃低价值查询｜所属路线：路由与调度（工作负载级预算）

---
### [46] Conformal Cascade: Distribution-Free Accuracy Guarantees for Multi-Tier LLM Inference

**基本信息**: arXiv:2607.25018 | 预印本 | Yifan Dou、Shikan Lian、Shibo Li（佛罗里达州立大学）

**研究内容**: 级联是生产环境的主流降本方案：查询先走小模型，必要时升级大模型。升级与否由“延迟规则”决定，现有规则普遍用置信度阈值——softmax 概率、self-consistency 一致率、学习路由分数。但 LLM 的置信度校准很差，任何阈值都分不开“自信但错误”和“真的简单”这两类输入；用户想承诺“最多 5% 错误”时，根本不知道该把阈值设在哪里，还得为每对模型、每个基准重新扫阈值。

论文改用“候选答案集合的大小”取代置信度阈值：每层抽 N 条独立回答，构造一个预测集；集合缩成单个答案就接受（该层对答案有把握），集合里有多个候选就升级到下一层。关键性质是升级不会造成错误——只有“接受”这个动作会承诺一个答案，所以每层的保证可以组合成级联整体的正确性上界（Kα 并集界；在“选择保持”假设下收紧为 α）。论文给出任意 K 层级联结构的公式化描述与正确性上界定理、两层级联期望成本的闭式表达式，并在 18 个多选基准 × 4 个开源模型族上评估：72 对“模型族×基准”中严格胜过 49、持平 6、落败 17。

**方法**: 五个步骤。“不一致分数”：第 k 层从模型抽 N 条独立响应，每个答案的分数定义为“没抽到该答案的比例”——常被抽到的答案分数低，即“一致度高”。分位数校准：在独立校准集上取分数的分位数，预测集就是分数不超过该分位数的答案集合。级联执行：逐层走，集合大小在 1 到 $\kappa$ 之间就接受（返回集内分数最小的答案，$\kappa$ 默认 1），空集表示模型拿不准、照常升级，跑完 K 层就回退到最后一层的最小分数答案。正确性保证：被接受且答错的总概率不超过 Kα——原因是接受事件可以拆成 K 个互不重叠的“在第 k 层首次接受”事件，每层取 α/K 就保证级联整体错误 ≤α。期望成本只依赖校准集上的“第一层不接受”比例，不需要额外测试查询就能估算。

**实验设置**: 4 个两层配对（Llama-3.2-3B→3.1-8B、Gemma-4-E4B→31B、Ministral-3-3B→14B、Phi-3.5-mini→Phi-4）；18 个多选基准；每查询每层 N=16 样本、温度 0.7、vLLM 推理；调用成本 c1=1、c2=2.7；基线含 Always-Weak/Always-Strong、Agreement 级联、Entropy 级联、Random defer，共 12 个可调配置。

**关键结果**:
- 候选集级联在多数模型×基准组合上胜过阈值式级联：72 对里严格胜过 49、持平 6、落败 17，典型增益 +2.8 到 +4.6 个百分点（MathQA、GPQA-Diamond、MedMCQA、TruthfulQA 等）
- 错误率有可证上界、实测还更紧：设 α=0.10 时实测错误率落在 α 与 Kα=0.20 之间、更接近 α——所以可按"用户能接受的最大错误率"反推阈值，不必逐对模型扫参
- 分层自动按难度走：第一层接受率从 0%（GPQA-Diamond、MMLU-Pro 四族全升级）到 95.6%（Belebele-Eng）；难题组比 Always-Strong 高 3.8 个百分点、简单组代价不超过 0.6 个百分点——难易问题都不用取舍
- 成本节省显著且可用 κ 继续调：K=2、c1=1、c2=2.7、接受率 80% 时期望成本 1.54（比 Always-Strong 省 43%）；κ 从 1 放宽到 3，期望成本从 2.86 降到 1.34（−53%），代价 5.3 个百分点精度——想更省就放宽 κ
- 闭源 API 成本比越高越划算：实际成本比多在 3–50 之间，级联收益随该比值增大而增大

**观测**: 粒度：调用级｜观测内容：每层抽取N条独立回答构造的候选集大小、非一致性分数、每层调用成本｜数据来源：模型采样与校准集

**动作**: 用观测到的候选集大小，决定接受该层答案还是升级到下一层——集合缩成单个答案就接受、有多个候选就升级。

**优化方式**: 候选集级联：用"候选答案集合大小"取代置信度阈值——集合缩成单个答案就接受、有多个候选就升级，保证被接受且答错的概率不超过Kα｜所属路线：路由与调度（兼大小模型分工，保证性路线）

---

### [47] FrugalGPT: How to Use Large Language Models While Reducing Cost and Improving Performance

**基本信息**: NeurIPS 2023 | arXiv:2305.05176 | Lingjiao Chen、Matei Zaharia、James Zou（斯坦福大学）

**研究内容**: 可按查询计费的 LLM API 数量快速增长（OpenAI、AI21、CoHere 等），论文先回顾主流 API（GPT-4、ChatGPT、J1-Jumbo 等）的定价，发现这些模型的定价结构高度异质，费用可相差两个数量级；在大规模查询与文本上直接使用 LLM 可能非常昂贵——例如用 GPT-4 支撑客服一个月可能超过 21000 美元，且最大的 LLM 还带来环境与能源代价。

论文据此概述并讨论三类可降低推理成本的策略：prompt 自适应（对给定查询优化提示，让便宜模型也能答好）、LLM 近似（用小模型或缓存近似昂贵模型）、LLM 级联（按查询选择模型组合，必要时升级到更贵模型）。作为示例，论文提出 FrugalGPT——一个简单而灵活的 LLM 级联实例化：学习针对不同查询该用哪些 LLM 组合，在成本与质量之间取最优。实验表明，FrugalGPT 能以最高 98% 的成本下降匹配最好的单一 LLM（如 GPT-4）的性能，或在相同成本下把准确率提升 4%。

**方法**: 三级降本：prompt adaptation（对给定查询优化提示，让便宜模型也能答好）、LLM approximation（用小模型或缓存近似昂贵模型）、LLM cascade（按查询学习选择模型组合，必要时升级到更贵模型）。核心是学习每个查询该走哪条路径，在成本与质量之间取最优。

**实验设置**: 多任务、多 LLM API，含真实定价与查询；对比单一 LLM 与多种级联配置。

**关键结果**:
- 按查询选择模型组合的级联路由值得先试：能以最高 98% 的成本下降匹配最好单一 LLM（GPT-4）的性能，或在同等成本下把准确率提升 4%——意味着多数查询用便宜模型即可，只在必要时升级

**观测**: 粒度：调用级｜观测内容：各API的定价、每查询的token消耗与回答质量、级联中各层的成本-质量曲线｜数据来源：API定价表与真实查询结果

**动作**: 用观测到的便宜模型回答质量，决定是否升级到更贵模型——便宜模型先答、质量不足再升级。

**优化方式**: 成本感知级联路由：prompt自适应 + 近似LLM + LLM级联，便宜模型先答、质量不足再升级｜所属路线：路由与调度（成本感知级联）

---

## 九、多智能体协调

### [48] PSMAS: Phase-Scheduled Multi-Agent Systems for Token-Efficient Coordination

**基本信息**: arXiv:2604.17400 | 预印本（审稿中） | Mohit Dubey（Open Gigantic）

**研究内容**: 大规模部署 LLM 智能体催生了多智能体系统：专业智能体协作分解、执行、验证复杂任务。最简单的实现是让所有智能体同时激活、每个智能体都收完整累积上下文，对 $n$ 个智能体与上下文长度 $L$，总token消耗是 $O(n \times L)$，广播式架构里 $L$ 还随智能体数增长，实际是二次膨胀。作者实测生产级 5-智能体代码评审管道单次调用 42,000–71,000 token，其中 29–38% 是根本用不上的冗余上下文。

已有缓解手段——静态剪枝、层级分解、学习路由——都把协调当成静态分配问题，控制不了每个智能体何时激活；即使最强的自适应基线也是逐步骤离散路由，没有时间结构，系统性冗余依然存在。本文把智能体激活重新建模为对共享注意力空间的连续控制，建模在圆形流形 $\mathbb{S}^1$（几何上的圆，这里指把激活时机安排在一个循环周期上）上：每个智能体按任务依赖拓扑分到固定角度相位，全局扫描信号匀速旋转，只激活落在角窗口内的智能体，空闲智能体只收压缩后的上下文摘要，从而降低每步token消耗。论文还形式化证明了排序正确性、延迟噪声下的稳定性、收敛到全激活不动点与最优窗口，并证明调度与压缩是两个独立的收益来源。

**方法**: 每个智能体按任务依赖拓扑分到角度位置 $\theta_i \in [0, 2\pi)$，两种分配法：拓扑相位分配按拓扑序均匀分布，加权相位分配按期望延迟成比例分布（可更快扫描）。一个指针 $\phi(t) = \omega t \bmod 2\pi$ 匀速旋转，只激活角度差在窗口 $\varepsilon$ 内的智能体；激活的收完整上下文，空闲的只收压缩摘要（Mistral-7B 生成，长度约 $\alpha = 0.12$，缓存到指针扫过才刷新）。成本模型表明：总成本由激活智能体的完整上下文、空闲智能体的压缩上下文与激活智能体的输出三部分组成，其中激活比例 $f(\varepsilon) = \varepsilon/2\pi$ 由调度决定、压缩比 $\alpha$ 由压缩器决定，两个旋钮互不干扰。论文还给出排序正确性、延迟噪声下稳定性、收敛到全激活不动点与最优窗口等定理。

**实验设置**: LangGraph 中间件实现（约 80 行 Python）；四个结构化基准（HotPotQA-MAS、HumanEval-MAS、ALFWorld-Multi、WebArena-Coord）与两个非结构化场景（智能体辩论、开放研究问答），每配置 $n=500$；基线含全激活、静态剪枝、随机调度、AgentPrune、HuggingGPT 路由、RouterLLM-自适应，并消融 $\alpha \in \{0.12, 0.20, 0.30, 0.40, 1.0\}$。

**关键结果**:
- 相位调度可安全省钱：平均 token 减少 27.3%（范围 21.4–34.8%），性能下降不超过 2.1 个百分点（p<0.01）——结构化协作任务可放心用
- 调度是主要收益来源：只做调度就省 18–20 个百分点，压缩比退化到 α=0.40 也不受影响；反过来只压缩不调度只省 16.2 个百分点、还掉 4.6 个百分点性能——先做调度，压缩是叠加项
- 加权相位分配优于最强学习路由 RouterLLM-RA：多省 5.6 个百分点 token，性能损失还少 2.0 个百分点——手工拓扑分配足以胜过复杂路由
- 无结构场景收益减半到 14–19%，但仍是所有非 PSMAS 基线里性能损失最小的，延迟从 38.4s 降到 29.7s——非结构化任务也可用，只是收益打折

**观测**: 粒度：智能体级｜观测内容：每个智能体每步的激活状态（激活/空闲）与token数、空闲智能体的压缩摘要长度、扫描指针相位｜数据来源：框架运行计量

**动作**: 用观测到的扫描指针相位与智能体状态，决定哪些智能体激活、空闲智能体只接收压缩摘要。

**优化方式**: 相位调度：用旋转扫描指针控制智能体激活，只激活落在角窗口内的智能体，空闲智能体只收压缩摘要，激活比例与压缩比两个旋钮互不干扰｜所属路线：多智能体协调（相位调度）

---
### [49] DynaGraph: Lightweight Multi-Model Interaction Framework via Dynamic Topological Reconfiguration

**基本信息**: arXiv:2605.29511 | 预印本 | 郭彦兴、郑子豪（共同一作）、方舟（南京大学）、梁玲、包林、王宗伟、蔡一茂（北京大学及联合单位）

**研究内容**: 复杂推理任务（多跳问答、数学推理、金融风险分析）通常依赖 GPT-4、Llama 3-70B 等超大单体模型，一个模型包办管线所有环节，计算冗余严重，也难以模块化扩展。现有替代方案各有瓶颈：CoT、ToT 等提示技术运行在预定义刚性拓扑上，没有运行时自愈，局部异常级联成整体失败；MoE 耦合度高、内存开销大，无法独立热更新；静态 DAG 管道开销可预测但错误沿链级联；无约束动态智能体（如 Reflexion）灵活但轨迹发散、反复试错，GPU 内存不可控。

DynaGraph 的定位是轻量多模型框架：任务建模为执行图，节点是专家执行、边是上下文依赖，由实时反馈驱动拓扑演化。执行层在共享基座上时分复用 LoRA 适配器，内存占用保持 O(1)（峰值约 16.6GB），整套系统可在单张消费级 GPU 上完成训练与推理部署；控制层评估器监控执行置信度，按错误严重程度触发分层自愈：细粒度修补缝合局部缺口，子图重建截断并重生成损坏分支。核心贡献是以 8B 规模逼近 72B 单体（StrategyQA 87.6%、MATH 82.7%），并比无约束动态架构减少 68.6% token、68.1% 延迟。

**方法**: 四个领域专家（检索、逻辑、表达、规划）作为 LoRA（rank=8, α=16）时分复用同一个冻结的 DeepSeek-8B 基座：任一时刻 GPU 上只有基座加一个活动适配器，峰值内存 O(1)，实测约 16.6GB。执行时规划器先生成任务 DAG，每个节点由一个专家执行，返回三元组反馈（输出、异常标志、置信度）；评估器监控全局不确定性（1 减去已执行节点的平均置信度），遇到任何节点异常、置信度低于 0.35 或不确定性不低于 0.45 就暂停。修复分两级：局部数据缺口插一个补丁节点（细粒度修补）；逻辑严重断裂则截断失败节点下游分支、重新生成子图（子图重建），重建次数设上限防无限递归。中心规划器用 Critic 引导的 DPO 训练，奖励为任务完成度乘动作合法性（非法拓扑一票否决）再减去与节点数、重建次数相关的惩罚项。

**实验设置**: 三个数据集（StrategyQA 开放域多跳、MATH 数学推理、FinQA 金融数值推理）；DeepSeek-8B 基座、单张 RTX 5090；8B 基线含 Standard Prompting/CoT/ToT/Self-Consistency、ReAct/Reflexion/3×8B 多智能体，72B 参照为 Qwen-2-72B-Instruct；GPT-5 作为离线 DPO 教师。

**关键结果**:
- 动态重配置能把 token 和延迟各砍约三分之二：比无约束动态架构（Reflexion）token 减少 68.6%、延迟减少 68.1%（FinQA 上 7,690→2,480 token，86.9s→30.2s）——无约束动态架构的直接替代
- 8B 足以逼近 72B 单体：StrategyQA 87.6%、MATH 82.7%、FinQA 82.5%，对照 72B 的 92.5%/89.2%/86.1%——不想上超大模型时可先试这类方案
- 显存需求差约 3 倍：3×8B 多智能体部署要超过 49.5GB，本方案只要 16.6GB——单张消费级 GPU 即可训练和部署
- 两级自愈缺一不可：去掉子图重建，MATH 从 82.7% 崩到 41.5%；去掉细粒度修补，StrategyQA 延迟从 15.3s 涨到 29.4s——局部修补和整体重建要一起上

**观测**: 粒度：会话与轨迹级｜观测内容：一次完整任务（含失败与重建节点）的全部token、节点置信度与全局不确定性、拓扑异常｜数据来源：框架运行计量

**动作**: 用观测到的节点置信度与全局不确定性，决定局部插补丁节点还是重建子图。

**优化方式**: 动态重配置：评估器监控执行置信度，局部缺口插补丁节点、严重断裂重建子图；以LoRA时分复用基座压内存——训练部分属训练时手段｜所属路线：多智能体协调（动态重配置）

---
### [50] The Harness Effect: How Orchestration Design Sets the Token Economics of Enterprise Agentic AI

**基本信息**: arXiv:2607.06906 | 预印本 | Muayad Sayed Ali、Aliaksandra Novik 等 30 余位（Writer, Inc.）

**研究内容**: 论文提出“token最大化”来描述行业现状：用token买能力——更长的推理轨迹、更多的智能体轮次、更宽的工具返回、更完整的历史回放——每任务消耗的token涨得比任务价值快。单位token的价格下降只会掩盖这种趋势（这是杰文斯悖论的翻版：资源变便宜反而推高总消耗），总账单照样上涨。

论文主张真正的成本杠杆在“运行框架”（论文称为 harness）——执行智能体任务的那层程序，它负责拼装上下文、暴露工具、安排执行轮次、派发子任务、记录token用量。为了把这一层的贡献单独量出来，论文做了一次受控交换：22 个锁定的任务、6 个基座模型（Claude Sonnet 4.6、Gemini 3.1、Gemini Flash 3.5、Qwen 3.6、GLM 5.1、Palmyra X6）全部保持不动，只把冻结的传统生产智能体循环换成 Writer Agent Harness。

结果分两方面看。效率上，每任务成本 −41%（$0.21→$0.12）、中位耗时 −44%（48 秒→27 秒）、每任务token −38%（14.2K→8.8K），且对六个模型全部成立（每个模型便宜 33%–61%）——在本负载上，换运行框架省的钱比在最贵与最便宜模型之间切换（最多 36%）还多。质量上，任务完成度基本持平（0.78→0.81，样本量只有 22，只能作方向性结论），而且模型从运行框架里提取的质量提升与其本身强度几乎完全相关（r=0.99，n=6），论文称之为“框架杠杆”；质量每美元提升 82%，每百万token完成任务数从 54.9 升到 92.0。

**方法**: 论文把一次智能体任务的账单写成输入、输出两类token分别计价的和。输入侧有五项由运行框架决定：系统提示、历史记录、工具定义、检索内容、用户输入。最简单的实现每轮把完整历史重放一遍，累计输入随轮数二次方增长；运行框架管理上下文（前缀缓存、历史压缩、工具输出卸载、检索截断）后，累计输入近似线性。再引入“有效输入价格”：缓存命中的输入token只按原价的十分之一计费，所以有效输入价格等于输入价乘命中相关因子，命中比例完全由运行框架拼装上下文的方式决定——本工作实测同前缀调用时 99.9% 的提示token走缓存读。两句话概括：运行框架既决定“发多少token”，又决定“大头token按什么价计费”。

六个机制族全部落在运行框架里。前缀稳定：提示词分成“固定前缀 + 每轮易变的尾部”两区，前缀装工具定义、固定系统提示、只追加的对话记录，每轮会变的东西禁止进前缀——前缀逐字节不变，缓存才能命中。渐进压缩：上下文用到模型预算的 80% 时，把旧历史压成一份固定结构的摘要，摘要由便宜的辅助模型在付费循环之外生成。上下文卸载：子智能体在自己的上下文里读和搜，只回传不超过 8KB 的摘要；技能只给名字和描述、全文按需读取；超过 20K 字符的工具输出只给头尾预览、全文落到工作区文件。零token等待：需要等人审批或等后台任务时挂起，事件到达再恢复，不靠轮询烧token；崩溃后从持久化状态恢复。失败处理：失败先分类再决定是否重试，同一个失败工具调用重复三次就停止再调用它，整个任务最多执行 50 轮。模型无关底线：路由计划是数据而非代码分支，任何模型都走同一条执行路径，并对弱模型做工具定义清理。

**实验设置**: 22 个锁定任务（9 个能力域：模型系统意识、证据检索、内容生成、Playbooks、MCP 工具、演示、语音、图像、子智能体）× 6 个模型 × 2 套运行框架；成本按固定的 OpenRouter 价表、从记录每次调用token用量的追踪组件统一计算，两侧同价表；质量用 LLM 裁判面板按事先核对过的 pass/fail 标准打分。

**关键结果**:
- 换运行框架的收益显著：成本/任务 −41%、中位墙钟 −44%、token/任务 −38%，质量每美元 +82%，每百万 token 完成任务数 54.9→92.0（+68%）——升级运行框架比换模型更划算
- 降本与模型无关：六个模型成本降幅从 −33%（Gemini 3.1）到 −61%（Flash 3.5），而基线内换模型最多省 36%——运行框架是更大的成本杠杆，且对未来模型继续有效
- 质量增益与模型能力强相关：与基线能力 r=0.99；48 个能力×模型格中 30 升、11 平、7 降，7 个降全部在小模型、集中在依赖框架协调的能力——小模型上换框架不能保证不降质量
- 子智能体对模型有门槛：只在两个最强模型上越过可用可靠性阈值（0.85–0.86），Flash 档只有 0.42–0.45——弱模型不建议开子智能体编排
- 规模化收益可观：每月 100 万任务时成本从 21 万美元降到 12 万美元，一年省 108 万美元，节省随任务量线性增长

**观测**: 粒度：会话与轨迹级（记账在调用级）｜观测内容：每任务的输入/输出token总量、成本（固定价表）、中位墙钟、每百万token完成任务数与每美元质量｜数据来源：trace shim 记录每次调用用量

**动作**: 用观测到的每任务token/成本/墙钟，识别运行框架是最大成本杠杆，据此统一实施前缀稳定、渐进压缩、上下文卸载等六机制。

**优化方式**: 运行框架优化：前缀稳定、渐进压缩、上下文卸载、零token等待、失败处理六机制由运行框架统一实施，把"运行框架"本身当作优化对象｜所属路线：多智能体协调（运行框架优化，跨上下文/记忆/技能）

---

### [51] SVR-MAD: A Bayesian-Inspired Framework for Posterior-Guided Multi-Agent Debate

**基本信息**: arXiv:2605.23099 | 预印本 | Weifan Jiang、Rana Shahout、Minghao Li 等（哈佛大学）

**研究内容**: 多 Agent 辩论（MAD）通过让 Agent 交换中间推理步骤与答案、根据同行信息修订自己的回答，在科研、数学、编码等领域提升准确率。但 MAD 的成本随通信图增长而快速上升：每条共享消息都被追加到另一个 Agent 的上下文，增加输入 token 与 KV 缓存开销；Agent 数或辩论轮次一多，开销迅速累积，限制扩展性。近期的降本工作用先验信号（token 级 log 似然、LLM 自报置信度）从全连接通信中移除节点或边，但这些信号在幻觉下变得不可靠，会降低依赖它们的 MAD 方法的准确率。

论文提出 SVR-MAD，一个贝叶斯启发式的 MAD 框架：把辩论前的信号当作先验，把辩论结果当作后验式证据来估计 Agent 的正确性。SVR-MAD 用该证据增量式构建通信图，优先保留那些"答案经受住同行挑战"的 Agent。跨多个 LLM 与基准的实验显示，SVR-MAD 相对最准确的竞争性 MAD 基线，把 token 成本降低最多 61%，同时匹配或提升准确率。

**方法**: 把先验信号（token 级 log 似然、LLM 自报置信度）与辩论结果（后验证据）结合估计 Agent 正确性；增量式构建通信图——首轮全回答、次轮成对挑战、逐轮只向"答案经受住挑战"的 Agent 保留通信。

**实验设置**: 多 Agent 辩论基准；多个 LLM；与最准确的竞争性 MAD 基线对比。

**关键结果**:
- 用辩论结果做后验证据能安全降本：通信 token 减少最多 61%，准确率匹配或提升——想砍多智能体辩论的通信开销时可用这种逐轮剪枝
- 别依赖先验信号：prior signals 在难题上的正确率仅 5–34%，自报置信度不可靠——剪枝依据要用"答案经受住同行挑战"的后验证据

**观测**: 粒度：系统级｜观测内容：Agent间消息的通信token、各Agent答案经同行挑战后的存活率（基于后验证据而非自报置信度/log概率）｜数据来源：辩论运行记录

**动作**: 用观测到的各Agent答案经挑战后的存活率（后验证据），决定逐轮剪掉哪些通信、保留哪些Agent。

**优化方式**: 贝叶斯后验通信剪枝：首轮全回答→次轮成对挑战→逐轮仅保留"答案经受住挑战"的Agent｜所属路线：多智能体协调（通信剪枝）

---

## 十、模型协作与大小模型分工

### [52] PyroDash: Cost-Efficient Token-Level Small-Large Language Model Collaborative Inference

**基本信息**: arXiv:2607.20327 | 预印本 | Niqi Lyu、Pengtao Shi、Wei Qiu、Jianlin Zhong、Sicong Xia、Jianyao Ma、Yicheng Ding（Pyromind Dynamics Inc.）

**研究内容**: PyroDash 解决“小模型便宜但难题不可靠，大模型可靠但贵”的两难。论文的背景观察是：云端大模型能力强，但高频或长生成负载下 API 账单高昂，把输入传给远程服务器还有隐私问题；开源小模型便宜、可自托管，但泛化弱，量化、蒸馏也补不上与强模型的能力差距，单一规模都不够。协作推理的思路是小模型先独立推理，只在某一步超出能力时再请大模型帮忙，关键问题从“选哪个模型”变成“推理到哪一步该介入”。

现有方法只解决了一部分：请求级路由在解码前就定好模型，但很多题是“表面简单、实际越推越难”，中途需要帮助时它们无法响应，升级也是从头重生成而不是从失败点续写；token级协作要么要独立路由器、要么允许反复切换、要么需要访问大模型内部的输出分数（logits），且缺乏明确的账单成本目标，反复切换还会让无状态 API 反复重发不断增长的上下文，变成额外的预填充费用；投机解码需要拿到目标模型的输出分数，优化的是延迟而不是 API 账单。PyroDash 的定位：路由策略完全内化在小模型里，大模型只做一次冻结的交接续写——不需要独立路由器、不重训大模型、不访问内部输出分数，兼容闭源 API。交接点选择很关键，因为商业 API 对输入输出token分开计价，交接点同时决定大模型的预填充前缀费用和剩余自回归解码费用。

**方法**: 小模型在自回归生成中输出专用控制token $\tau_{off}$，协作引擎检测到后把原始问题加部分推理轨迹打包，交给冻结的大模型一次性完成剩余推理。三步训练：(1) 控制token嵌入学习，用锚点嵌入均值加噪声初始化，微调嵌入层与临时 LoRA 后丢弃适配器只保留新token行；(2) 面向交接的监督微调冷启动，在按基座能力划分易难的 24K 数据集上联合训练独立推理与条件卸载；(3) 成本感知 GRPO 对齐（GRPO 是一种强化学习优化算法，用一组采样样本的相对表现来更新策略，不需要额外的价值模型），奖励为回答正确性减 $\lambda$ 倍归一化推理成本（实际成本除以全用大模型的成本），$\lambda$ 是用户可调的旋钮，越大越省钱。交接点选择很关键，因为它同时决定大模型的预填充前缀费用和剩余解码费用。

**实验设置**: 小模型用 Qwen3.5-4B，大模型用 GLM-5.2-FP8（冻结）；五个数学推理基准：GSM8K、Minerva、OlympiadBench、AIME25、AIME24；按公开 API 价格计费；基线含小模型单独、小模型加 SFT、仅大模型、RouteLLM、GlimpRouter。

**关键结果**:
- λ 调小可同时提精度降成本：λ=0.05 时平均准确率 64.04%，超仅大模型基线（57.68%）6.33 个百分点，成本还 −20.4%——小模型加条件卸载能超过全用大模型
- λ 调大是省钱模式：λ=0.6 时准确率 54.55%，大模型 token 占比仅 1.90%、每样本平均 0.012 次大模型调用，总成本从 49.36 美元降到 1.78 美元（−96.4%）——预算敏感场景可用
- 比请求级路由省得多：RouteLLM 大模型 token 占比 77.37%、GlimpRouter 75.11%，而 λ=0.6 策略准确率更高、大模型使用量约为其四十分之一——token 级交接优于路由级决策
- 训练流程可复现：SFT 冷启动把单独准确率从 28.36% 提到 46.25%，GRPO 再提到 64.04%；λ 从 0.05→0.1 时大模型使用骤降——λ 是现成的精度-成本旋钮

**观测**: 粒度：token级｜观测内容：小模型在每个生成位置的继续/交接决策、小模型与大模型各四类token与单价、$\lambda$ 成本参数｜数据来源：框架运行计量

**动作**: 用观测到的当前生成位置与 $\lambda$ 成本参数，决定小模型继续自己写还是发 $\tau_{off}$ 交接给大模型续写。

**优化方式**: token级迁移：小模型生成 $\tau_{off}$ token 把剩余推理交给冻结大模型续写；成本感知GRPO把API账单写进训练奖励——训练部分属训练时手段｜所属路线：模型协作（token级迁移，兼训练时效率塑造）

---
### [53] PiERN: Token-Level Routing for Integrating High-Precision Computation and Reasoning

**基本信息**: arXiv:2509.18169 | 预印本 | Hengbo Xiao、Jingyuan Fan（共同一作）、Guannan He（通讯，北京大学）、Feixiong Chen、Tianming Shao（北京大学长沙计算与数字经济研究院）、Xin Tong（北京航空航天大学）、Jingzhao Zhang、Chao Lu（清华大学）

**研究内容**: 复杂系统决策依赖高精度数值计算（偏微分方程求解、电池健康估计等），而 LLM 在语言理解和逻辑推理上进步很大，但涉及高精度浮点运算、多步计算或偏微分方程求解时经常给出错误或不精确的结果。论文指出两条现有路线都不够：端到端微调 LLM 学数值计算，当输入输出是高维数值矩阵或时空网格解时学不会（甚至不服从指令），还可能灾难性遗忘；多智能体系统由 LLM 调度外部专家，虽然精度高，但每步要做任务分析、数据对齐、代码生成、工具调用、结果分析，通信协调开销巨大，导致推理效率低、响应延迟高、GPU 能耗大、扩展性有限。

PiERN 的定位是把高精度计算专家（神经或非神经）直接内嵌进 LLM 架构，在单条推理链里按token粒度在“语言推理”和“专家计算”之间交替，让精度、可解释性、效率三者兼得。核心贡献有四：一是架构加分步训练加token级动态路由推理范式；二是以 LLM 作文本到计算解码器，把任务语义映射成专家的数值输入，突破纯数值输入的限制；三是支持推理驱动的组合式计算，计算结果经推理链路由到其他专家做协作迭代；四是发布文本条件的 PDEBench/GCAM 扩展集和电池域多专家协作数据集。

**方法**: 三个组件：物理隔离的计算专家（神经或非神经，如 PDE 求解器、电池健康估计网络）、文本到计算模块（把自然语言任务映射成专家需要的数值输入）、token路由器（读当前步隐藏状态，输出“LLM 继续生成”和“各专家”之间的概率分布，交叉熵训练）。三步训练解耦：先预训练专家并冻结，再训文本到计算对齐（MSE 加对比损失），最后训路由器。推理时：句子“电池健康是……”的前半段路由到 LLM 生成自然语言，到需要具体数值时路由到专家返回高精度值再拼回序列，LLM 接着推理“……说明状态良好”。

**实验设置**: 自建文本-计算任务集：PDEBench 三个方程（1D 扩散-反应、1D 扩散-吸附、Burgers）、GCAM 气候政策（32 区域、9 个领域专家）、电池管理系统（容量估计 + 套利收益两个专家协作）；骨干为 Qwen3 系列（PiERN-PDEBench-2.4B 起自 Qwen3-0.6B）；对照为 AutoGen 多智能体系统与 Llama-Factory 微调模型；指标 RMSE、延迟、GPU 能耗、token数、路由成功率、MMLU/GLUE。

**关键结果**:
- token 级内嵌专家远快于多智能体调度：PDEBench 上延迟与 GPU 能耗比多智能体基线低 1–2 个数量级（2.4B 模型 1D 扩散-反应 0.99s/269.59J，对 Qwen3-0.6B 的 9.85s/2,156.93J）——需要数值计算的任务别走多智能体
- 开销极小：全程仅约 20 token，而多智能体基线要 500–1,500，专家路由成功率 100%——精度不依赖冗长轨迹
- 精度达标：数值精度与官方求解器相当（1D 扩散-反应 0.298 对 0.326），而微调 LLM 完全无法遵循指令——内嵌专家比微调 LLM 可靠
- 多任务同样受益：GCAM 每任务 1.25s 对 Agent-Expert 2.49s（快 2 倍）；电池任务 MSE 比微调 LLM 低 1–2 个数量级
- 不伤通用能力：MMLU/GLUE 与基础模型无显著退化（2.4B：MMLU 40.17 对 Qwen3-0.6B 40.32）——引入专家的同时无需担心通用能力下滑

**观测**: 粒度：token级｜观测内容：每步隐藏状态 $h_t$、每token路由决策（LLM/专家）、每任务token数、响应延迟与GPU能耗｜数据来源：框架运行计量

**动作**: 用观测到的每步隐藏状态，决定下一个token由LLM生成还是交给计算专家处理。

**优化方式**: token级路由：token 路由器读隐藏状态输出"LLM继续生成"与"各专家"的概率分布，让LLM与物理隔离的高精度专家在单条推理链内按token交替｜所属路线：模型协作与大小模型分工（token级路由）

---
### [54] AgentInfer: Towards Efficient Agents — A Co-Design of Inference Architecture and System

**基本信息**: arXiv:2512.18337 | 预印本 | Weizhe Lin、Hui-Ling Zhen、Shuai Yang 等（华为）

**研究内容**: AgentInfer 指出传统加速方法（量化、投机解码等）只优化“每秒token数”（投机解码是一种推理加速技术：小模型先快速草拟多个 token，大模型一次验证草稿，把串行生成换成并行验证），但智能体的真正瓶颈是“完成整个多轮任务的墙钟时间”——推理上下文随轮次线性增长、工具交互异构、失败要重试，延迟是跨推理循环系统性累积的，而非来自单次孤立的模型推理。论文强调“每秒token数”这个指标在智能体场景具有误导性：智能体的进展是路径依赖的，一个“更快但更不准”的模型会引入事实错误、无效工具调用和前后不一致的摘要，这些错误逐轮级联，迫使智能体重试和重复推理，把任何token级加速都抵消掉，因此评估要看端到端任务延迟、总算力成本和回合稳定性。

它还指出标准优化在智能体上的三类失效：量化陷阱（INT8 让单步前向快 45%，但数学推导和 JSON 格式的精度损失引发 3.5 倍重试，总解题时间反而增加 70%）、粒度错配（摘要剥掉执行工具所需的具体变量名和数值，澄清轮次增至 3.5 倍，还带来上下文漂移）、记忆持久化瓶颈（标准调度清除等待工具的智能体的 KV 缓存，超 32K 上下文命中率仅 15%，重算预填充造成延迟突然飙升）。它的定位是把推理架构和系统调度联合设计，以“任务完成”而非 TPS 为优化目标，在深度研究智能体（多轮推理 + 工具搜索 + 证据综合）上验证。论文把问题拆成四个协同组件：AgentCollab 用层次化双模型推理动态分配角色，AgentSched 是缓存感知的混合调度器，AgentSAM 基于后缀自动机的投机解码、跨会话复用语义记忆，AgentCompress 异步压缩并重组智能体记忆而不打断推理，四者构成一个自我进化引擎。

**方法**: 四个协同组件。AgentCollab（架构层）：大模型做战略规划，小模型执行常规推理和工具调用，用结构化的进度检查信号自评进度，进度停滞才升级大模型（约 1.3 倍加速）。AgentSched（系统层）：缓存感知混合调度器，按每条任务的边际价值（shadow price）动态在短任务优先和保留 KV 缓存两种模式间切换（约 1.2 倍）。AgentSAM：基于后缀自动机的投机解码（后缀自动机能高效匹配与复用字符串后缀，这里用来跨会话复用语义记忆中的 token 预测），异步线程构建避免阻塞首token时间。AgentCompress：语义压缩加异步上下文摘要，因为搜索内容token常占上下文的 50% 以上，异步压缩不打断推理（约 1.2 倍）。四者叠加成 1.8–2.5 倍端到端加速。

**实验设置**: 基准 BrowseComp-zh 和 DeepDiver；模型 openPangu-DeepDiverV2-7B（小）、openPangu-DeepDiverV2-38B（大）、openPangu-7B（压缩用）；基础设施 vLLM-Ascend v0.9.1（Ascend 910B3 NPU）；对比大模型单独、小模型单独、大模型加各组件的消融。

**关键结果**:
- 以任务完成时间为目标的联合设计有效：无效 token 减少超过 50%，端到端加速 1.8–2.5 倍，准确率保持——评估智能体别只看每秒 token 数
- 大小模型分工可行：AgentCollab 达 33.8% 准确率（大模型单独 34.6%、小模型单独 18.3%），并带来 1.32 倍加速——小模型覆盖大部分轮次、大模型只在进度停滞时介入
- 量化要谨慎：INT8 让每秒 token 数提高 45%，但任务成功率从 88.2% 降至 61.7%、重试增至 3.5 倍，总解题时间反而增加 70%——智能体场景里"更快但更不准"会被级联错误抵消
- 各组件收益可加：AgentCompress 把上下文从 40K+ 压到约 20K，AgentSched 把 KV 命中率从 63% 提到 72%，AgentSAM 使 LLM 延迟降 26%——搜索内容常占上下文 50% 以上，压缩价值最大
- 并发收益随会话数放大：全系统 QPS 提升 1.97 倍（4 并行会话）与 2.52 倍（16 并行会话）——高并发部署时收益更明显

**观测**: 粒度：多层级（token/请求/轮次/会话）｜观测内容：大小模型的token分配、上下文增长、投机决策的token预测、整个会话轨迹总消耗与无效token比例｜数据来源：框架运行计量

**动作**: 用观测到的进度、上下文增长与KV缓存状态，决定分工、排队、投机、压缩四类动作，统一以端到端任务完成时间为目标。

**优化方式**: 推理架构与系统联合设计：层次化双模型分工（AgentCollab）、缓存感知混合调度（AgentSched）、投机解码（AgentSAM）、异步语义压缩（AgentCompress），统一以端到端任务完成时间为优化目标｜所属路线：跨路线（路由/分工/投机解码/记忆）

---

## 十一、数据格式与表示优化

### [55] ONTO: A Token-Efficient Columnar Notation for LLM Input Optimization

**基本信息**: arXiv:2604.17512 | 预印本 | Harshavardhanan Deekeswar（Independent Researcher，印度钦奈）

**研究内容**: LLM 从对话助手转向运维智能（遥测分析、日志异常检测、根因分析），开始大规模消费结构化数据，输入token大部分花在格式而不是内容上，而序列化格式直接决定token成本、上下文容量与推理延迟：1,000 条 IoT 遥测记录序列化成 JSON 约需 80,000 token，device_id、temperature 这类字段名每条重复一遍，大括号、引号、冒号再占数千。论文给出的实例中，一次对 1,000 条遥测记录的查询甚至消耗了 70 万以上token，真正用于推理的值只占零头。

论文的核心洞察是：为 LLM 输入做序列化与文档交换是两类不同问题——提示由已知能力的单一系统一次性消费，不需要交换格式的防御性冗余，可以“模式声明一次、数据多行复用”。现有格式各有缺位：JSON 每个记录重复字段名、深层嵌套产生长程依赖；YAML 只比 JSON 省 1–6%；CSV 列式但无法嵌套；Parquet 压缩好但二进制、不能直接进提示；提示压缩有损且不利用记录规律。本文提出面向 LLM 输入的列式记法 ONTO，占据“列式效率 + 嵌套结构 + 可直接进提示”的空位，定位为输入预处理优化而非通用序列化格式。

**方法**: 核心原则为“模式一次声明、数据多行排列”。字段名每个实体仅声明一次，值以管道符（|）分隔排列成行，缩进表示层级，实体行用 [N] 显式记录条数，数组元素用脱字符（^）分隔，反引号强制字符串、区分空值 null 与空串，类型自动推断。语法选择均有理由：管道符在运维数据中罕见且通常只占一个token；缩进替代嵌套大括号，省掉成对的 { } [ ] 与引号；固定 4 空格缩进让解析不需要向前看；[N] 声明让解析器能校验每列是否恰好 N 个值，也告诉模型数据量。

**实验设置**: 三个合成运维数据集（嵌套的 IoT 遥测、扁平的 System Metrics、日志），各在 100/500/1,000 条规模生成；token用 tiktoken 的 cl100k_base 计数；延迟在 Qwen2.5-7B-Instruct（q4_K_M 量化，Ollama 运行时）上测，每场景 20 次运行；理解准确率用 GPT-5.4-mini 测查找、计数、列举、聚合四类任务。

**关键结果**:
- 列式格式显著省 token：三个数据集 token 减少 46–51%（IoT 46.3%、系统指标 50.8%、日志 47.3%），而 YAML 只比 JSON 少 1–6%——结构化数据进提示词时值得换格式
- 省 token 主要来自消除重复键：键消除单独贡献 41,979 token（毛节省的 113.5%），标点减少再省 15,300，缩进是嵌套数据的代价（约 5,000 token）——优化优先砍重复的字段名
- 加速是亚线性的：首 token 时间减少 5–7%、总推理时间减少 9–10%（46% 的 token 减少对应约 10% 加速）——token 减少的好处主要在省成本与上下文容量，而不是延迟
- 无损：带格式说明时理解准确率 83.8% 对 JSON 82.5%，无实质下降，约 2 条记录后就开始比 JSON 划算——记录越多越值得用

**观测**: 粒度：请求级｜观测内容：每条记录的输入token，拆成键/标点/值/结构/空白五类做消融｜数据来源：tiktoken 计数

**动作**: 用观测到的token构成（键占52.6%），识别格式开销来源，据此用列式格式从结构上消除键重复。

**优化方式**: 列式格式：字段名只声明一次、数据按行排列、缩进表示层级，从结构上消除JSON的重复键开销，是无损预处理层｜所属路线：数据格式与表示（列式格式）

---
### [56] LAR: Latent Action Reparameterization for Efficient Agent Inference

**基本信息**: arXiv:2605.18597 | 预印本 | 黄文豪（蒙特利尔大学/Mila）、曾清雯（悉尼大学）等多机构合作（耶鲁、复旦、DeepWisdom、UIUC、Amazon Science、斯坦福、港科大（广州））

**研究内容**: LLM 智能体的动作是逐token生成的低层文本，一次语义简单的行为也要经过几十个决策token，推理成本随“有效决策长度”线性增长，任务越复杂，决策序列越长，延迟和计算开销越难承受。已有的效率优化要么在系统层、硬件层或提示工程上加速单个token的生成、提高吞吐，要么压缩提示词和记忆，但都没有改变“决策单元就是单个token”这个根本结构——每步生成变快了，完成任务的决策步数没有变，总成本仍随任务长度恶化。

论文的核心论点是，效率瓶颈出在动作空间本身的表示上：每个生成的token都是一次显式决策，这种过度细粒度的决策表示让有效决策长度虚增，因此动作表示应该和模型架构、系统设计一样，成为一等建模对象。LAR 主张从轨迹中自动学出一批“潜在动作”，每个潜在动作对应一段多步语义行为，让规划与执行都在更高层的抽象上进行。设计的关键挑战是抽象与可执行性的平衡：纯隐式表示对内部推理有效，但智能体要通过显式、协议受限的接口调用外部工具，动作必须可解码、可执行。LAR 的做法是明确划出“压缩成潜在动作”与“保持显式输出”的边界——把系统提示、工具调用语法、重复配置这类低熵、结构稳定的片段压缩成潜在动作，而具体搜索词、数值实体等高熵参数内容原样保留在显式输出里，避免过度抽象破坏工具交互。

**方法**: 核心概念是“转移等价”：两个动作片段如果不管前面发生过什么，后续行为都差不多，就可以合并成一个潜在动作。实际操作中用片段的下一个token熵 $H(s)$ 近似这个条件——低熵说明后续可预测、与上下文无关，可以安全压缩；高熵内容（如具体搜索词、代码标识符）依赖上下文，必须原样保留以保证可执行性。流水线四步：先从轨迹中提取词级 n-gram 片段，按频率与熵过滤，按频率除熵加一的分数排序取 top-K 构成潜在动作词汇表；再生成“双格式”训练数据，把匹配片段替换成潜在动作符号；然后用轨迹级 KL 蒸馏对齐模型——冻结基座，只训练 LoRA（rank=8）和新嵌入，约 0.1% 参数。推理时潜在动作符号就是普通词表token，零额外开销。渐进抽象实验揭示三个阶段：适度抽象提升性能，抽象边界处达到峰值，过度抽象（高熵内容也被压缩）性能骤降——因为工具调用被破坏。

**实验设置**: 两个 8B 基座（Qwen3-8B、Llama-3.1-8B-Instruct），潜在动作从各基座自己的 rollout 轨迹学习；三个基准覆盖多步推理（TriviaQA）、结构化代码生成（KodCode）、网页工具交互（Mind2Web）；基线和效率方法包括 Vanilla、ReAct、TokenSkip、ACON、ConciseHint；跨基准泛化用 Musique、HumanEval、MBPP；8 张 H200 GPU。

**关键结果**:
- 动作重参数化全面生效：Qwen3-8B 上 TriviaQA 80.09（动作 token −27.1%）、KodCode 54.30（−9.2%）、Mind2Web 39.84（−2.9%），全面优于效率类基线；Llama 上 Mind2Web 28.30（−20.8%）——跨基座、跨任务类型均有效
- 词汇可迁移：用 KodCode 学到的潜在动作词汇直接应用到 HumanEval、MBPP，无需重训，达到或超过 ReAct——训练一次、多任务复用
- 压缩边界是关键：把搜索查询也压缩成潜在动作会导致工具交互整体失败——高熵内容必须显式保留，只压缩低熵结构片段
- 可与其他训练叠加：潜在动作让 GRPO 策略训练收敛更快、更稳；系统级收益温和（token 吞吐最高提升约 17%）——主要收益在决策层，不在吞吐层

**观测**: 粒度：步骤与动作级｜观测内容：整条轨迹的有效决策长度（所有动作token总数）、动作片段的下一个token熵、转移等价性｜数据来源：轨迹统计

**动作**: 用观测到的动作片段熵与转移等价性，决定把哪些低熵片段学成潜在动作、高熵参数内容保留原样。

**优化方式**: 动作重参数化：用熵过滤找出"转移等价"的低熵动作片段，学成潜在动作token，保留高熵参数内容，把"逐token决策"变成"逐潜在动作决策"｜所属路线：数据格式与表示（动作重参数化）

---

## 十二、训练时效率塑造

### [57] SlimSearcher: Training Efficiency-Aware Web Agents via Adaptive Reward Gating

**基本信息**: arXiv:2606.07074 | 预印本 | Zequn Xie、Junjie Wang（浙江大学）、Dan Yang、Jie Feng、Yue Shen、Jian Wang、Jinjie Gu（蚂蚁集团）

**研究内容**: 深度研究类 web 智能体在复杂信息查找任务上能力很强，但计算代价高昂：现有开源智能体为回答一个问题往往要跑几百轮推理（如 MiroThinker）。论文把低效归因于“效率陷阱”，表现为两种失败模式：盲目工具依赖——连常识性问题也调外部搜索，白白消耗 API 调用并增加延迟；表演式推理——在确实需要工具的任务里充满重复循环、反复验证已确认的事实和无产出的探索分支，轨迹冗长却对答案没有实质贡献。二者的共同缺陷是模型无法提取“最小必要路径”。关键观察是：只靠提示词让模型“省着用”几乎无效，效率必须靠训练塑造。

论文指出现有训练范式放大了这些缺陷：SFT 采用只看正确性的拒绝采样，任何答对的轨迹无论多冗余都被当正样本，模型把臃肿模式内化；RL 只奖励端到端成功率，训练中模型靠增加搜索轮次和上下文长度换取精度，出现“效率崩溃”。SlimSearcher 把效率优化贯穿整个训练管线：SFT 阶段做Pareto 式效率筛选，只教“既正确又经济”的轨迹；RL 阶段用级联奖励门控——“答对”“工具花费”“生成长度”三个信号相乘，正确性硬门杜绝模型用幻觉换省token，工具与长度信号不做绝对惩罚，而是以本批采样轨迹里成功路径的最小成本为参照做相对打分，避免“越短越好”的偏差，也防止模型钻奖励的空子。整体把策略推向最小必要路径，实证上可在保持或提升精度的同时大幅减少工具调用轮次。

**方法**: 最终奖励为正确性、工具成本与生成长度三个因子的乘积，任何一个为零整条轨迹拿不到奖励。正确性是硬门：答案相符为 1，否则为 0，防止模型为省token输出幻觉答案。工具不做绝对惩罚，而是以当前一批轨迹里成功路径的最小工具成本为参照，算相对偏差再指数变换到 0–1：只在“同样答对的轨迹之间”比谁更省工具，省多少相对最省的成功路径——绝对惩罚梯度弱，还会带来“越短越好”的偏差。生成长度对token数用同样方法。训练两阶段：SFT 用奖励引导的拒绝采样挑“最小必要路径”做演示，RL 用 GRPO 配同款奖励。

**实验设置**: 4 个基准：XBench-DeepSearch、BrowseComp、GAIA、HLE；骨干是 Tongyi-DeepResearch-30B 和 Qwen3-30B-A3B-Instruct，SFT 与 RL 各用 64 张 H800；搜索用 Serper API、解析用 Jina Reader；SFT 语料 13,863 条轨迹、RL 语料 1,510 对；每项评估跑 3 次取平均；基线含闭源官方报告成绩和开源智能体，外加“提示控制”基线。

**关键结果**:
- 效率优化可以在降本的同时不损精度：Tongyi 骨干 SFT+RL 后 GAIA 工具轮次 20.56→10.61（−48.4%）、token 7,378→4,915（−33.4%），精度反而从 0.682 升到 0.709；BrowseComp 轮次 63.70→47.63，精度 0.410→0.447；四个基准工具轮次整体减少 17–58%——训练时把效率写进奖励是可行路线
- 正确性硬门是防"省token作弊"的关键：去掉它 GAIA 精度从 0.665 崩到 0.136，模型几乎不再探索工具——奖励里必须把正确性当硬约束而非软权重
- 相对参照打分能防"越短越好"的偏差：去掉自适应参照，HLE 轮次从 19.51 膨胀到 31.05 且准确率反而下降——绝对惩罚换不来好的效率—质量平衡
- 效率差距可达一个量级：同一任务 MiroThinker 用 288 个工具、SlimSearcher 用 22 个解决（约 13 倍）；另一任务 MiroThinker 102 个工具仍失败，SlimSearcher 3 个工具成功——开源智能体的默认行为有巨大压缩空间
- token 少不等于智能：DeepSeek-V3 每查询 token 最少（422–2,713）但精度仅 0.038–0.373——选模型不能只看 token 便宜，要看"每成功任务的成本"

**观测**: 粒度：轨迹级（兼token级奖励）｜观测内容：整条轨迹的正确性、工具调用轮数、模型生成的推理token数（不含工具返回）、最小必要路径参照｜数据来源：训练语料与RL轨迹

**动作**: 用观测到的轨迹正确性与工具/长度成本，决定SFT筛选与RL奖励——只教"既正确又经济"的轨迹（训练时手段）。

**优化方式**: 级联奖励门控：正确性、工具、长度三信号相乘写进SFT筛选与RL奖励，用相对参照替代绝对惩罚——属训练时手段，需重训模型｜所属路线：训练时效率塑造（级联奖励门控）

---

## 十三、执行粒度控制

### [58] Agent Capsules: Quality-Gated Granularity Control for Multi-Agent LLM Pipelines

**基本信息**: arXiv:2605.00410 | 预印本 | Aninda Ray

**研究内容**: N 个智能体的管道每轮至少发 N 次 LLM 调用，每次调用都要把系统提示、任务上下文、前面智能体的输出重复付一遍账，论文称这部分为“协调开销”，此外每次调用还承担网络往返、服务端调度、预填充计算等固定成本。合并成一次调用能省掉这些开销，但合并后质量会悄悄下降：带工具的智能体丢了工具，小模型把多智能体指令压成浅显回复。这就是论文提出的“复合执行问题”：什么时候可以安全合并？合并破坏质量时该怎么办？

现有主流框架（CrewAI、LangGraph、Google ADK、MetaGPT、AgentScope 等）都把每个智能体当独立调度单元，没有把多个智能体合批进一次调用的能力，合不合并完全依赖手工工程调试。论文提出 Agent Capsules，一个运行时层，把多智能体管道执行当作带经验质量约束的优化问题自动处理，靠三个机制：组合分数用协调开销、智能体数、工具调用密度、依赖深度四个运行时信号判断何时能安全合并；质量门控把合并输出与逐智能体输出做影子对比，跌破质量底线就回退；升级阶梯在质量持续不达标时从标准合并升到两阶段、再到顺序执行，靠“拆细”而非改写提示词恢复质量——受控负结果实验证实，往合并调用里注入更多上下文只会让压缩更差。

**方法**: 核心是三个机制。其一是组合分数：协调开销token与输出token的比值（权重最高）、智能体数、平均工具调用数、依赖链深度四个信号线性加权，超过阈值才触发合并；这是“行为指纹”而非能力排名，工具调用密度是跨模型的最强区分信号。其二是质量门控：合并输出与逐智能体输出用 LLM 裁判影子对比，滚动 10 次均值低于 0.75 底线就回退，避免单次裁判噪声误触发。其三是升级阶梯：质量持续不达标时从标准合并升到两阶段（工具收集加合并推理）、再到顺序执行，每步都往“拆得更细”走而不是改写提示词。另有自动输出引导（平均输出超过 1500 token每智能体才加“简洁”指令）、预算化结构提示、Anthropic 前缀缓存对齐等开关。

**实验设置**: 主力模型是 Claude Sonnet 和 Haiku，GPT-4o、gemini-2.5-flash-lite 等用于跨厂商验证，覆盖 4 种管道拓扑（5–14 个智能体）；两个对照基准：手工调优 LangGraph 的 14-智能体竞品情报管道（Haiku，每格 15 次运行）；DSPy 的 5-智能体尽职调查管道（Sonnet 干活、Opus 当裁判，7 个任务），并对比 MIPROv2。

**关键结果**:
- 合并调用能省大量输入 token 且不损质量：比手工调优 LangGraph 细化模式少 51% 输入 token（5,121 对 10,475）、合并模式少 42%（3,246 对 5,572），质量均小幅上升（+0.020 / +0.017）——协调开销大头在输入侧，合并是直接有效的省钱手段
- 自动合批优于人工编译：比未编译 DSPy 少 19% token且质量持平；比 MIPROv2 少 68% token、质量 +0.052
- 输出侧也能省：顺序合并加自动输出引导在 Sonnet/Haiku 上输出 token 省 63–64%，质量基本持平——"简洁"指令只在平均输出超 1500 token/智能体时才加，别盲目加
- 质量回退阶梯确实有效：code_review 组质量 0.313→0.724（+0.411，约为裁判最小可检测差异 0.030 的 14 倍），token −10%、延迟 −15%——合并坏了就"拆细"执行，不要靠加上下文硬撑
- 门控决策可靠：控制器在所有实测单元上与先知路由一致，质量门控保住 0.26–0.42 质量点——质量底线设 0.75（滚动 10 次均值）可用

**观测**: 粒度：组级兼调用级｜观测内容：组内合并路径与逐智能体路径的输入/输出token、协调开销比率四个信号、滚动质量均值｜数据来源：框架运行计量

**动作**: 用观测到的协调开销比率与滚动质量均值，决定合并调用还是拆细执行、质量跌破底线时回退。

**优化方式**: 质量门控复合执行：组合分数选执行模式（合并/逐智能体），滚动质量均值跌破底线就回退，升级阶梯从合并→两阶段→顺序执行｜所属路线：执行粒度控制（质量门控复合执行）

---
### [59] Doomed from the Start: Early Abort of LLM Agent Episodes via a Recall-Controlled Probe Cascade

**基本信息**: arXiv:2607.06503 | 预印本 | Kai Ruan 等（中国人民大学高瓴人工智能学院、中科院计算所、杜克大学、中科院自动化所、浙江大学），通讯作者 Hao Sun

**研究内容**: 长程智能体任务的一条轨迹跨多轮交互、消耗数千token，其中大量计算花在注定失败的轨迹上——误解任务、进入不可恢复状态、原地打转。能早期识别并中止，省下的计算可以转给重试、多采样，或者直接降低服务成本。

问题在于用什么信号判断“要失败”。论文发现：行为信号（观察智能体做了什么）在第一轮几乎不比随机好，要到第 3–4 轮才有信息量——可那时超过三分之一的轨迹已经结束；基于内部激活的线性探测器（用模型内部隐藏状态做线性回归的小分类器）第一轮就能达到行为记分器两三轮后的水平。另外，逐轮检查有累积风险：两个各自放行 98% 成功轨迹的门，全局召回可能只有 96%——逐轮保证没法组合成“整个轨迹”的保证。

论文提出“召回受控中止级联”：在前 6 轮每轮放一个基于隐藏状态的线性探测器作为闸门，门阈值用 Clopper-Pearson 精确二项置信下界（对二项分布比例给出的保守置信下界）校准，各轮的召回预算在独立验证集上联合搜索，使“最终成功的轨迹全部通过所有门”的全局召回达到用户指定目标；选定的级联还会在独立数据上冻结，给出与数据分布无关的认证保证。

**方法**: 分四步。门与评分：前 6 轮各设一门，门对仍在运行的轨迹打分——取智能体该轮最后一个动作token的模型内部隐藏状态做 L2 正则逻辑回归（用哪一层由逐层探测器的 AUC 扫描选定），分数超过阈值就中止该轨迹。门校准：对每个候选阈值，用 Clopper-Pearson 精确二项置信下界保证“至少指定比例的成功轨迹通过”——这比普通的分位数校准更保守，但在小样本下也不会失效。全局预算搜索：召回预算向量在网格上搜索，候选必须满足验证集全局召回目标加安全边际，取验证节省最大者部署。事后认证：冻结的级联在独立认证集上重算置信下界，下界低于目标就完全放弃中止——因为级联在见到认证数据前已经固定，这个证书对搜索过程免疫。

**实验设置**: 2 个环境（TextCraft、WebShop）× 3 个模型（Llama-3.2-3B、Qwen-2.5-7B、Qwen3-1.7B）共 6 格，每格 800 条运行轨迹；目标召回 0.90/0.92/0.95/0.97；指标为生成token节省比例与全局成功召回；压力测试 ALFWorld（120 任务 477 条 rollout）；探测器与行为特征记分器对照。

**关键结果**:
- 逐轮门级联全面优于单门与均匀分配：24/24 配置中全部胜出，实测召回与目标偏差至多 0.024——想提前中止就用级联，别用单门
- 收益上限取决于召回目标与环境：90% 召回下最强格 TextCraft/Qwen-2.5-7B 节省 60.2%±6.7（单门仅 40.1%），WebShop/Qwen3-1.7B 达 54.9%±5.7（单门 8.3%，约 6.5 倍）——级联相对单门整体优势 1.5–8.8 倍
- 越严的召回目标越吞收益：95% 目标降到 45.0%±12.5 与 41.5%±15.8；97% 目标下最弱格仍省 12.0%±10.2——按可容忍的召回损失选目标，别一味求高
- 内部激活比行为信号早得多就有信息量：探测器 TextCraft/Qwen-2.5-7B 第 1 轮 AUC 即 0.86，行为记分器第 1 轮几乎不比随机好（最尖锐对比 41.6% 对 9.9%）——行为信号太晚，别用作早期中止依据
- 高目标要付数据代价：严格目标 0.97 时节省缩到 12–17%，但 20/20 种子达标；证书样本复杂度上，114 个成功回合仅能支撑约 0.974 的目标——追求高召回先评估手上成功样本够不够

**观测**: 粒度：轮次级（兼会话轨迹级）｜观测内容：每轮末尾读一次模型内部隐藏状态、被中止轨迹剩余生成token比例、逐轮召回与全局召回｜数据来源：模型内部状态探测器

**动作**: 用观测到的轮末内部隐藏状态，决定是否中止注定失败的轨迹，并联合搜索各轮召回预算使全局召回达标。

**优化方式**: 召回受控提前中止：前6轮每轮放一个线性探测分类器作闸门，阈值用Clopper-Pearson置信下界校准，联合搜索各轮召回预算使全局召回达标｜所属路线：执行粒度控制（提前中止）

---
### [60] The Shadow Price of Reasoning: Economic Perspective on Optimal Budget Allocation for LLMs

**基本信息**: ICML 2026 | arXiv:2606.03092 | Xu Wan、Speed Zhu、Jianwei Cai、Guang Chen、Ximing Huang、Wiggin Zhou、Mingyang Sun（浙江大学、腾讯 HY、北京大学）

**研究内容**: 推理时扩展是提升 LLM 性能的主要手段，但真实部署有严格计算预算。标准做法是统一策略：给每个查询相同的生成上限（固定 max new tokens），这隐含假设所有查询的“计算-效用”曲线都相同，与推理任务的高度异质性冲突。

论文的受控实验发现推理效用呈 S 形，分三个阶段：Strict 相（低于阈值时短轨迹一致失败，效用约为 0）、Surge 相（越过阈值后性能陡升）、Ample 相（继续生成收益递减甚至损害质量）。难题在统一上限下可能停在 Strict 相——算力白花、效用为零；易题被推进 Ample 相——多出来的token只有递减收益。资源受限时，统一分配是系统性浪费。

论文把推理预算分配建模为全局约束优化：给定总token供给，决定每个查询拿多少token，最大化总期望效用。经济学解法：最优时每个活跃查询的token都花到“再多花一个token的收益等于统一边际成本”为止；边际收益够不到这个统一价的查询应该拿零预算（理性放弃）。CLEAR 系统据此实现：轻量预测器估计每个查询的临界阈值（进入有效推理所需的最小计算量）；这个统一边际成本由“所有查询的token总需求恰好等于总预算”这一条件解出（经济学里叫市场出清价），用二分搜索求出，再由 Lambert W 函数给出闭式分配。

**方法**: 效用函数为“移位激增函数”：低于临界阈值效用为零，越过阈值后按激增衰减曲线上升，三个参数分别对应 Strict、Surge、Ample 三相。带预算约束的拉格朗日问题（拉格朗日方法把"约束"加权折进目标函数来求解带约束的优化）给出shadow price条件：最优时每个活跃查询的边际效用都等于同一个shadow price；最优分配为闭式 Lambert W 解（Lambert W 是满足 $W(x)e^{W(x)}=x$ 的数学函数；该超越方程不能代数求解，Lambert W 给出精确解），并给出放弃条件——净盈余为负就放弃该查询；因分配函数单调递减，用二分搜索求市场出清价。实现上分两步：阈值预测器用 DeBERTa-v3-base（86M 参数），训练目标是对生成答案长度的对数回归（作为临界阈值的代理）；衰减参数由平均预算与平均阈值自适应给出。算法对shadow price做二分搜索（40 次迭代），逐查询算出token上限，净盈余为负就零分配。

**实验设置**: 骨干 Qwen2.5-Math-7B 与 Qwen3-30B-A3B（冻结）；评测混合池 MATH-500、AMC-23、AIME-24/25、Minerva、OlympiadBench，合成四种推理流量流（Balanced/Mostly-Easy/Mostly-Hard/U-Shaped）；预算每查询 256/512/1024/2048 token；基线 Uniform、Predictor、TALE-EP、CLEAR(Heuristic)、CLEAR(Auction)、Oracle；代码实验 HumanEval+、MBPP+、BigCodeBench（best-of-4）。

**关键结果**:
- 预算紧张时按查询分配收益最大：256 token/查询下相对 Uniform 绝对精度提升 Balanced +11.6（3.0→14.6）、Mostly-Easy +24.0（9.0→33.0）、Mostly-Hard +5.2（1.0→6.2）、U-Shaped +14.2（4.4→18.6），稀缺时全局精度最高约 3 倍——预算越紧越值得做，预算充足时收益自然收窄
- 代码生成同样适用：best-of-4、每查询 4096 token 下 HumanEval+ 36.8→43.1（+6.3）、MBPP+ 39.4→45.9（+6.4）、BigCodeBench 16.2→19.8（+3.6）
- 收益来自"全局分配 + 理性放弃"而非效用形状：换三角/二次型效用结构收益几乎不变——实现时不用纠结效用曲线的精确形状
- 增益随模型变大收窄：30B 上只有 +0.6~+2.4；分配正确性靠阈值排序而非绝对预测精度，shadow price 是统一的归一化标准——预测器不用太准，排序对了就行

**观测**: 粒度：调用级（请求级分配）｜观测内容：每个查询的token分配量（等价max new tokens）、临界阈值预测、shadow price与预算消耗｜数据来源：阈值预测器 + 预算模型

**动作**: 用观测到的临界阈值预测与shadow price，决定每个查询拿多少token、收益不足的查询直接放弃。

**优化方式**: shadow price预算分配：让每个查询的token用到"边际收益等于统一shadow price"为止，收益不足的查询直接放弃，用Lambert W闭式解求分配｜所属路线：执行粒度控制（预算分配）

---
### [61] Token Budgets: An Empirical Catalog of 63 LLM-Agent Budget-Overrun Incidents, with an Affine-Typed Rust Mitigation as a Case Study

**基本信息**: arXiv:2606.04056 | 预印本 | Sajjad Khan

**研究内容**: LLM-Agent 的预算超支是文档化的生产故障类别：单个重试循环可能在运维人员察觉之前在账户上累积数千美元。论文首先指出，已有的缓解措施都在运行时追踪开销，而"进程内完整性属性——成本承载值不可别名、不可双花、委托后不可使用——只能靠临时包装而非类型系统来保证"，因此超支很容易被代码缺陷或重构引入。

论文的贡献首先是实证性的：一个来自 21 个编排框架、2023–2026 年 63 起已确认生产事故的目录，每起都配有引用的 GitHub issue、维护者或用户陈述以及（如有报告的）美元损失，组织为八簇失败分类法，并辅以 47 条结构条目记录"预算原语缺失"这一条件；分类在 113 个样本上的两名独立标注者 Cohen's κ=0.837。机制上，作为对照该目录评估的缓解方案，论文构建 token-budgets：一个 1180 行 Rust crate（无 unsafe，核心 Budget API 不用 Arc<Mutex>），用 affine ownership 让"克隆、双花、委托后误用"在类型层面成为编译错误，而非运行时需人工避免的危险。美元上限本身是估计器假设下的运行时算术，affine 层提供程序内的完整性，使该算术不可绕过；类型系统保证的是记账完整性，而非对计费诚实性的担保。

**方法**: 1180 行 Rust（无 unsafe）的 affine type 预算建模（affine type 是一种类型约束：每个值最多被使用一次、用完后即失效，从类型层面杜绝复制与双花）：Budget 值不可 Clone/Copy，spend/split/merge 均 move self，用 borrow checker（Rust 编译器在编译期检查引用借用合法性的机制）替代运行时互斥锁；对 63 起事故做八簇失败分类。

**实验设置**: 160 次测试；Python asyncio 对照；单 Agent 场景 4 行 Python 等价。

**关键结果**:
- 类型层面的硬保证能根除超支：同场景 Python asyncio 30/30 次全部超支，Rust affine 版本 0/30——记账完整性别依赖运行时互斥，交给编译期更可靠
- 静态保证不带来误伤：160 次测试零违规、零误拒绝——合法预算操作不会被拦下，可放心使用
- 在线估计误差差异很大，选对口径很关键：静态 4–6×、自适应 2.11×、tokenizer-direct 约 1.0×——要贴近真实计费就用 tokenizer-direct

**观测**: 粒度：调用级｜观测内容：每次 API 调用的 token 消耗（provider 上报）、在线估计器的估计误差（静态/自适应/tokenizer-direct）、预算超支事故的模式与根因｜数据来源：真实运行事故与 API 账单

**动作**: 用观测到的token消耗与估计误差，在编译期把预算建模为不可复制/不可双花的值，从类型层面阻止超支与重复记账。

**优化方式**: 编译时硬保证：Budget 值不可 Clone/Copy，spend/split/merge 均 move self，用 borrow checker 替代运行时互斥锁，从类型层面禁止超支与重复记账｜所属路线：执行粒度控制（预算分配）

---

### [62] Learning to Configure Agentic AI Systems

**基本信息**: arXiv:2602.11574 | 预印本 | Aditya Taparia、Som Sagar、Ransalu Senanayake（亚利桑那州立大学）

**研究内容**: 配置基于 LLM 的 Agent 系统——选择工作流（如投票者、验证者、优化器）、工具、token 预算与提示——涉及巨大的组合设计空间。当前的配置方式主要依赖静态启发式或"厨房水槽"策略：给所有查询用同一套配置、把大量历史与检索证据灌满上下文。论文指出这种做法行为脆弱、浪费算力——简单查询和难题得到相同的资源配置。论文把 Agent 配置形式化为半马尔可夫决策过程（SMDP）——一种"决策点稀疏"的强化学习框架：不像普通 MDP 那样每个时间步都做一次决策并观测一次状态，而是先选一个会持续多个步骤的"选项"跑到底，状态在选项开始时观测、回报在选项结束时一次性累积。这里每个配置就是一个这样的选项：决定了查询开始时的资源配置后，整套配置把整个查询跑完，回报就是任务成功与否减去成本。据此提出 ARC（Agentic Resource & Configuration learner），一个轻量分层策略，为查询动态选择特定配置。

跨推理、工具使用与 agentic 基准，ARC 一致优于预算匹配的工具增强 LLM，平均把推理准确率提升 31.3%、工具使用准确率提升 13.95%，并把 τ-Bench（Airline）Pass@1 从 9.0% 翻倍到 18.0%。结果表明，学习按查询选配置是"一刀切"设计的可行替代。

**方法**: SMDP + 分层 RL：高层选择 workflow/工具/预算，低层组合 prompt；结合 masked RL 与 SFT；所有对比采用 budget-matched 基线，保证"提升"不是配置更贵的假象。

**实验设置**: 推理、工具使用与 agentic 三类基准；所有对比 budget-matched。

**关键结果**:
- 按查询动态选配置比一刀切显著更强：推理准确率 +31.3%、工具使用 +13.95%，τ-Bench（Airline）Pass@1 从 9.0% 翻倍到 18.0%
- 提升不是"用更贵配置换来的"：所有对比采用 budget-matched 基线——同类预算下仍占优，说明按难度分配才是收益来源，而非预算倾斜

**观测**: 粒度：会话与轨迹级（任务级）｜观测内容：任务难度信号、各配置四元组（工具集/prompt模板/token预算/模型）的成本与正确性反馈｜数据来源：真实任务运行

**动作**: 用观测到的任务难度信号，决定每个查询用哪组配置四元组（工具集/prompt模板/token预算/模型）。

**优化方式**: 按查询难度动态选择配置四元组；用预算匹配基线保证比较公平，避免"配置更贵所以更强"的假象｜所属路线：执行粒度控制（预算分配）

---

### [63] A Stackelberg Framework for Resource-Aware LLM Agents: Learning, Repair, and Conditional Guarantees

**基本信息**: arXiv:2606.23026 | 预印本（技术报告） | Baoxun Wang（腾讯平台与内容事业群）

**研究内容**: LLM Agent 越来越多地作为多轮系统运行，必须在有限的计算预算下分配上下文、提示冗长度与工具访问。静态阈值简单，但在异构任务与不断演化的会话状态下很脆弱。论文把资源治理形式化为上下文 Stackelberg 博弈（一种两层的领导者-跟随者博弈：领导者先宣布自己的策略，跟随者再据此选择最优反应，双方都清楚对方的优化目标，刻画"一方先定规则、另一方再适应"的决策场景）：Controller 先承诺一个质量目标与一个成本激励，Executor 随后以关于上下文、提示与工具使用的资源动作响应——这是一个领导者-跟随者结构，Controller（领导者）先行动，Executor（跟随者）据此响应。

论文学习一个条件响应模型来预测 Executor 的反应，据此优化领导者策略，再用真实 API 校准与向经验选择的动作集投影来"修复"策略，使模拟中优化的策略落到真实操作点。对受限博弈，论文给出均衡存在性、跟随者响应稳定性、安全集投影与从替代环境到真实环境迁移（在有界值误差下）的条件性保证。主实验含 300 个被评估轮次的真实 API 运行：相对保守基线，修复后的控制器把平均 token 成本降低 17.4%（p=0.022），质量无显著变化（p=0.44）。论文明确说明理论结果是条件性的、实验未估计遗憾或迁移常数，因此确立的是一个有前景的修复操作点，而非经认证的真实系统均衡。

**方法**: Stackelberg 博弈 + 条件响应模型学习 + 领导者策略优化 + policy repair（真实 API 校准 + 向经验动作集投影），附条件性理论保证。

**实验设置**: 300 个被评估轮次的真实 API 实验；对比保守基线。

**关键结果**:
- 真实 API 上可在不损质量的前提下降本：相对保守基线，修复后的控制器把平均 token 成本降 17.4%（p=0.022），质量无显著变化（p=0.44）——注意理论保证是条件性的，落地的真正依据是 API 校准后的修复操作点

**观测**: 粒度：调用级 + 任务级｜观测内容：任务级质量评分、调用级 token 消耗、真实 API 校准数据｜数据来源：真实 API 运行

**动作**: 用观测到的任务质量与调用级token消耗，决定Controller设什么质量目标与成本激励、Executor选什么执行参数。

**优化方式**: 双层博弈预算控制：Controller 设质量目标与成本激励，Executor 选执行参数；策略修复用真实 API 校准把模拟策略修正到真实操作点｜所属路线：执行粒度控制（预算分配）

---

### [64] Early Diagnosis of Wasted Computation in Multi-Agent LLM Systems via Failure-Aware Observability

**基本信息**: arXiv:2606.01365 | 预印本 | Xianyou Li、Weiran Yan、Yichao Wu、Penghao Liang 等（纽约大学、东北大学）

**研究内容**: 使用工具与多智能体的 LLM 系统把推理、检索与动作交织在一起，会产生长轨迹；当任务失败时，大量 token 已消耗在注定无用的路径上。失败感知的可观测性要在最终答案评估能解释发生了什么之前，先诊断浪费的计算。论文提出一个基于 trace 的框架，针对三智能体架构（orchestrator、search agent、execution agent）：把结构化事件转换为在线信号，覆盖循环、预算压力、低信息增益与工具不稳定四类浪费模式；再叠加离线的语义接地度指标与有选择的 LLM-as-judge 评估。监控必须保持 LLM-free，否则监控自身的 token 开销会抵消它带来的节省。

在相同上限下的 165 条 GAIA 验证 trace 上，98 次运行产出可用最终答案、67 次失败或无答案停止；在被警告的失败运行中，平均 58.1% 的 token 在首次警告之后才消耗，说明存在大量干预机会。10 个任务的 Level-2 试点用警告触发多样化搜索或要求证据，把 post-warning token 占比从基线的 0.638 降到 0.304。结果支持分层设计：廉价的在线信号帮助 orchestrator 重定向或终止冗余行为，更深的语义检查识别已完成的答案是否接地可靠。

**方法**: 三层检测架构——(1) 在线信号层（运行时、LLM-free、循环/预算压力/低信息增益/工具不稳定）、(2) 离线语义层（任务后、接地性评分）、(3) LLM-as-judge（最终手段）；干预策略含强制切换策略、要求证据、增加行为多样性。

**实验设置**: 165 条 GAIA 验证 trace；10 个任务的 Level-2 干预试点。

**关键结果**:
- 失败轨迹的大部分 token 花在"已可判定失败"之后：失败 run 中平均 58.1% 的 token 在首次警告后继续消耗——提前干预的窗口很大
- 干预有效且显著：用警告触发多样化搜索或要求证据，post-warning token 占比从 63.8% 降到 30.4%——监控本身保持 LLM-free，避免抵消节省

**观测**: 粒度：步骤级（兼系统级）｜观测内容：循环检测、预算压力、低信息增益、工具不稳定等在线信号；post-warning token 占比｜数据来源：运行时信号，全部 LLM-free

**动作**: 用观测到的步级信号（循环/预算压力/低信息增益/工具不稳定），判断是否在浪费token，一旦触发就强制切换策略、要求证据、增加多样性。

**优化方式**: 三层检测 + 主动干预：在线信号层实时检测（不调LLM），离线语义层与 LLM-as-judge 兜底；检测到异常后强制切换策略、要求证据、增加行为多样性｜所属路线：执行粒度控制（提前中止/运行时干预）

---

### [65] Stop Wasting Your Tokens: Towards Efficient Runtime Multi-Agent Systems

**基本信息**: ICLR 2026 | arXiv:2510.26585 | Fulin Lin、Shaowen Chen、Ruishan Fang、Hongwei Wang、Tao Lin（浙江大学、西湖大学）

**研究内容**: 多智能体系统（MAS）在数学推理、代码生成、复杂问答等领域表现突出，但不断增长的自主性与运行复杂度常导致关键低效——过度的 token 消耗与源于错误信息的失败。现有方法主要做事后失败归因，缺乏主动、实时的干预来增强稳健性与效率。论文提出 SUPERVISORAGENT，一个轻量、模块化的运行时自适应监督框架：不改变基础 Agent 的架构，由一个 LLM-free 的自适应过滤器触发，在关键节点介入，主动纠正错误、引导低效行为并净化观测。

在具有挑战性的 GAIA 基准上，SUPERVISORAGENT 平均把 Smolagent 框架的 token 消耗降低 29.68%，且不牺牲成功率；在另外五个基准（数学推理、代码生成、问答）与多种 SOTA 基础模型上的大量实验进一步验证了广泛适用性与稳健性。

**方法**: LLM-free 自适应过滤器在三个关键交互点（Agent-Agent、Agent-Tool、Agent-Memory）检查信号，触发时启动 Supervisor 干预（纠正错误、引导低效行为、净化观测）；不修改基础 Agent 架构。

**实验设置**: GAIA + 5 个额外基准（数学推理、代码生成、问答等）；多模型；以 Smolagent 框架为宿主。

**关键结果**:
- 运行时监督能在不牺牲成功率的前提下省 token：GAIA 上平均降 29.68%——不需要改基础智能体架构即可获得
- 泛化性与零开销可兼得：跨 5 个基准、多种模型都成立，监控本身不调 LLM、开销接近零——适合直接插进现有框架

**观测**: 粒度：步骤级（兼系统级）｜观测内容：输出无变化、上下文接近上限等步级异常信号（在 Agent-Agent、Agent-Tool、Agent-Memory 三个交互点）｜数据来源：运行时规则检查，不调 LLM

**动作**: 用观测到的交互点异常信号（LLM-free），决定是否启动Supervisor干预，监控本身不消耗LLM token。

**优化方式**: 零开销运行时监督：LLM-free 过滤器做准入检查，仅在触发时启动 Supervisor 干预，监控本身不消耗 LLM token｜所属路线：执行粒度控制（提前中止/运行时监督）

---

> **文档说明**：本文档覆盖 65 篇论文（2024–2026），内容以 papers/ 目录下的论文原文 PDF 为准，编号与参考文献编号一致。观测—动作配对与汇报综述第 3.10 节对应；优化方式字段区分于动作字段——动作回答"用观测空间做了什么"，优化方式回答"具体用什么方法实现优化"。
